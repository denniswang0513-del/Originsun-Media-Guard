# ─── Originsun Media Guard Pro — Python Dependencies（初裝完整版）───
# 用途：Install_Originsun_Agent.bat 新機安裝時 pip install 的完整清單（含 torch 等重依賴）。
# 與 requirements_agent.txt 的分工：那份是 OTA 更新時 update_agent.py 用的輕量版
# （刻意排除 torch/whisper/TTS/DB）。兩份都活著、各管一條路，別合併也別刪。

# ── Core Server ──
fastapi
uvicorn
python-socketio
python-multipart
aiofiles
pydantic

# ── Utilities ──
customtkinter
xxhash
requests
Jinja2
Pillow
psutil
croniter

# ── Google API ──
google-api-python-client
google-auth-oauthlib
google-auth-httplib2
google-auth

# ── TTS ──
edge-tts
soundfile
torch
torchaudio
f5-tts

# ── Whisper (語音辨識) ──
faster-whisper

# ── Report ──
playwright
