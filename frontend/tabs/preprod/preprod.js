// preprod.js — 前期製作 / 拍攝企劃 (起始頁骨架)
// 目前為佔位頁，之後擴充拍攝企劃單 / 排程 / 勘景 / 分鏡時在此初始化。

export function initPreprodTab() {
    // No interactive elements yet — the page is static markup. Kept as a real
    // init entry so tab-config TAB_LOADERS stays uniform and future logic has
    // a home without touching the loader registry.
}
