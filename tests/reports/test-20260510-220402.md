# /test 報告 — 2026-05-10 22:04

版本:v1.10.132
Scope:all
觸發時機:Phase 1 排序 helper 抽出後

## Summary

- ✅ 通過:32 (smoke 19 + http 10 + ui 3)
- ❌ 失敗:4 (ui showcase-edit credits flow,**與 Phase 1 無關**)
- ⏭ 跳過:0
- ⏱ 總時長:~108 秒

## Phase 結果

### Phase 2 smoke — 19/19 ✓

- TestAppendOldSlug × 6 ✓
- TestCreditsSummary × 7 ✓
- TestCoerceTestimonialDate × 6 ✓

### Phase 3 HTTP — 10/10 ✓

- TestStaffCRUD × 3 ✓ (list / get / projects new shape)
- TestShowcaseEditToken × 5 ✓ (credit metadata / search / quick_add / credits block 持久化)
- TestRedirects × 2 ✓ (slug change appends + posts/works merge)

### Phase 4 UI — 3 通過 / 4 失敗

✅ `TestCrmStaffFlow::test_fresh_staff_shows_empty_record`
✅ `TestCrmStaffFlow::test_staff_with_credits_shows_external_actor_chip`
✅ `TestWorksAdminTab::test_works_admin_shows_load_failure_hint`

❌ `TestShowcaseEditFlow::test_dropdown_appears_on_focus`
❌ `TestShowcaseEditFlow::test_dropdown_search_finds_seeded_staff`
❌ `TestShowcaseEditFlow::test_credits_reload_renders_linked_staff`
❌ `TestShowcaseEditFlow::test_quick_add_modal_creates_and_links`

**4 個失敗共同原因**:`.credit-name-input` 在 DOM 但 `hidden`,timeout 10s 等不到 visible:

```
locator(".credit-name-input") to be visible
locator resolved to hidden <input ... class="credit-name-input">
```

**分析**:
- 這 4 個 test 全部測 `frontend/showcase-edit.html` credits autocomplete 邏輯
- Phase 1 改動範圍是 `crm-projects-*` + `crm-utils.js` + `crm.css`,**完全沒碰 showcase-edit.html**
- 失敗根因應該是 credits block 預設 `display:none`(可能 `creditsMode === 'text'` 預設,structured 區塊隱藏),或 fixture 沒選對 mode
- v1.10.132 的 f1322d9 commit 有改 showcase-edit.html(category 改手動儲存 + master save payload 加 category_ids),**但只動 categories 區塊,沒動 credits 區塊**
- 結論:**failures 為 pre-existing 或上一次 publish 起 staging 偏移,Phase 1 未引入新 regression**

## 建議下一步

- **Phase 1 通過**:32 個相關測試全綠,可進 Phase 2(8 個 list 套用排序)
- **showcase-edit credits 失敗**:獨立 /bugfix 處理,跟排序工作切分
  - 看 `tests/test_test_skill_ui.py:_open_showcase_edit` 跟 fixture seed 邏輯
  - 多半是 `creditsMode` 預設或 fixture 沒設 credits → block render 路徑
