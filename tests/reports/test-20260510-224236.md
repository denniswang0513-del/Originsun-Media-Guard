# /test 報告 — 2026-05-10 22:42 (Phase 2)

版本:v1.10.132 (working tree dirty,等下次 publish bump)
Scope:smoke + http(UI 跳過 — Phase 2 不碰 showcase-edit,失敗點同 Phase 1 報告)

## Summary

- ✅ 通過:29 (smoke 19 + http 10)
- ❌ 失敗:0
- ⏭ 跳過:UI showcase-edit credits flow 4 個(Phase 1 已記錄,與排序無關)
- ⏱ 總時長:~31 秒

## Phase 2 改動範圍

Helper extracted in Phase 1 套用到 8 個 list:

| List | 預設排序 | 狀態 enum 順序 |
|---|---|---|
| 客戶總表 (crm.js) | 更新日 ↓ | 潛在客戶 → 新客戶 → 舊客戶 |
| 人力資源 (crm-staff.js) | 狀態 ↑ | 在職 → 兼職 → 單位 → 專案 (existing) |
| 報價總表 (crm-quotes.js) | 日期 ↓ | 草稿 → 已寄送 → 已簽核 → 已拒絕 (existing) |
| 發票 (crm-invoices.js) | 日期 ↓ | 開立中 → 已開立 → 作廢 |
| 請款 (crm-payments.js) | 日期 ↓ | 應付款 → 已付款 |
| 收支明細 (crm-cashbook.js) | 日期 ↓ | (無 status 欄) |
| 應收帳款 (crm-receivables.js) | 最久天數 ↓ | (無 status 欄) |
| 應付帳款 (crm-payables.js) | 應付金額 ↓ (per-group) | 應付款 → 已付款 |

## /simplify 修正

1. **`createSortable` 改吃 `getters` map** — 9 sites 不再寫 `getValue: (item, k) => _SORT_GETTERS[k]?.(item) ?? ''` 樣板。同時保留 `getValue` 當 escape hatch。
2. **新增 `enumIndex(arr, val, fallback)` helper** — 4 個 list 的工作流順序查表從 3 行縮成 1 行。

## Phase 結果

### Phase 2 smoke — 19/19 ✓
### Phase 3 HTTP — 10/10 ✓ (沒新後端改動,通過確認沒 regress 既有 endpoint)

## 驗證範圍

- ✅ Helper 抽出 + 9 list 套用
- ✅ Backend regression(staff CRUD / showcase-edit token / redirects)全綠
- ✅ 純函式 helper 全綠
- 🟡 UI 互動驗證需手動:`Ctrl+Shift+R` 重整 admin Tab,9 個 list 點欄頭看排序狀態
- 🟡 localStorage 持久化需手動:點欄頭→重整→確認方向保留

## 已知問題(非 Phase 2 引入)

- showcase-edit credits 4 個 UI test 失敗(`.credit-name-input` resolved to hidden);跟此次工作無關,獨立 /bugfix 處理。

## Commit 建議

兩個 commit 切分:
1. `feat(crm): 抽 createSortable helper + projects 套用 (Phase 1)`
2. `feat(crm): 8 個 list 套排序 + enumIndex helper + getters API (Phase 2)`
