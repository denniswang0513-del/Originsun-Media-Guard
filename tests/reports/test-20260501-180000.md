# /test 報告 — 2026-05-01 18:00

版本：v1.10.119（2026-05-01）
分支：feature/website-m
Scope：`smoke`

## Summary

- ✅ 通過：**19**
- ❌ 失敗：**0**
- ⏭ 跳過：**2 phases**（Phase 3 HTTP / Phase 4 UI — `--scope=smoke` 限定）
- ⏱ 總時長：0.47 秒

## Phase 結果

### Phase 1：環境探勘 ✅
- `.venv/Scripts/python.exe` 存在
- working tree：累積 9 階段變動（27 modified + 8 untracked），符合預期未 commit
- version.json：v1.10.119（注意：build_date 仍是 5/1，提醒：累積大量變動但未 bump）

### Phase 2：smoke 純函式 ✅
全 19 case 通過：

#### `TestAppendOldSlug` (6/6)
- ✅ test_basic_change_appends
- ✅ test_same_slug_no_append
- ✅ test_numeric_old_slug_skipped
- ✅ test_self_loop_protection
- ✅ test_empty_old_slug_no_append
- ✅ test_already_in_old_slugs_no_duplicate

#### `TestCreditsSummary` (7/7)
- ✅ test_empty
- ✅ test_single_block_single_entry
- ✅ test_two_blocks_two_entries
- ✅ test_skips_empty_name
- ✅ test_truncates_at_30_chars
- ✅ test_only_first_two_entries_taken
- ✅ test_malformed_block

#### `TestCoerceTestimonialDate` (6/6)
- ✅ test_iso_string_to_date
- ✅ test_does_not_mutate_input
- ✅ test_empty_string_to_none
- ✅ test_invalid_date_to_none
- ✅ test_non_string_passthrough
- ✅ test_missing_key_passthrough

### Phase 3：HTTP — 跳過（`--scope=smoke`）

需要：`--scope=http` 或 `--scope=all`

### Phase 4：UI — 跳過（`--scope=smoke`）

需要：`--scope=ui` 或 `--scope=all`

## 建議下一步

**短期（今天）**：
1. version.json bump（v1.10.119 → v1.10.120）— 累積 9 階段變動沒升版號
2. 跑 `/test --scope=http` 驗證 HTTP endpoints（需要 master + NAS PG）
3. 跑 `/test --scope=ui` 驗證 Playwright UI flow（需要 chromium）

**部署前驗證**：
4. e2e 手測 SEO 301（改 work slug → 重 build → curl 舊 URL → 看 301）— /test 不涵蓋
5. CLAUDE.md 更新（記錄演職員系統 + SEO 301 雙保險的 9 階段變動）
6. 跑 `/publish` 推到 NAS website-api + OTA 代理機

## 結論

**核心 helper 邏輯通過 19 個邊界 case 驗證**：slug change detection / credits summary / date coercion 都符合設計（含純函式不污染 input、自循環防護、純數字 slug skip、超長截斷等 corner case）。

下一個 commit 階段 — **Phase 3 HTTP 測試**（fixture + httpx 直打 master endpoints）— 可開工。
