# Google API Credentials (NEVER commit these)
credentials/token.json
credentials/credentials.json

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
*.egg-info/

# Logs
*.log
*.txt.bak
server_test_log.txt

# Build artifacts
Originsun_Agent.zip
dist/
build/

# Node.js / Astro (Phase M website/)
node_modules/
.astro/
.DS_Store

# IDE
.vscode/
.idea/
*.swp

# Config (runtime only, not committed)
settings.json
bookmarks.json
scheduled_jobs.json
job_history.json
watcher_config.json
watcher_history.json
update_status.json

# OS
.DS_Store
Thumbs.db

# AI Models (20GB+, downloaded on demand) — 只 match 根目錄，避免誤傷 db/models/
/models/

# Voice cache (runtime generated)
voice/

# Filmstrip cache (runtime generated)
cache/

# Test resources & output (video files, logs, reports)
Test/
test_zone/
Desktop/

# Test artifacts in root
out_*.mp4
_test_*.mp4
frame_*.jpg
test_*.mp4
test_*.vtt
test_*.srt

# Test artifacts in frontend
frontend/test_*.mp3

# Test concat output
tests/test_concat/*.mov
tests/test_concat/*.mp4
tests/test_concat/*.txt

# Python embed runtime (very large, downloaded on install)
python_embed/

# FFmpeg binaries (bundled separately)
ffmpeg.exe
ffprobe.exe
get-pip.py

# API keys & runtime secrets
api_keys.json
publish_history.json

# Publish rollback artifacts
_publish_rollback/

# Test artifacts (one-off)
TestWorkFlow_CRM/
test_state.png

# Claude Code working directories
.claude/worktrees/
.claude/plans/
.claude/launch.json
.rebuild_meta.json
*.rebuild_meta.json
