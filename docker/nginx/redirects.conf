# SEO 301 redirects — Auto-generated by sync_redirects_to_nas()
# DO NOT EDIT BY HAND — changes will be overwritten next sync.
#
# 來源：GET /api/website/redirects（聚合所有 published post.old_urls）
# 首次部署時這裡是空的（initial empty）— admin 在編輯器填 old_urls 後，
# /publish 流程會 regenerate 這份檔案 → docker cp 進容器 → nginx -s reload。

# (no redirects yet — admin 在編輯器填 old_urls 後會自動填入)
