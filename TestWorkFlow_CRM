## 程式碼審查與測試流程
1. 使用/simplify檢查測試

2. **程式碼靜態檢查**
    - 執行靜態分析工具進行語法與邏輯驗證
    - 檢查型別標註、import 依賴與模組引用
    - 驗證非同步函式簽名與 await 用法
    - 檢查資料庫查詢與 ORM 用法

3. **本機單元測試**
    - 執行 `pytest tests/unit/` 驗證核心函式
    - 測試資料庫 CRUD 操作、JSON 序列化、錯誤邊界
    - 確保型別檢查與例外處理正確

4. **本機整合測試**
    - 執行 `pytest tests/integration/` 進行 API 測試
    - 驗證資料庫同步、檔案持久化、日誌輸出
    - 確保 Socket.IO 事件與非同步函式正常運作

5. **實機部署驗證**
    - 在 https://mediaguard.originsun-studio.com/ 部署最新版本
    - 新增測試資料筆件，驗證端到端 CRUD 流程
    - 確認無功能迴歸與資料完整性

6. **實機 UI 驗證**
    - 在網頁介面執行建立、修改、刪除、搜尋操作
    - 驗證表單驗證與錯誤訊息正確顯示
    - 確認資料狀態一致且無孤立記錄

7. **品質確認**
    - 檢查後端日誌（`_current_task_log_file` 與 `_status_log_buffer`）
    - 驗證資料庫與 JSON 檔案同步正確
    - 確保所有使用者操作均有適當的 Socket.IO 事件回饋

8. 測試完成後commit