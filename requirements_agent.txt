# ─── Originsun Media Guard Pro — Agent Dependencies (Minimal) ───
# This file is used by update_agent.py on agent machines.
# Do NOT add server-only packages (torch, TTS, whisper, sqlalchemy).
#
# 版本釘定（2026-07-07）：以生產 master python_embed 實裝版本為準。
# 浮動版本曾讓 fresh install 拉到 fastapi 0.139 弄壞 route 內省（CI 實測）。
# 升級套件 = 有意識的行為：先在 dev 升級測過、更新這裡的釘版、再發版。

# ── Core Server ──
# 🔴 fastapi 停在 0.13x：0.141 起 `include_router` 不再把路由攤平到 `app.routes`
#    （變成巢狀的 _IncludedRouter），而「哪些路由是公開的」「哪些帶錢的有守衛」
#    那幾支測試都是靠攤平的清單在掃 —— 升上去它們會整組變瞎。要升的話那是
#    一件獨立的事：先把列舉路由的 helper 改成會往下走，再升。
fastapi==0.136.3
uvicorn[standard]==0.41.0
python-socketio==5.16.2
python-multipart==0.0.32
aiofiles==25.1.0
pydantic==2.10.6

# ── Utilities ──
xxhash==4.0.1
requests==2.33.0
psutil==7.2.2
Jinja2==3.1.6
Pillow==12.3.0
croniter==6.2.2

# ── Google Auth (for login) ──
google-auth==2.48.0
google-auth-oauthlib==1.3.0
google-auth-httplib2==0.3.0
google-api-python-client==2.190.0

# ── 傳遞相依但**明釘版本**（2026-09-19 /health）──
# 這幾個不是我們直接 import 的，是 fastapi／google-auth 拉進來的。以前沒釘，
# 結果「裝到哪一版」由各台機器 pip 解析當時決定 —— 09-17／09-18 兩份 health
# 報告說「機隊 0 個漏洞」是假陰性，實際上十台都帶著 starlette 的 10 條。
# 釘在這裡，機隊才真的跟著修。升級前先在 dev 跑完整測試（見檔頭那句）。
starlette==1.6.0           # 10 條 CVE（修在 1.0.1+）；fastapi 只要求 >=0.46，沒有上限
cryptography==50.0.1       # 6 條，google-auth 拉進來的
pyasn1==0.6.4              # 6 條，同上
httplib2==0.32.0           # 2 條，google-api-python-client 拉進來的
anyio==4.15.1              # 2 條，fastapi／httpx 拉進來的

# ── Auto-detected by publish ──
httpx==0.28.1

# ── 影像紀錄（media_log）：QR + HEIC 縮圖（函式層 import，掃描抓不到）──
# 缺套件時 QR 端點回 503、HEIC 僅無縮圖，皆不擋主流程
qrcode==8.2
pillow-heif==1.4.0

# ── Auto-detected by publish ──
pillow_heif

# ── Auto-detected by publish ──
pymupdf

