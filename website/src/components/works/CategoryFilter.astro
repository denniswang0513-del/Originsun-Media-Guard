---
/**
 * CategoryFilter.astro — 作品集分類 pills（白底）
 */
import type { ICategory } from "../../types/category";
import Tr from "../ui/Tr.astro";

interface Props {
    categories: ICategory[];
    activeSlug?: string | null;
    basePath?: string;
}
const { categories, activeSlug = null, basePath = "/works" } = Astro.props;
---

<div class="flex flex-wrap gap-2 mb-12 justify-center">
    <a href={basePath}
       class:list={[
           "px-5 py-2 text-xs tracking-[0.25em] uppercase border transition-colors",
           !activeSlug
               ? "border-neutral-900 bg-neutral-900 text-white"
               : "border-neutral-300 text-neutral-500 hover:border-neutral-900 hover:text-neutral-900",
       ]}>
        <Tr k="works_filter_all" />
    </a>
    {categories.map(cat => (
        <a href={`${basePath}?category=${cat.slug}`}
           class:list={[
               "px-5 py-2 text-xs tracking-[0.25em] uppercase border transition-colors",
               activeSlug === cat.slug
                   ? "border-neutral-900 bg-neutral-900 text-white"
                   : "border-neutral-300 text-neutral-500 hover:border-neutral-900 hover:text-neutral-900",
           ]}>
            <Tr zh={cat.name_zh} en={cat.name_en || cat.name_zh} />
            <span class="text-neutral-400 ml-1">({cat.count})</span>
        </a>
    ))}
</div>
