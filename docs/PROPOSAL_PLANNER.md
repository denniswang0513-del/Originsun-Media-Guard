# 提案企劃工具 — 方法論整理（工作稿）

> **狀態**：✅ **階段一～三全部實作於 dev**（2026-07-28，已跑 /simplify 修 18 項）：
> 資料層（`preprod_proposals.plan` JSONB + migration）、三端點（PUT /plan、
> PATCH /plan/cell 樂觀鎖+FOR UPDATE、GET /plan/meta?since= 短路）、
> `plan-matrix.js` 可換膚元件（錨點卡/提示問句/兩範例/409 衝突 UI/dirty-check/
> 30s 共編輪詢橫幅）、overlay「企劃」分頁、`/proposal-plan.html` 獨立登入網址
> （官網白底、清單+直達 ?pid=）。測試：API 27 + overlay 18 + 獨立頁 15 + 輪詢 3 全過。
> **未發版**（要上生產走 /publish）。選配未做：Socket.IO 即時同步、Markdown 匯出。
> **落點決策（2026-07-28）**：擴充現有提案庫 P-b（`preprod_proposals`），
> 在提案詳情 overlay 加「企劃」分頁；用途 = **想案子的思考輔助**
> （非強制 SOP、非客戶提案書輸出）。
> **追加（同日）**：另開獨立登入網址 `/proposal-plan.html`（官網白底）
> 供內部分享，**多人可共編**同一份企劃 —— 見 §7。
> **素材來源**：2026-07-28 上午講師分享「以兒童為中心的思考」
> （2026 兒童影片暨教育節目相關課程）。

---

## 0. 方法論註冊表（多套並存）

工具**不綁死單一方法論**。每套方法論是一顆具名按鈕，掛在註冊表裡；
owner 之後上不同的課，就長出第二顆、第三顆按鈕。

| id | 按鈕標籤 | 來源 | 狀態 |
|----|---------|------|------|
| `child_program_huang` | **兒童節目的製作方法（黃鴻儒）** | 2026-07-28 課堂分享「以兒童為中心的思考」 | 🔨 整理中（第 1~2 節） |
| — | （未來的方法論） | — | — |

**互動位置**：提案詳情 →「企劃」分頁，尚未開始規劃時顯示方法論按鈕列，
點一顆即以該模板開一份 plan。已開始的 plan 直接渲染，不再問。

**一筆 plan 必須記住 `template_id` + `template_version`** —— 否則日後改模板
會把已填的舊案子畫面弄亂（格子對不上、問句換掉）。

---

## 1. 方法論骨架 — `child_program_huang`

### 核心動作
**四個提問 × 三個視角** 的 4×3 矩陣。同一組四個提問，站在三個不同立場各問一次，
12 格填完就是一份企劃的思考底稿。

### THEME（整張表的題目）

工作表最上方有一列 `THEME:` 橫跨全表 —— **整個矩陣掛在一個主題底下**，
先有題目才開始填 12 格。課堂範例的 THEME 即「帶兒童到山裡的小溪玩紙船」。

### 列：四個提問（固定順序、遞進）

以 **owner 提供的實際工作表**為準（2026-07-28）：

| # | 提問 | 在問什麼 |
|---|------|---------|
| 1 | 找到合適的兒童 | 選角 — 這件事需要誰 |
| 2 | 建立共同目標 | 動機 — 讓他覺得這是「他的」目標 |
| 3 | 發展流程 | 執行 — 他能自己決定什麼、哪裡會失去耐性 |
| 4 | 亮點 | 高潮 — 最棒的一刻 |

> 📌 **標籤是全域統一的，不隨視角改寫。**
> 第 7 張投影片出現過「**介紹**合適的兒童／**說明**共同目標／**呈現**流程」，
> 那是講解時強調「換立場後你要做的動作不同」的修辭，**不是工作表的欄位名**。
> 工作表三欄共用同一組列標題 → 資料模型**不需要** per-lens 標籤覆寫。

### 欄：三個視角

| # | 視角 | 素材狀態 |
|---|------|---------|
| 一 | 參與的兒童 | ✅ 心理前提（3 張）+ 四組提示問句 |
| 二 | 觀看的兒童 | ✅ 心理前提（3 張）+ 四組提示問句 |
| 三 | 拍什麼（＝腳本 × 拍攝） | ✅ 決策錨點（2 張）+ 分鐘數/基調；**無提示問句**（可能本來就沒有） |

### 右欄是看著左欄長出來的

第 7 張投影片副標：**「先把上一頁的答案貼進左欄，再一起發想『觀看的孩子』」**，
表格呈現為 `四個提問｜參與的孩子｜觀看的孩子` 三欄。
§2.5 的實例也證實第三欄是從左兩欄推導的。

→ **三欄不是各自獨立的表單**，右欄要看著左欄寫。
但這在數位化後**自動滿足** —— 一張三欄表格，左欄本來就在旁邊；
投影片要「貼進左欄」只是因為一次只能顯示一頁。
**不需要做分頁式推進或唯讀左欄的機制**（見 §3 原則 6）。

### 投影片標記（待確認語意）

- 矩陣總表頁（第 3 張）標「**現場共同決定**」
- 各視角填寫頁（第 5、7 張）標「**現場填寫**」

→ 可能代表方法論區分「共識決」與「個人發想」兩種格子。**待 owner 確認**是否要在工具中體現。

---

## 2. 各視角內容

> 每個視角的素材有兩片：**心理前提卡**（該視角的思考錨點）+ **四組提示問句**
> （該視角 × 四個 HOW）。兩片到齊，這一欄才算完整。

### 2.1 第一視角｜參與的兒童 ✅ 完整

#### 貫穿句

> 只想一件事：**怎麼讓這個孩子帶著熱情，真的產生行動。**

#### 心理前提（三張卡 — 回答下面四個提問時要靠這三句話）

| 前提 | 說明 |
|------|------|
| **只做想做的事** | 他只想做感興趣，或是被誘發興趣的事情。 |
| **想被肯定** | 他希望有自信，由自己作主，而不是被指揮。 |
| **討厭等待／無聊** | 等待、重來、反覆講解，都是他放棄的時刻。 |

#### 提示問句

（以工作表為準，✅ 完整無缺漏）

| HOW | 提示問句 |
|-----|---------|
| 1 找到合適的兒童 | 這件事需要什麼樣的孩子？／你打算怎麼找？／如何確認他適合？ |
| 2 建立共同目標 | 什麼是兒童能處理的目標？／怎麼讓他覺得這是「他的目標」？／他想完成或證明什麼？ |
| 3 發展流程 | 環境能提供什麼？／有什麼是他能自己決定、自己動手？／流程哪一段最可能讓他不耐煩？ |
| 4 亮點 | 他覺得最棒的一刻會是什麼？／什麼能讓他真正興奮？ |

#### 課堂範例案

**帶兒童到山裡的小溪玩紙船**

---

### 2.2 第二視角｜觀看的兒童 ✅ 完整

引導語：**螢幕前的兒童想看什麼？**

#### 心理前提（三張卡）

| 前提 | 說明 |
|------|------|
| **想看沒看過的** | 陌生的地方、沒見過的做法，才留得住他的眼睛。 |
| **想知道為什麼** | 他要理由，不要結論。過程比答案更有吸引力。 |
| **想「如果我也可以」** | 他要能想像自己站在那裡。 |

頁尾另有一句：**「當然，說一個好故事，是必須的。」**
位置在三張卡下方，像是該視角的總結／底線要求，不是第四張卡。

#### 提示問句

引導語：**先把上一頁的答案貼進左欄，再一起發想「觀看的孩子」**

（以工作表為準，✅ 完整無缺漏）

| HOW | 提示問句 |
|-----|---------|
| 1 找到合適的兒童 | 同齡觀眾會好奇他什麼？／他擁有什麼吸引人的特質？ |
| 2 建立共同目標 | 這個目標如何簡單說清楚？／如何讓觀眾跟著期待？ |
| 3 發展流程 | 流程裡哪些是觀眾沒看過的？／哪裡讓他想知道「為什麼」？ |
| 4 亮點 | 哪一刻會讓觀眾想「如果是我，也想要…」？／什麼時刻會讓觀眾驚呼？ |

**前提卡 → 問句的對應**（前提卡不是背景說明，是問句的出處）：

- 想看沒看過的 → 3「流程裡哪些是觀眾沒看過的？」
- 想知道為什麼 → 3「哪裡讓他想知道『為什麼』？」
- 想「如果我也可以」→ 4「哪一刻會讓觀眾想『如果是我，也想要…』？」

---

### 2.3 第三視角｜拍什麼（第三階段＝腳本 × 拍攝）✅ 可實作（無提示問句是常態）

投影片標題：**第三階段｜腳本 x 拍攝**　引導語：**拍攝內容要怎麼設計？**

> 🔑 **這個階段跟前兩個不同構。**
> 前兩階段的卡片是「兒童心理」（他想要什麼），第三階段的卡片是
> **「你要做的決定」** —— 逼你在衝突處選邊、並畫出現場底限。

#### 決策錨點（兩張卡 — 注意是 2 張，不是 3 張）

| 決策 | 說明 |
|------|------|
| **主角在意什麼？觀眾在意什麼？** | 兩者不一定一致。當它們衝突時，你站哪一邊？這個選擇會決定整集的語氣。 |
| **百分百真實重要嗎？底限是什麼？** | 重來、引導、設計，哪些可以做、哪些不能做？先把底限講清楚，現場才不會臨時鬆動。 |

第一張卡**直接指名前兩欄**：「主角」＝參與的兒童、「觀眾」＝觀看的兒童。
→ 印證第三階段是前兩欄的**收斂**，但收斂方式是「選邊 + 畫底限」，不是綜合摘要。

#### 規格設定（owner 補充，非投影片文字）

第三階段還有兩個前兩階段沒有的動作：

- **設定分鐘數**
- **設定基調**（呼應第一張卡的「這個選擇會決定整集的語氣」）

→ 這兩項是**單一值欄位**，不屬於 4×3 的任何一格。

✅ **分鐘數確認掛 lens 層**（2026-07-28 實例演練）：工作表的欄頭寫
`拍什麼 (影片長度：6 mins)` —— 就顯示在該欄的標題列。
基調未在實例中出現，暫比照辦理。

#### 提示問句

✅ **拍什麼欄的提示問句＝「四個方向」的提問**（owner 2026-07-28 最終確認：
「這個是九宮格的最右邊四格的內容」）。

| HOW | 提示問句 |
|-----|---------|
| 1 找到合適的兒童 | 你需要建立這個角色嗎？ |
| 2 建立共同目標 | 你需要讓觀眾清晰了解你的目標嗎？ |
| 3 發展流程 | 需要清晰的講解說明流程嗎？ |
| 4 亮點 | 這一集經營的亮點，是觀眾想看、現場小朋友也做得很開心？ |

> 📝 修訂軌跡：先誤判「等第 9 張投影片」→ 再誤判「本來就沒有問句」→
> 實例其實早有線索（紙船例的「建立共同目標 × 拍什麼」格寫著
> 「需不需要建立角色？」）→ owner 點破：四個方向就是這欄四格的內容。

---

### 2.4 擬腳本｜四個方向 ✅（位置已定：＝拍什麼欄四格，非獨立區塊）

> 🔑 **2026-07-28 owner 最終定位：「這個是九宮格的最右邊四格的內容。」**
> 四個方向不是矩陣之後的獨立階段 —— 它們就是「拍什麼」欄四格的思考提問。
> 擬腳本的思考發生在矩陣裡，不在矩陣外。原「腳本區獨立視圖／同頁下方區塊」
> 的兩版設計**全部作廢**；本節以下保留為方法論內容的紀錄。
> `plan.directions` 資料結構保留於後端（相容），UI 不再渲染獨立區塊。

> owner 口述（2026-07-28）：
> 「依照四個方向擬腳本，有些可以放有些不用放。」
> 「這些都是**腳本的策略**，有各種不同的方式，來構成一隻片子的樣子。
> 　做法會回到**節目的特性**。」
> 「**每一個都不是取捨**，只是在思考這些方向需要思考的問題而已。
> 　**這些問題的答案，會形成這個節目的樣貌。**」

**「四個方向」＝四個提問**（✅ owner 確認）。

#### 四個方向的思考提問（owner 口述）

| 方向 | 這個方向要想的問題 |
|------|---------|
| 一 **合適** | 你需要建立這個角色嗎？ |
| 二 **目標** | 你需要讓觀眾清晰了解你的目標嗎？ |
| 三 **流程** | 需要清晰的講解說明流程嗎？ |
| 四 **亮點** | 這一集經營的亮點，是觀眾想看、現場小朋友也做得很開心？ |

#### 🔑 這不是取捨，是思考（owner 2026-07-28 明確更正）

雖然措辭是「你需要…嗎？」，**這些不是 yes/no 的開關**。
它們是每個方向該想清楚的問題，**答案匯集起來形成這個節目的樣貌**。

因此：

- ❌ **沒有 include/exclude 勾選**。工具不提供「這個方向不放」的開關。
- ❌ **不做檢核 gate**。取捨組合沒有對錯，工具不該判人不合格。
- ✅ **每個方向就是一個回答欄位**（自由文字）。四個答案合起來
  就是這支片子的樣貌描述。
- ✅ **上游依據＝節目特性**。怎麼答不是憑感覺，是回推自這個節目是什麼。

> 📌 早先的「有些可以放有些不用放」講的是**結果**——
> 想過的東西不會全部進到片子裡；不是要在工具裡做一個挑選機制。

#### 對模型的影響

- **方向層是回答，不是布林**：
  `directions[how_key] = { answer: str, updated_at }`
- **腳本區在矩陣下方，同一頁**（原案「獨立視圖」已改）：
  §2.5 發現方向提問會出現在拍什麼格內 → 兩者的思考是交織的，
  拆成兩個畫面反而不合實際。四個方向各一段：提問 + 回答欄。
- **方向四可並排兩欄輔助思考**：它問的是亮點在「參與」與「觀看」兩端
  是否都成立，UI 把兩格並列會比較好想（是輔助，不是驗證）
- **矩陣內容完整保留**：沒有刪除或隱藏機制

#### 待確認

1. 腳本階段的產出長什麼樣 — 分場大綱？段落順序？還是就是這四段回答？
2. 要不要新增「節目特性」欄位（plan 層，與 THEME 並列）——
   若四個方向的答案要回推自它，它就該在畫面上，而不是留在腦袋裡。

---

## 2.5 實例演練（owner 提供，2026-07-28）

**THEME：六歲小孩去溪裡玩紙船**　　**拍什麼欄頭：影片長度 6 mins**

| 四個提問 | 參與的兒童 | 觀看的兒童 | 拍什麼（6 mins） |
|---|---|---|---|
| **找到合適的兒童** | 1. 特質：喜歡大自然的孩子、願意手作、不怕髒<br>2. 怎麼找（增加命中機率）：去拍攝地點找（蹲點成本大）、營隊、科展、徵選 | 1. 外顯、外表的認同：性別、穿著<br>2. 內在特質：專注神情、樂於分享（率先發言）、要有不同的角色所長 | **X（屬於前置）** |
| **建立共同目標** | 如何讓小朋友的目標（內在動力）跟節目目標一致：設計目標 or 任務？<br>觸發事件（意料 & 意外）<br>eg. 競賽 | 摺紙的過程跟結果<br>建立從 A 點到 B 點<br>未知的結果 | 1. 需不需要建立角色？<br>2. 片頭：介紹 A 到 B（動畫）<br>3. 示範帶：船的主觀視角 |
| **發展流程** | 1. 製作不同材質的紙船、競賽<br>2. 認識溪邊環境：提升自主力、就地取材<br>3. 輔助角色（有可能是成人）：引導衝突<br>4. 節目素材是否豐富：進階版 | 折法、材質、環境、創意（特質）、示範、競賽方式（怎麼比） | 環境<br>材質：葉子、紙、樹枝<br>競賽（怎麼比） |
| **亮點** | 1. 船飄起來的時刻：船怎麼樣飄起來（嘗試 & 失敗 & 成功）<br>2. 紙船抵達最終想要去的地方：成功抵達的成就感<br>3. 競賽：設計紙船戰力（材質、折法、排除障礙） | 1. 分工：賽道、船<br>2. 失敗 & 成功<br>3. 物理知識<br>4. 船的主觀視角（船長） | 1. 創造緊張感、懸疑感 |

### 從實例讀到的結構事實

1. **分鐘數在「拍什麼」欄的欄頭** → ✅ 確認為 **lens 層欄位**（非 plan 層）。
   顯示位置就是該欄的標題列。
2. **格子內容是有序條列**（1. 2. 3.），一格多則，項目內常用
   「標籤：內容」的形式（如「特質：喜歡大自然的孩子…」）。
   → 格子是 textarea 即可，不需要做成結構化的條目清單；
   但顯示時要保留換行。
3. **格子可以是「X + 原因」**：`X（屬於前置）`。
   → **空白 ≠ X**。空白是還沒想，X 是想過了且不適用。
   工具要能表達這個差別（可為一個「不適用」標記 + 原因欄，
   或就讓使用者自己寫 X —— 傾向後者，不增加機制）。
4. **拍什麼欄是從左兩欄推導的**，對照痕跡明顯：
   - 觀看「船的主觀視角（船長）」→ 拍什麼「示範帶：船的主觀視角」
   - 參與「競賽」+ 觀看「競賽方式（怎麼比）」→ 拍什麼「競賽（怎麼比）」
   - 觀看「折法、材質、環境」→ 拍什麼「環境／材質：葉子、紙、樹枝」
   → 再次印證 §2.3：第三欄是收斂，並排前兩欄是必要的。
5. **四個方向的提問會出現在拍什麼格內**：
   「建立共同目標 × 拍什麼」寫著「1. 需不需要建立角色？」——
   那正是方向一的思考提問。→ 擬腳本的思考與拍什麼欄是交織的，
   不是嚴格的先後兩段。
6. **拍什麼欄沒有預填提示問句也能運作**。原先推測的「第 9 張投影片
   ＝拍什麼 × 四個提問」也許根本不存在 —— 這欄可能本來就靠
   §2.3 的兩張決策卡 + 左兩欄推導，不另給問句。**待確認。**

### 用途

這份實例應該**內建成模板的範例資料** —— 新開一份 plan 時可一鍵載入
「看範例」，比任何說明文字都好懂。

---

## 2.6 實例二｜登山總動員（10歲）

**真實播出節目的案例**，比 §2.5 的紙船練習更具參考價值。
以下為 **owner 提供的工作表版**（已校正先前從第 10 張投影片轉錄的錯字）。

**THEME：登山總動員（10歲）**　　拍什麼欄頭：**無分鐘數**

| 四個提問 | 參與的兒童 | 觀看的兒童 | 拍什麼 |
|---|---|---|---|
| **找到合適的兒童** | 以攀樹為例<br>人際網絡連結：嚮導的孩子<br>與山林的關係是熟悉的 | （空白） | 學習歷程、情緒 |
| **建立共同目標** | 線上聊天-實體見面（體驗與討論）-行前準備（一起練習）-線上會議（旅行團出發） | 兩個特質相異的孩子在這趟冒險中，經過重重現實考驗（日落時間、路徑變化、實際樹種），找到屬於他們的合作方式，創造他們的 chill 時光？ | 訪談取代討論拍攝現場的拍攝需求 |
| **發展流程** | 找出敘事線：獨立找樹、架繩上樹<br>找出核心：對兩人的挑戰/新的東西<br>熟悉場域 | 攀樹過程＋兩個孩子＝步驟、困難與好玩＝好奇 | 行前會議<br>有情境的再一次<br>攝影分組 |
| **亮點** | sth new：獨立完成架繩並上樹-和夥伴到山林去玩（沒有大人） | 暖身＆壓力：冒險 | 場勘、彩排、畫重點＝共同的圖像<br>定點＝boom、移動＝mini mic |

> 📝 先前從投影片轉錄的版本有數處錯字（太陽西下時間→日落時間、
> 有情境的寫一次→再一次、影排→彩排、量重點→畫重點、
> something new→sth new），已以工作表為準改正。
> **再次印證 §5 決策：以工作表為準，投影片為輔。**

### 從實例二讀到的結構事實

1. **第三欄在投影片裡叫「腳本 x 拍攝」**，工作表叫「拍什麼」——
   **同一欄的兩個名字**（呼應第 8 張的階段標題）。
2. **第三欄同時承載「拍什麼」與「怎麼拍」**。
   紙船例是拍攝內容（片頭動畫、示範帶、環境材質）；
   登山例大量是**拍攝方法**（訪談取代討論、攝影分組、場勘彩排、
   定點 boom／移動 mini mic）。
   → 這欄自由度比另兩欄高，UI 不要用問句把它框死。
2b. **格子可以是空白 —— 空白是合法狀態。**
   「觀看的兒童 × 找到合適的兒童」在此例整格空白（不是漏拍或漏填）。
   → 呼應 §2.4「不做檢核 gate」：工具**不得**提示「你少填了這格」。
   對照 §2.5 的 `X（屬於前置）` —— 三種狀態各有意義：
   有內容／明寫 X 表示不適用／留白表示這案子不需要。
3. **列標題有兩套說法並存**（非隨視角變 — 呼應 §1 的更正）：
   - 工作表版：找到合適的兒童／建立共同目標／發展流程／亮點
   - 投影片版：介紹合適的兒童／說明共同目標／呈現流程／**營造亮點**
   → 模板存**一組 key**，label 可帶 `alt_label`；預設顯示工作表版。
4. **「觀看的孩子 × 2 HOW」寫成一個長懸念問句**
   （「…能在這趟冒險中…找到合作的方式…嗎？」）。
   → 觀看視角在「目標」這格，寫法可以是**戲劇問句**而非條列。
   這是實例一沒示範到的寫法，值得放進範例庫。
5. **兩個實例的風格差異很大**（練習 vs 真實製作），
   內建範例應**兩個都給**，讓使用者看到同一套框架的不同用法。
6. **THEME 慣例帶年齡**：「六歲小孩去溪裡玩紙船」／「登山總動員（10歲）」。
   → 年齡是這套方法論的關鍵前提（決定孩子能處理什麼、觀眾是誰），
   THEME 欄位的 placeholder 應提示帶上年齡。
7. **分鐘數是選填**：紙船例欄頭有 `影片長度：6 mins`，登山例沒有。
   → lens 層欄位，可留空，不強制。

---

## 3. 程式結構草案

### 概念模型
```
TemplateRegistry（方法論註冊表 — 決定有幾顆按鈕）
└─ Template
   ├─ id / label / author / source / version
   ├─ theme_label : 題目欄的標籤（本模板為 "THEME"）
   ├─ hows[]   : key / label / 副標（全域統一，不隨視角改）
   └─ lenses[] : key / label / 引導語 / 貫穿句
       ├─ premises[]         : 錨點卡 { kind, title, body }
       │                       kind = "psychology"（前兩階段）| "decision"（第三階段）
       │                       張數不固定（前兩階段 3 張、第三階段 2 張）
       ├─ fields[]           : 該階段的單值欄位 { key, label, type }
       │                       例：第三階段的 分鐘數 / 基調
       └─ prompts[how_key][] : 該視角在該 HOW 下的提示問句（可為空）

Plan（使用者填的內容，掛在一筆提案上）
├─ template_id / template_version   ← 必存，決定用哪套模板渲染
├─ theme : str                      ← 整張表的題目，先填才開始
├─ cells[lens_key][how_key] : { answer: str, updated_at, updated_by }
├─ directions[how_key] : { answer: str, updated_at, updated_by }
│                        四個方向的思考回答 — 合起來形成節目的樣貌
│                        （**不是** include/exclude 布林，見 §2.4）
└─ field_values[lens_key][field_key] : str   ← 分鐘數 / 基調 等單值
```

**關鍵設計原則**

1. **提示問句與心理前提卡屬於模板，不是使用者資料。** 模板獨立存放並帶版本號 ——
   日後方法論長出第五個 HOW 或第四個視角時，改模板不動 migration。
2. **plan 綁 template_id + version。** 渲染時用 plan 記錄的版本，不是最新版；
   模板改了不會把已填的舊案子畫面弄亂。
3. **矩陣維度由模板決定，不寫死 4×3。** 下一套方法論可能是 3 步驟直線、
   或 5×2 —— UI 照模板的 `hows × lenses` 動態長，不做「四個提問」的硬編碼。
4. **THEME 先行。** 題目是整張表的根，沒填 theme 就別開始填格子
   （可做成軟提示，不強制擋）。
5. **各階段不必同構。** 錨點卡的性質（心理 vs 決策）、張數（3 vs 2）、
   有沒有單值欄位（分鐘數／基調）都因階段而異。模型要容得下差異，
   UI 也不能假設三欄長得一樣。
6. **三欄同時可見。** 方法論要求「先把上一頁的答案貼進左欄」——
   那是紙上流程的限制（投影片一次只能顯示一頁）。
   **數位化後這個需求自動滿足**：一張三欄表格，左欄本來就在旁邊。
   → 不需要做「分頁式推進 + 唯讀左欄」的機制，一張表格即可。
   要保留的是**引導**（欄頭的錨點卡、格子的提示問句），不是推進的順序。

### 資料層
- 使用者填的內容 → `preprod_proposals` 新增 **JSONB 單欄** `plan`
  （不為每格開 column；方法論一定會改）
- 模板定義 → 前端純資料常數（見 §6）

### 前端落點
`frontend/tabs/proposals/proposals.js` 的詳情 overlay（現為無分頁、
body 兩欄 `.prop-info-col` + `.prop-refs-col`，約 L163-282）
→ 頂端加分頁列，現有內容收成「基本資料」，並列「企劃」分頁。

---

## 4. 待補素材清單

| # | 缺什麼 | 影響 | 狀態 |
|---|--------|------|------|
| 1 | 第二視角「觀看的兒童」心理前提 | 該欄的思考錨點 | ✅ 2026-07-28 補齊 |
| 2 | 第二視角提示問句 | 矩陣中間欄的引導 | ✅ 2026-07-28 工作表補齊 |
| 3 | 4 HOW 亮點的提示問句 | 曾以為被遮擋漏一句 | ✅ 工作表確認就兩句，無缺漏 |
| 4 | 第三階段的錨點卡 | 第三欄的思考錨點 | ✅ 2026-07-28 補齊（決策型 2 張） |
| 5 | 「拍什麼」是視角還是收斂？ | 決定第三欄的 UI 形態 | ✅ 兩者皆是 — 同構的欄，但錨點是決策 |
| 6 | 第三階段「拍什麼 × 四個提問」提示問句（推測第 9 張） | 矩陣第三欄沒有引導 | 🔶 **可能不存在** — 實例顯示這欄靠決策卡+左兩欄推導即可運作 |
| 7 | 分鐘數／基調 掛 lens 層還是 plan 層？ | 資料模型小決策 | ✅ lens 層（欄頭顯示，實例已證） |
| 8 | 矩陣之後的擬腳本階段 | 決定工具有沒有「產出」階段 | ✅ 四個方向各一個回答欄，答案形成節目樣貌 |
| 8a | 「四個方向」＝四個提問？ | 腳本視圖怎麼分段 | ✅ 2026-07-28 確認：是 |
| 8b | 四個方向的思考提問 | 缺一不可 | ✅ 2026-07-28 四個到齊 |
| 8c | 是不是取捨機制（include/exclude）？ | 決定方向層的資料型別 | ✅ **不是** — owner 明確更正，改為回答欄位 |
| 8d | 腳本階段的產出形態（分場大綱／段落順序／就是這四段回答） | 決定要不要做匯出 | ⬜ 缺 |
| 9 | 有沒有檢核判準（什麼情況不該往下走） | 決定要不要做 gate／檢查清單 | ✅ **不做** — owner：取捨是策略非對錯，回推自節目特性 |
| 9a | 要不要新增「節目特性」欄位（plan 層，與 THEME 並列） | 取捨的上游依據該不該上畫面 | ⬜ 待 owner 確認 |
| 10 | 開場投影片（前提、適用範圍）與結尾投影片（產出物） | 決定工具的頭尾 | ⬜ 缺 |

### 素材編號對照（投影片左上角圓圈數字）

| # | 標題 | 狀態 |
|---|------|------|
| 1 | （未提供 — 推測是開場） | ⬜ |
| 2 | 把企劃拆成四個提問 | ✅ |
| 3 | 兒童中心 × 四個提問（12 格總表，標「現場共同決定」） | ✅ |
| 4 | 第一階段｜參與的兒童（心理前提） | ✅ |
| 5 | 參與的兒童 × 四個提問（標「現場填寫」） | ✅ |
| 6 | 第二階段｜觀看的兒童（心理前提） | ✅ |
| 7 | 觀看的兒童 × 四個提問（標「現場填寫」） | ✅ |
| 8 | 第三階段｜腳本 x 拍攝（決策錨點） | ✅ |
| 9? | 拍什麼 × 四個提問 | ⬜ 推測存在 |
| 10 | 登山總動員的兒童中心 × 四個提問（真實案例） | ✅ |
| 11+ | 收斂／產出／結尾 | ⬜ |

> 投影片編號連續，缺號即缺素材 —— 用這張表確認有沒有漏拍。

---

## 5. 決策紀錄

| 日期 | 決策 | 理由 |
|------|------|------|
| 2026-07-28 | 落點 = 擴充提案庫 P-b，不另開 Tab | 客戶／報價／成案／win-loss 全部現成接得上，填完直接是一筆提案資產 |
| 2026-07-28 | 定位 = 個人思考輔助，非團隊 SOP | owner 明示；內容深度優先於協作與權限 |
| 2026-07-28 | 資料存 JSONB 單欄 + 模板版本化 | 方法論會持續演化，避免每次改欄位都要 migration |
| 2026-07-28 | 方法論做成**具名按鈕的註冊表**，首顆＝「兒童節目的製作方法（黃鴻儒）」 | owner 明示；日後上不同的課要能各自成一顆按鈕，不是改寫同一套 |
| 2026-07-28 | 矩陣維度由模板決定，UI 不硬編碼 4×3 | 下一套方法論的形狀未知（可能是直線步驟或其他矩陣） |
| 2026-07-28 | **以 owner 的實際工作表為準，投影片為輔** | 工作表是真正在用的操作介面；投影片有講解用的修辭（如 HOW 動詞改寫），會誤導資料模型 |
| 2026-07-28 | ~~HOW 標籤依視角覆寫~~ **撤銷** | 工作表三欄共用同一組列標題，投影片的「介紹／說明／呈現」是修辭不是欄位名 |
| 2026-07-28 | 加 `theme` 欄（THEME 題目先行） | 工作表最上方有 THEME 列橫跨全表，是整張表的根 |
| 2026-07-28 | 錨點卡加 `kind`（psychology／decision）、張數不固定、lens 可帶 `fields[]` | 第三階段的卡是決策不是心理、只有 2 張、還多出分鐘數／基調兩個單值欄位 |
| 2026-07-28 | 腳本獨立視圖；不採用的**不刪** | owner：「依四個方向擬腳本，有些可以放有些不用放」— 矩陣是素材庫不是成品，想過沒用的點子留著複盤 |
| 2026-07-28 | ~~取捨標記在方向層 `include: bool`~~ **撤銷** | owner 明確更正：「每一個都不是取捨，只是在思考這些方向需要思考的問題而已」 |
| 2026-07-28 | 方向層改為 `directions[how_key].answer`（自由文字） | owner：「這些問題的答案，會形成這個節目的樣貌」— 是回答不是開關 |
| 2026-07-28 | **不做檢核 gate、不做挑選機制** | 沒有對錯、沒有取捨；工具的角色是承接思考，不是驗證或篩選 |
| 2026-07-28 | 分鐘數掛 **lens 層**，顯示在該欄欄頭 | 實例演練的欄頭即 `拍什麼 (影片長度：6 mins)` |
| 2026-07-28 | 格子用 **textarea（保留換行）**，不做結構化條目清單 | 實例的格子是有序條列 + 「標籤：內容」自由書寫，硬做成條目會綁死寫法 |
| 2026-07-28 | 「六歲小孩去溪裡玩紙船」**與「登山總動員」兩個實例都內建為範例資料** | 一鍵載入看範例，比說明文字好懂；兩例風格差異大（練習 vs 真實製作），並陳才看得出框架的彈性 |
| 2026-07-28 | 四個提問存**一組 key**，label 帶 `alt_label`（工作表版／投影片版） | 兩套說法並存且都出現在正式素材，但指的是同四項 |
| 2026-07-28 | **空白格是合法狀態，工具不得提示「少填」** | 登山例的「觀看 × 找到合適的兒童」本來就空白；三種狀態（有內容／X 不適用／留白）各有意義 |
| 2026-07-28 | 分鐘數**選填** | 紙船例欄頭有 6 mins、登山例沒有 |
| 2026-07-28 | ~~擬腳本四方向為獨立區塊~~ **撤銷** — 四方向提問＝拍什麼欄四格的提示問句 | owner：「這個是九宮格的最右邊四格的內容」；紙船例格內出現方向提問早是線索。核心句改掛拍什麼欄 motto；`plan.directions` 後端保留相容、UI 不再渲染 |

---

## 6. 實作計畫（2026-07-28 擬，尚未動工）

> 前置：§1~§2.6 的方法論素材已足以實作；缺的（開場/結尾投影片、
> 「節目特性」欄位）不擋任何一階段。

### 6.0 範圍聲明

**要做**：一份掛在提案上的思考矩陣 + 四方向腳本回答，能填、能存、
能載入兩個內建範例。

**明確不做**（皆有決策依據，見 §5）：

| 不做 | 為什麼 |
|------|--------|
| 完成度檢核 / 必填 gate | 取捨是策略非對錯；空白與 X 都是合法狀態 |
| 格子的挑選 / 勾選機制 | owner 更正：四個方向不是取捨 |
| AI 自動生成內容 | 這是「逼自己想」的工具，代寫會抽掉它的意義 |
| PDF / 簡報匯出 | 先不做輸出管線（階段四選配 Markdown） |
| **逐格權限** | 有 `preprod_plan` 模組就能填整份；內部協作不做細粒度權限 |
| **對外 token 分享頁** | owner 選定「帳號登入頁（內部用）」，不做免登入公開連結 |
| 自動合併同格衝突 | 文字合併沒有正確解，猜錯比報錯糟 → 409 讓人自己決定 |
| 分頁式推進 + 唯讀左欄 | 數位化後三欄同時可見，紙上流程的限制不存在 |

### 6.1 資料層

**A. Model**（[`db/models.py`](db/models.py) `PreprodProposal`，約 L867）

```python
plan = Column(JSONB, nullable=True)   # 企劃矩陣（見 docs/PROPOSAL_PLANNER.md §3）
```

**B. Migration**（[`main.py`](main.py) `_crm_cols` 清單，約 L455-471）

```python
("preprod_proposals", "plan", "JSONB"),
```

既有 DB 靠這行補欄，新 DB 由 `create_all` 直接帶。**兩者都要改**，
只改 model 會讓生產庫少欄位。

**C. Schema**（[`core/schemas.py`](core/schemas.py)，接在 `ProposalPayload` 後）

```python
class ProposalPlanPayload(BaseModel):
    """企劃矩陣（黃鴻儒兒童節目方法論）— 結構鬆散，以模板決定形狀。"""
    template_id: str
    template_version: str
    theme: Optional[str] = ""
    cells: Optional[dict] = None          # {lens: {how: {answer, updated_at}}}
    directions: Optional[dict] = None     # {how: {answer, updated_at}}
    field_values: Optional[dict] = None   # {lens: {field: str}}
```

⚠️ **加大小上限**：端點裡 `len(json.dumps(payload)) > 200_000` → 413。
JSONB 沒有天然上限，個人工具也不該讓單筆膨脹到失控。

### 6.2 模板常數（前端純資料）

**新檔** `frontend/tabs/proposals/plan-templates.js`（純資料，無邏輯）

```js
export const PLAN_TEMPLATES = {
  child_program_huang: {
    id: 'child_program_huang',
    label: '兒童節目的製作方法（黃鴻儒）',
    source: '2026-07-28 課堂分享「以兒童為中心的思考」',
    version: '1.0.0',
    theme_label: 'THEME',
    theme_placeholder: '例：六歲小孩去溪裡玩紙船／登山總動員（10歲）— 建議帶上年齡',
    hows: [
      { key: 'fit',     label: '找到合適的兒童', alt_label: '介紹合適的兒童' },
      { key: 'goal',    label: '建立共同目標',   alt_label: '說明共同目標' },
      { key: 'process', label: '發展流程',       alt_label: '呈現流程' },
      { key: 'highlight', label: '亮點',         alt_label: '營造亮點' },
    ],
    lenses: [ /* participant / viewer / content — 內容見 §2.1~2.3 */ ],
    directions: [ /* 四方向的思考提問，見 §2.4 */ ],
  },
};
export const PLAN_EXAMPLES = { /* 紙船 + 登山總動員，見 §2.5 / §2.6 */ };
```

**為什麼放前端而不是後端**：純顯示用資料，後端只存 blob，不需要
解讀模板。放前端零 API 往返、改字不用重啟。
**代價**：日後若要後端匯出或 AI 輔助，得把這份資料搬到 `core/`——
純資料搬家是複製貼上，可接受。

**版本策略**：模板改動時 `version` 進版，**舊版留在同一檔案**
（`PLAN_TEMPLATES.child_program_huang_v1_0_0`），plan 依 `template_version`
取對應版本渲染；查不到就 fallback 最新版並在畫面上標「模板已更新」。

### 6.3 API

> ⚠️ **原案「整份 plan 覆寫」已因共編需求作廢**（見 §6.8）。
> 多人同時編輯時，整份覆寫會讓後送出的人把別人剛填的格子全部蓋掉。

`plan` 是 `preprod_proposals` 的欄位，既有 `GET /{pid}` 直接帶回即可
（改回應組裝，不新增讀取端點）。寫入分兩顆：

| 方法 | 路徑 | 說明 |
|------|------|------|
| PATCH | `/api/v1/proposals/{pid}/plan/cell` | 寫**單一格**：`{lens, how, answer}` 或 `{direction, answer}` |
| PUT | `/api/v1/proposals/{pid}/plan` | 整份寫入 — 僅用於「開始一份企劃」「載入範例」「清空」 |

**為什麼分兩顆**：日常輸入走 PATCH，一次只碰一格 → 兩人同時填**不同格**
完全不衝突（實務上最常見的情境）。整份 PUT 只在明確的整體操作時使用。

**同格衝突**（兩人同時改同一格）採**樂觀鎖**：

- PATCH 帶 `base_updated_at`（前端載入該格時的時間戳）
- 伺服器端該格的 `updated_at` 較新 → 回 **409** + 伺服器現值
- 前端跳提示「這格已被其他人更新」，並列兩個版本讓人自己決定
- **不做自動合併**（文字合併沒有正確解，猜錯比報錯糟）

**其他約束**：

- 守衛沿用 `proposal_auth`（2026-08-11 由 `_check_auth` 改名；模組清單以該函式為唯一正本；2026-08-03 起含
  `crm_projects` — 提案×專案整合）——
  **`preprod_plan` 已在 `core/auth.py` `ALL_MODULES`，RBAC 三處同步免了。**
- 只寫 `plan` 一欄，不碰 proposal 其他欄位
  （避開 §5 記過的地雷：整包 `model_dump()` 洗掉未送欄位）
- 每格寫入記 `updated_by`（登入者 username）—— 共編下要知道是誰改的

**伺服器端實作要點**（共編正確性的真正所在，API 形狀只是表面）：

- ⚠️ **PATCH 必須在單一交易內原子更新**：兩個 PATCH 同時進來、各自
  read-modify-write 整包 JSONB → 後 commit 的把前者的格子蓋掉。
  解法擇一：`SELECT … FOR UPDATE` 鎖列後改（簡單、夠用），或
  `jsonb_set()` 讓 PG 在 UPDATE 內原地改格（免鎖但 SQL 較繞）。
  **選 FOR UPDATE** —— 單列鎖、毫秒級持有，個位數使用者無爭用問題。
- **`updated_at` 由伺服器蓋章**（客戶端時鐘不可信）；PATCH 回傳該格
  新 `updated_at`，前端以它更新下一次的 `base_updated_at`。
- **單格上限 20KB**（整份 200KB 上限留在 PUT）——
  一格貼進整篇腳本就該擋。
- **加 `GET /plan/meta` 輕量端點**：只回
  `{cells: {lens: {how: updated_at}}, directions: {...}, theme_updated_at}`
  —— §7.4 的 30s 輪詢打這支，不用每次抓全文比對。

### 6.4 前端

**A. overlay 加分頁**（[`proposals.js`](frontend/tabs/proposals/proposals.js) `openDetail`，約 L194-282）

`.prop-panel-head` 下加分頁列；`.prop-panel-body` 現有兩欄
（`.prop-info-col` + `.prop-refs-col`）整個包成「基本資料」分頁，
並列「企劃」分頁。切換用 CSS `display`，不重新 fetch。

**B. 企劃分頁的三個狀態**

1. **未開始** → 方法論按鈕列（目前一顆）＋「看範例」兩顆
2. **進行中** → THEME 輸入 + 矩陣 + 腳本區
3. **範例模式** → 唯讀渲染範例，頂端橫幅「這是範例，不會存檔」

**C. 矩陣**

- 一張表格：列＝四個提問、欄＝三個視角，**三欄同時可編輯**
- 欄頭：視角名 + 引導語 + `fields`（拍什麼欄的分鐘數／基調 inline input）
- 欄頭下方：**錨點卡**（可摺疊，預設收合；展開顯示 2~3 張卡）
- 格子：`<textarea>`，placeholder = 該視角×該提問的提示問句
  （沒有問句的格子 placeholder 留空，**不要編造**）
- 存檔：blur 或停止輸入 1.5s 後 debounce PUT，右上角顯示「已儲存」

**D. 腳本區**（矩陣下方，同一頁不另開視圖）

四個方向各一區塊：思考提問（灰字）+ `<textarea>` 回答。
§2.5 發現方向提問會出現在拍什麼格內 → 兩者同頁比較貼近實際思考。

**E. 行動端**

三欄在窄螢幕改**縱向堆疊**（視角一整欄 → 視角二整欄 → 視角三整欄），
不做橫向捲動。這與 §5「不做分頁式推進」不衝突：窄螢幕是排版退化，
不是流程限制。

### 6.5 分階段交付

| 階段 | 內容 | 可驗收的成果 |
|------|------|-------------|
| **一** | 6.1 資料層 + 6.3 API + 6.4A/B/C 的裸矩陣（無錨點卡、無 placeholder） | 能開、能填 12 格、重整後還在 |
| **二** | 模板引導層：錨點卡、提示問句 placeholder、THEME placeholder、分鐘數/基調 | 不看 doc 也知道每格要寫什麼 |
| **三** | 腳本區（四方向）+ 兩個內建範例 | 一整套流程走得完 |
| **四**（選配） | 匯出（Markdown 純文字先行）、與 CRM 專案的連動 | 有需要再說 |

每階段結束跑 `/simplify`，各自 commit（owner 慣例，見 memory）。

### 6.6 驗證方式

- `.venv\Scripts\python.exe -m py_compile main.py core/schemas.py routers/api_proposals.py db/models.py`
- 前端：HTML tag 平衡、`window.*` 與 onclick 引用一致、ES module import 路徑
  （新檔三件都要過：`plan-templates.js` / `plan-matrix.js` / `proposal-plan.html`）
- dev 8001 手測：新增提案 → 開企劃 → 填 → 重整 → 內容還在
- **共編手測**：兩個瀏覽器分頁同開一份 → 各填不同格（互不干擾）→
  同改一格（後存的收到 409、兩版本並列、先存的字沒消失）
- **雙色系截圖**：SPA overlay（深）與 `/proposal-plan.html`（白）各一張，
  對照確認元件變數兩邊都吃到
- migration 驗證：dev 庫（`mediaguard_dev`）確認 `preprod_proposals.plan` 欄位存在
- **不發版**。本機 dev 驗完，要上生產另外走 `/publish`（見 CLAUDE.md 2.1 共存守則）

### 6.7 已知風險

| 風險 | 對策 |
|------|------|
| `plan` JSONB 無上限、貼大量文字會膨脹 | 端點 200KB 上限回 413 |
| 模板改版後舊 plan 渲染錯位 | 舊版模板留檔 + `template_version` 對應；查不到 fallback 並標示 |
| debounce 存檔在關 overlay 時掉最後一次輸入 | 關閉前 flush 一次；離開頁面 `beforeunload` 保險 |
| 三欄 textarea 在 1280px 以下太窄 | 斷點改縱向堆疊；textarea 可拉高 |
| 生產庫沒跑到 migration | `_crm_cols` 是 startup 每次跑的冪等 ALTER，重啟即補 |

---

## 7. 獨立網址 + 共編（2026-07-28 追加需求）

> owner：「我希望提案企劃可以有一個獨立網址可以分享，顏色風格採取官網配置。」
> 決策：**帳號登入頁（內部用）** + **可一起填（共編）**。

### 7.1 頁面規格

**新檔** `frontend/proposal-plan.html`

比照 [`media-log-workspace.html`](frontend/media-log-workspace.html) 的
「獨立登入網址」家族做法（同族：`website-admin.html`、`my.html`）：

| 項目 | 規格 |
|------|------|
| 網址 | `/proposal-plan.html`（清單）／`/proposal-plan.html?pid=<id>`（直達某份企劃） |
| 閘門 | 未登入 → 登入卡（帳密 + Google GSI）；登入後檢查 admin 或 `api_proposals.proposal_auth` 的模組清單（2026-08-03 起含 `crm_projects`） |
| robots | `<meta name="robots" content="noindex,nofollow">` |
| 主機 | **兩邊都有**（2026-08-07）：登入模式在 master／foundry（需 CRM 後端）；公開 `?t=` 連結由 NAS 對外容器 serve，master 關機也開得了 —— 見 `core/public_assets.py` 與 `routers/api_proposals.public_router` |
| 分享方式 | 直接把網址貼給同事，對方登入後即進到同一份企劃 |

**登入後的兩層畫面**

1. **清單**（無 `?pid`）：列出可見提案，標示哪些已有企劃、哪些還沒開始
2. **企劃**（有 `?pid`）：THEME + 矩陣 + 腳本區，即 §6.4 的內容

**既有基建復用（不要重造）**

- 登入後請求一律走 **`authFetch`**（已在 `frontend/js/shared/utils.js`，
  memory 明示勿再複製一份）；Google 登入沿用 GSI credential 模式。
- 清單資料用既有 `GET /api/v1/proposals`，回應**加一個計算欄 `has_plan`**
  （`plan IS NOT NULL`）—— 清單只要旗標，不要把整包 plan 帶回列表。
- ⚠️ **XSS**：共編後格子內容是「別人寫的字」，所有 answer 渲染必須過
  `esc()`；textarea 的 value 賦值走 DOM property（`el.value = …`）而非
  模板字串內插。409 並列「伺服器版本」時同樣要 escape。

### 7.2 視覺：官網配置

直接沿用 media-log-workspace 已驗證過的一套（**不重新設計**）：

```
--red: #c9372c;  --ink: #262626;  --sub: #737373;  --line: #e5e5e5;  背景 #fff
```

- sticky header（logo + 品牌字 + 登入者 + 登出），`backdrop-filter: blur(12px)`
- eyebrow 小標（`letter-spacing: .35em`、大寫、`--sub`）
- 按鈕：`.btn-primary`（深底白字、大字距）／`.btn-ghost`（描邊）
- 圓角一律 `2px`（官網調性，不用大圓角）

### 7.3 🔑 這條需求回頭改了元件的做法

企劃 UI 現在要同時出現在**兩個色系**：

| 掛載點 | 色系 |
|--------|------|
| CRM SPA 提案詳情 overlay（§6.4） | 深色 |
| `/proposal-plan.html` 獨立網址 | 官網白底 |

→ **企劃 UI 必須從第一行就寫成可換膚的獨立元件**，不能綁在 overlay 裡。
比照影像紀錄元件的既有做法（memory 有記）：

- 元件自帶 CSS 變數 `--plc-*`（plan component），**深色為預設**，
  定義在元件自己的 `:root`
- 白底靠 `html.plan-theme-light` 覆寫
  （specificity `html.plan-theme-light` (0,1,1) > `:root` (0,1,0)，一定贏）
- 元件不直接寫死顏色字面值，一律吃變數

**新檔** `frontend/tabs/proposals/plan-matrix.js` —— 純元件，
匯出 `renderPlan(container, { proposal, template, readonly })`。
overlay 與獨立網址**都呼叫它**，差別只在外層 class。

> ⚠️ 這推翻了 §6.4A 的隱含假設（把矩陣直接寫進 overlay 的模板字串）。
> 先做元件、再讓 overlay 掛它 —— 順序反了會寫兩份。

### 7.4 共編

**衝突處理**：見 §6.3 —— 逐格 PATCH + 樂觀鎖 409，不自動合併。

**看得到別人**（分階段，非第一版必要）：

| 層級 | 做法 | 階段 |
|------|------|------|
| 最低 | 每格顯示 `updated_by` + 相對時間（hover 顯示） | 一起做 |
| 中 | 開啟時輪詢（30s）比對 `updated_at`，有異動顯示「有新內容，點此更新」 | 階段三 |
| 高 | Socket.IO 房間（`plan:{pid}`）即時廣播格子更新 | 選配 |

> 📌 **Socket.IO 是現成的**（`core/socket_mgr.py` 的 `sio` 單例，
> 全站已在用）→ 真要做即時同步，成本比想像低。但**第一版不做**：
> 逐格 PATCH 已經解掉九成衝突，即時同步是體驗加分不是必要。

**共編下的權限**：不做逐格權限。有 `preprod_plan` 模組的人就能填整份 ——
定位是內部協作，不是對外門戶。

### 7.5 修正後的分階段交付

| 階段 | 內容 | 可驗收 | 狀態 |
|------|------|--------|------|
| **一** | 資料層 + 逐格 PATCH API + **`plan-matrix.js` 元件（含 `--plc-*` 變數）** + overlay 掛載 | SPA 裡能開、能填、重整還在 | ✅ 2026-07-28（API 27 + UI 18 測試） |
| **二** | `proposal-plan.html`（登入卡 + 清單 + 白底覆寫）+ 模板引導層（錨點卡／提示問句） | 獨立網址可分享，兩個色系都正常 | ✅ 2026-07-28（15 測試；引導層隨階段一先做掉） |
| **三** | 腳本區（四方向）+ 兩個內建範例 + 輪詢更新提示 | 整套流程走得完、共編不打架 | ✅ 2026-07-28（腳本區/範例隨階段一；輪詢橫幅 3 測試含真 30s 等待） |
| **四**（選配） | Socket.IO 即時同步、Markdown 匯出 | 有需要再說 | ⬜ 未做 |

### 7.6 追加的風險

| 風險 | 對策 |
|------|------|
| 同格同時編輯 → 有人的字不見 | 樂觀鎖 409 + 並列兩版本讓人決定；**絕不自動合併** |
| debounce 存檔 + 共編 → 延遲期間別人先寫入 | 縮短 debounce 到 800ms；409 後不覆蓋、跳提示 |
| 兩個色系其一沒測到 | 驗收清單強制**兩邊都截圖**（比照 media-log 元件主題化當時的做法） |
| 獨立網址被當成對外連結傳出去 | `noindex` + 登入閘門；頁面明示「內部工具」 |
| ~~NAS 官網容器沒有 CRM 端點~~（2026-08-07 已解）| 提案的 10 條 token 端點搬進 `api_proposals.public_router`，對外容器只掛那一個（**不是**整個 router）；前端相依閉包由 `tests/unit/test_public_surface.py` 釘住 |

---

## 8. 決策紀錄（§7 追加）

| 日期 | 決策 | 理由 |
|------|------|------|
| 2026-07-28 | 加獨立登入網址 `/proposal-plan.html`，官網白底 | owner 要求可分享的獨立網址 + 官網配色 |
| 2026-07-28 | 分享模式＝**帳號登入頁**，不做 token 公開頁 | owner 選定；內部協作情境，登入閘門即足夠 |
| 2026-07-28 | **多人共編**（非唯讀分享） | owner 選定；比照課堂上大家一起貼便利貼的實際操作 |
| 2026-07-28 | ~~整份 plan 覆寫 PUT~~ **改為逐格 PATCH + 樂觀鎖** | 共編下整份覆寫會蓋掉別人剛填的格子 |
| 2026-07-28 | 企劃 UI 抽成**可換膚元件** `plan-matrix.js`（`--plc-*` 變數） | 同一份 UI 要同時活在 SPA 深色與獨立網址白底；比照影像紀錄元件主題化做法 |
| 2026-07-28 | 第一版**不做** Socket.IO 即時同步 | 逐格 PATCH 已解掉九成衝突；即時同步是加分不是必要（且 sio 現成，日後補成本低） |
| 2026-07-28 | 視覺**照抄** media-log-workspace 的變數與元件 | 已驗證過的官網白底一套，重新設計沒有收益 |

---

## 9. 追加功能（2026-07-28 晚，v2.4.29 發版後）

### 9.1 編輯中隨時看範例

矩陣工具列常駐兩顆範例按鈕（紙船／登山總動員）→ 唯讀檢視 → 「← 返回」
回到編輯（返回時 refetch，保住共編期間別人的更新）。

### 9.2 公開共編開關（細粒度權限）

> owner：「我希望權限設定可以細膩一點，我可以開放的時候才共編，
> 不開放時就要登入。」

| 狀態 | 行為 |
|------|------|
| **開放** | 鑄 `plan_share` scope JWT 存進 `plan.share_token` → 產生 `/proposal-plan.html?t=<token>` 連結，拿到的人**免登入**即可共編（署名＝訪客輸入的名字＋「(外部)」後綴，防冒充內部帳號） |
| **關閉** | token 自 plan 移除 → 該連結**立即 401**，只剩登入路徑可編。冪等 |
| **重新開放** | 鑄**新** token（舊連結永久失效） |

- 決策：~~「不做 token 公開頁」~~ **撤銷**（owner 明確要求開關式公開共編）
- token 不開新表：存於 plan JSONB（撤銷=移除欄位；公開 GET 一律剝除
  `share_token`；PUT 整份覆寫會保留它，clear 才吊銷）
- 公開端點 `/api/v1/proposals/shared/{token}[/cell|/meta]` 與登入路徑共用
  `_apply_plan_patch`／`_plan_meta_payload` 核心（樂觀鎖 409、20KB 上限、
  ?since= 短路 全部一致）
- 公開頁只回 title + plan — 提案的客戶/預算/deck 等欄位不外洩
- 測試：share 套件 26 項（API 生命週期 13 + 瀏覽器 13，含無登入 context
  實測編輯與停用後 401）

### 9.3 備忘欄（2026-07-28 晚追加）

矩陣下方一張「📝 備忘」卡（整份企劃共用一格，自由文字）。
存法比照 THEME（`plan.memo` / `memo_updated_at` / `memo_updated_by`，
PATCH kind=memo），共編樂觀鎖/輪詢/公開路徑/20KB 上限全繼承。
範例唯讀模式無備忘即不顯示。測試 11 項。

### 9.4 模板 v1.1.0 — 灌入完整講座筆記（2026-07-28 晚）

owner 提供工作坊完整筆記後三件事一次到位：

1. **模板內容升級**（純增量，keys/問句不變 → 不凍結舊版，舊 plan 直接吃新版）：
   - 錨點卡增補：參與三卡補後果（心離開/變太乖）；觀眾加第 4 卡
     「⚠ 影音紀錄陷阱」；拍什麼加第 3 卡「一集只能有一個重點」、
     底線卡改為筆記完整版（可做/不可做/紅線/對觀眾負責）
   - 新增 **tips 層**：每格一顆「💡 方法提示」可展開（12 格全有 —
     蹲點/兩階段甄選、看大不看小、副本任務、先講清楚vs慢慢揭曉、
     放下去就結束警訊、童年記憶是礦、腳本要有問號…）；唯讀範例不顯示
2. **本集取捨卡**（矩陣與備忘之間，模板 `summary_fields` 驅動）：
   「這一集的唯一重點」+「節目調性」兩欄 — 存 `field_values.plan.*`
   走現成 kind=field 通道，**後端零改動**。「節目調性」同時解掉 §2.4
   待確認 #2（節目特性欄位）；原拍什麼欄頭的「基調」欄移入此卡
3. **筆記入庫**：`frontend/huang-method-notes.html`（官網白底靜態頁、
   noindex、單一正本）+ 工具列「📚 方法論筆記」新分頁開啟

測試：v1.1.0 專屬 15 項 + UI/share 回歸全過。

### 9.5 格子隨內容自動長高（2026-07-30）

矩陣 12 格、擬腳本方向、備忘的 textarea 全改自動長高：內容多長格子就多長
（input 時量 `scrollHeight`，CSS `min-height` 保底、`resize: none` +
`overflow-y: hidden`）。首次渲染、409 衝突採用對方版本、重新載入都會重量。
owner 原話：「我想要內容多長 格子就多長」。

### 9.6 獨立頁基本資料側邊欄（2026-07-30）

`/proposal-plan.html` 企劃視圖改為左側欄兩分頁「基本資料｜企劃」（純文字，
側欄不放 emoji — UI 鐵則），預設停在企劃、切換只藏不拆（共編輪詢不中斷）。

- **登入模式**：用 GET /{pid} 既有完整資料渲染（含預算範圍、成案原因），唯讀
  + 「編輯請到後台提案庫」提示。
- **公開模式（?t=）**：`GET /shared/{token}` 加回白名單子集 `info` —
  客戶名/類型/提案日/狀態/標籤/簡報連結/參考片單；**不出去**：
  `budget_range`（金額）、`outcome_reason`（組織學習）、created_by／
  project_id／quotation_id（內部 id）。舊後端沒 `info` 時前端不顯示側欄
  （行為同從前）。連結渲染走 `escAttr` + http(s) 白名單（XSS/`javascript:` 防線）。

owner 原話：「基本資料我也想要整合進去公開共編頁面 變成側邊欄的tab」。
測試：API 白名單 + 公開/登入雙模式 UI 共 3 組全過。

### 9.7 基本資料可編輯 + 備註欄（2026-07-30）

owner：「這裡我希望他可以調整，也可以編輯，然後一樣要加入備注欄」。

**備註** = `preprod_proposals.notes`（TEXT 新欄，migration 在 `main.py` _crm_cols）。
**刻意不放在 plan JSONB**：① 尚未開始企劃也要能寫 ② 「清空企劃」不該把它一起清掉
③ 它描述的是提案本身而非某份企劃。SPA 提案庫的詳情卡與編輯表單同步顯示/可編。

**可編範圍分兩級**（`_renderInfo(host, info, {editable, save})` 的 editable 陣列，
真正守門在後端）：

| 欄位 | 登入頁 | 公開 ?t= 連結 |
|---|---|---|
| 類型 / 提案日 / 標籤 / 備註 | ✅ | ✅（`PATCH /shared/{token}/info`，`_PUBLIC_INFO_FIELDS` 白名單）|
| 預算範圍 / 成案原因 | ✅ | ❌ 連讀都不出公開端點 |
| 狀態 | ✅ 標頭的下拉（走共用管線） | ❌ |
| 客戶 / 標題 / 簡報 | ✅ 標頭「編輯」＋簡報上傳 | ❌ |

- **狀態不在側欄的欄位表裡，在標頭**：轉「成案」要建立或推進 CRM 專案、「未成案」
  強制填原因 —— 那是有副作用的管線動作，不是欄位編輯，混在自動儲存的欄位中間會讓人
  「順手」改掉。（2026-08-09 之前這一頁完全不給改，要回後台提案庫；現在兩邊呼叫的是
  同一支 `tabs/proposals/prop-actions.js`，守衛不會因為換了入口而變鬆。）
- 公開端點設計成**一次一欄**（`{field, value}`）而非整包 payload —— 免登入路徑不給批次覆寫能力；
  tags 限 20 個 × 64 字、文字欄 8KB。
- 自動儲存：debounce 800ms + change/blur flush + dirty-check，同企劃矩陣手感（無儲存按鈕）。
- `ptype` 若是 PTYPES 清單外的既有值（舊資料/CSV 匯入自由文字），select 會補一顆該值的選項
  ——否則靜默吃掉顯示。

**順手修掉提案日差一天**（既有 bug，非本次引入）：`pitch_date` 是「日曆日」但欄位型別
`timestamptz`，原本寫入 naive 午夜（由 DB session timezone 解讀）、讀出直接 strftime
→ 送 2026-08-01 讀回 07-31。現在寫入以 `_TW_TZ`(+08:00) 午夜下錨、讀出 `_fmt_date()`
固定換算 +08:00 再取日期，**既有列也一併讀正確**（不需資料遷移）。SPA 同步受益。

測試：API + UI 共 27 項（含公開端點 6 個禁止欄各自 422、tags 型別、8KB 413、
自動儲存往返、清單外 ptype 保留）。

### 9.8 參考影片可編輯（2026-07-30）

owner：「放參考影片希望也可以編輯」。獨立頁的「參考影片」卡改成可編清單：
每支一列＝標題（可編）+ 開啟↗ + 移除，下一行備註（可編），底部「網址 + 標題 → ＋加入」。
標題/備註走同一套自動儲存；加入/移除會 `opts.reload()` 後重畫該區（避免搶焦點）。

**公開 ?t= 連結的護欄**（`POST/PATCH/DELETE /shared/{token}/refs[/{rid}]`）：

- 網址只收 `http(s)`（擋 `javascript:` 等），長度 ≤512；備註 ≤4KB；每提案掛載 ≤30 支。
- **改標題/備註只放行「只掛在本提案」的片** —— 片庫（`preprod_references`）是跨提案共用
  資產，被其他提案共用時回 409「請由後台編輯」，免登入連結不得改到別人看到的資料。
  沒掛在本提案的 rid → 404。
- **網址不給改**（換片＝移除後重加）；移除只解關聯，片庫本體與其他提案掛載保留。
- 公開 `info.references[]` 開始回 `id`（要能指定解除/改備註；仍需持有 token）。

登入頁沿用既有端點（`POST /references` + `POST /{pid}/refs`、`PUT /references/{rid}`、
`DELETE /{pid}/refs/{rid}`），語意不變＝可改共用片庫。

測試：24 項（公開加/改/移除、四種壞網址、4KB、30 支上限、跨提案共用 409 + 內容未被改、
移除後另一提案掛載仍在、兩模式 UI 往返、前端也擋壞網址不打 API）。

## 10. 參考影片庫 v2（規劃中，另檔）

owner 2026-07-30：「參考影片他自己可以是一個小頁面，裡頭可以做一些註記、截圖標示等，
也可以被其他專案引用」，並提供團隊過去在 Notion 的 **🛠️ 參考影片資料庫** 架構
（15 欄屬性 + 頁面三段：影片/截圖/研究四欄）。

完整規劃 → [`REFERENCE_LIBRARY.md`](REFERENCE_LIBRARY.md)（含 Notion 欄位對照表、
資料模型、截圖標示技術方案、跨專案引用、四階段拆解、五個待拍板決策）。
**尚未實作。**

## 提案×專案管理整合（2026-08-03，owner 定調「提案的進程要跟專案管理整合」）

**權限**：`api_proposals.proposal_auth` / 片庫 `_ACCESS_MODULES` / 引用 registry 的
proposal 模組表，三處都加了 `crm_projects` —— 有專案管理的人可讀寫提案庫與片庫。
前端同步四入口：`tab-config.js` 的 `TAB_EXTRA_ACCESS`（tab key → 額外可見模組，
與後端閘門對齊的唯一前端映射）、`proposal-plan.html`、`reference.html` 閘門與 noperm 文案。

**管線整合**（`frontend/tabs/crm/crm-projects-proposals.js`，lazy-load）：
- 專案管理「提案」分頁頂端 = 提案庫未成案提案卡列（草稿/已提案/入圍 且 project_id 空）；
  「未成案」分頁 = status=未成案 的提案卡；總表 = 一條數字帶（點跳提案分頁）。
- **不複製狀態**：資料源永遠是 `/api/v1/proposals`（30s 快取），提案編輯走提案庫。
  擱置刻意不進管線。
- 卡上「成案 →」開 chooser：**建立新專案**（原路）或**連結既有專案**
  （`POST /convert` body 帶 `project_id` → 不重複建案；後端驗專案存在、回
  `linked_existing`）。連結路不檢查提案客戶（專案自帶客戶）。
- 專案詳情 Layer 2.5「提案來源」：`GET /proposals?project_id=` 反查，顯示
  標題/狀態/提案日/預算/參考數/deck/成案原因，點標題開 `/proposal-plan.html?pid=`。

**刻意不做**：提案↔專案狀態雙向同步（語意不同：win/loss 漏斗 vs 製作進程），
只在成案點連動；8 階段管線定義不動。
> ⚠️ 「不複製狀態」與「狀態不同步」兩條已被下一節（2026-08-06 合體）**部分推翻**
> —— 但只在明確定義的轉換點單向連動，不是當初否決的全面雙向同步。

## 提案=專案合體（2026-08-06，owner 拍板方案 A「對我來說提案其實已經在專案了」）

**核心**：提案誕生的那一刻就是一個管線「提案」階段的專案；提案獨有資料
（plan 企劃矩陣、pitch_date、budget_range、deck、win/loss 原因、share_token、
參考片掛載）留在 `preprod_proposals` 當 **project_id 1:1 衛星列** —— 衛星表 id
不變，公開 `?t=` 連結 / 片庫多型引用 / plan-matrix 全部零搬遷。

**行為**（正本 `routers/api_proposals.py` 頂部「提案=專案合體」區 +
`routers/crm/projects.py _sync_linked_proposals`）：
- `POST /proposals`：走 `create_project_in_session`（建案單一正本：AM 繼承/
  主表+雜支種子/客戶分級）自動建殼專案。body 帶 `project_id` → 改為掛載既有
  專案（專案詳情「提案企劃」分頁的建立路徑），client_id 繼承專案。
- **客戶可空**（2026-08-06 當日修正，owner：「這兩個直接就可以整並在提案的
  頁面上了」）：`crm_projects.client_id` 從 NOT NULL 放寬為 nullable
  （main.py startup `ALTER … DROP NOT NULL`）。原本「建提案客戶必填」是遷就
  舊 schema 的折衷，代價是沒客戶的草稿提案卡在提案帶進不了管線 —— 而前期
  草稿還沒定客戶是常態。現在一律入列，客戶欄顯示「待補客戶」，之後在專案
  詳情 inline 補（可清空、可改）。**手建專案仍要求選客戶**
  （`CrmProjectPayload.client_id: str` 必填 + 前端擋），只有提案建殼路徑允許空。
- **win/loss 觸發點 = 專案階段轉換**：專案進 製作/結案/歸檔 → 衛星提案記
  「成案」；轉「未成案」→ 提案記 loss，`outcome_reason` 必填（422 擋，
  detail.code=`OUTCOME_REASON_REQUIRED`；前端 inline 編輯先 prompt）。
  反向：提案側 PUT 轉成案/未成案 → **還在前期**（投標/開發/洽詢/提案）的
  專案跟著換階段；已進製作的不回頭拉。前期子狀態（草稿/已提案/入圍）互換
  不拉專案。
- `GET /crm/projects` 附掛 `proposal_status`（最近更新的衛星列）→ 管線列
  子狀態 badge。
- **存量遷移**：`migrate_unlinked_proposals_to_projects()`（main.py startup，
  冪等、逐列 FOR UPDATE 防機隊並發）：沒專案的提案全部補殼（含無客戶的）。
  只有「擱置」刻意不遷；擱置改回其他狀態時 PUT 會自癒建殼。
- **提案帶已退役**：正常情況永遠空（`:empty{display:none}` → 完全不佔版面），
  只在遷移失敗產生孤兒時當防呆顯示；總表不再打提案 API。
- **提案庫 Tab 保留 = 企劃工作區**（owner 拍板）；`/proposal-plan.html` 獨立
  登入頁與公開共編不受影響。專案詳情新增「提案企劃」分頁
  （`crm-projects-plan.js` 嵌 plan-matrix，同工作區 overlay 的登入模式）。

---

## §12 報價單分頁（2026-08-11，v2.4.62 這批）

owner 三個拍板：**CRM 報價為主清單**（唯讀鏡像，正本在報價管理 Tab）、
**外部報價檔上傳可多版本**（客戶回簽版/廠商比價版這類不是報價模組產的檔）、
**多份報價（上傳檔+CRM 版本合計 ≥2）時 AI 幫忙分析差異**。

- **落點**：`專案資產夾/{folder_subpath}/報價單/`。🔴 **刻意沒有 /uploads
  web root 退路**（deck 有）——報價單含金額，不進靜態直出；資產夾建不出來
  就 400 明講。下載走帶權限端點。
- **🔴 金額紅線**：`preprod_quote_files` / `preprod_quote_analyses` 的任何
  資料不准出公開 `?t=` token 端點（同 budget_range 慣例）；前端兩個介面的
  訪客模式**連分頁鈕都不建**（比照企劃書分頁）。
- **端點**：`routers/crm/proposal_quotes.py` —— GET `/crm/proposals/{pid}/quotes`
  （files+crm_quotations+最新 analysis，讀取端 bg_status.settle 自癒）、
  POST `…/quotes/upload`（版號每提案 max+1）、PATCH/DELETE `…/quotes/{fid}`
  （刪 DB 列+best-effort 刪磁碟檔——「報價單」子夾是系統管的落點）、
  GET `…/quotes/{fid}/download`、POST `…/quotes/analyze`（bg_task.fire 背景跑
  claude，inputs 存當下快照）。守衛用 `api_proposals.proposal_auth`（比照
  briefs —— 含 preprod_plan，打得開工作頁就能用分頁；資產夾的 `assets_auth`
  是另一條閘，不含 preprod_plan）。
- **AI 分析**：`services/quote_analyzer.py`。鐵則同企劃書的「（未填）」原則：
  資料裡沒有的一律「（資料中未見）」不准編；抽不出文字的檔（掃描 PDF）要在
  輸出開頭聲明未納入。單檔文字截 8000 字。
- **xlsx**：`core/doc_text` 新增 openpyxl 讀取器（`data_only=True` 要算好的值；
  read_only 串流防整本 styled cell 實體化）。**.xls 不支援**，錯誤訊息教人另存
  .xlsx。openpyxl 是 server-only（`SERVER_ONLY_PKGS` 已列，別讓依賴掃描把它
  寫進 requirements_agent —— python-pptx 錯名事故的同型陷阱）。
- **前端**：`tabs/proposals/quote-view.js` 兩介面共用（獨立企劃頁側欄
  「報價單」分頁 + 後台詳情分頁），照 brief-view 三條：只 import 同目錄與
  js/shared、--qv-* 可換膚、外殼由呼叫端給。分析輪詢用 shared/poll-job。

### §12.1 勾選/簡報綁哪一筆提案（2026-08-11 修）

端點掛在**專案層** (`/projects/{id}/proposal-assets/*`)，但勾選（`pinned_assets`）、
授權旗標（`pins_public`）與提案簡報（`deck_url`）存在**提案列**上。修之前只有
一條隱式規則「該專案最近更新的那筆」，於是一個專案並行多筆提案時：

- 後台：在看 B 卻標到 A（★ 與勾選都會漂）。
- 🔴 客戶：拿 B 的分享連結卻讀到 **A 的 `pins_public` 與 A 的勾選** ——
  B 自己設的「不對外開放」被忽略（`_shared_pins` 手上有 token 對應的提案，
  卻用 project_id 重新推導了一次）。

**修法**：`proposal_assets.pins_row(session, project_id, pid="")` 成為讀寫的
單一入口 —— 給了 `pid` 就用那一筆並**驗證它屬於這個專案**（否則 404，不然
pid 會變成跨專案讀別人資料的入口）；沒給才退回「最近更新」（單提案專案行為
完全不變，退路留給沒有提案脈絡的專案層總覽）。`pinned_of(session, prop)` 改收
**提案列**不收 project_id，客戶那條直接把 token 的提案傳進去。

前端一律帶 `pid`：`makePinsStore({base, pid})`（POST 進 body、DELETE 進 query）、
`/proposal-assets?pid=`、`/proposal-assets/deck` body 帶 pid。

順帶修掉 CRM 專案「提案企劃」分頁的既有 bug：資產夾卡片本來掛在 `loadPlanTab`
（只建一次），而切 chip 時 `_renderPlan` 的 `host.innerHTML=` 會把它整張洗掉且
不重建 → 改成 `_loadAssets(projectId, pid, host)` 跟著選定的提案重掛。

守門測試：`tests/unit/test_pins_row.py`（5 項，含跨專案 pid → 404）。

---

## §13 會議記錄分頁 + 「企劃」改名「創意發想」（2026-08-12）

**改名**（owner）：側欄與後台分頁的「企劃」→**「創意發想」**（那一格裝的是
黃鴻儒方法論的三視角×四提問矩陣，叫「創意發想」比「企劃」精準）。連帶改掉
的使用者字串：清單 badge「企劃中」→「發想中」、空狀態「用一套方法論開始
創意發想」、載入失敗與分享說明。**「企劃書」不動**（那是產出物，兩者刻意分名）；
CRM 專案詳情的「提案企劃」分頁**也不動** —— 它指的是整個提案區塊不是這一格，
要一起改再說。

**會議記錄**（`preprod_meeting_notes` + `routers/crm/proposal_meetings.py`
+ `frontend/tabs/proposals/meeting-view.js`）：

- 一次會議一筆：會議日期／主題／出席者／記錄本文，**逐欄自動儲存**
  （`autosaveDelegated`）。新到舊排序，**沒填日期的殿後**（剛建的那筆該留在
  原地，不該跳到最前面）；NULL 排序在 Python 做，各家 DB 的 NULLS 預設不同。
- 🔴 **沒有版本快照**：會議記錄是創意發想與企劃書的**輸入素材**，不是產出物。
  企劃書那套「重新生成＝新增一版」套過來只會讓人不敢改字。
- 🔴 **內部資料**：不出公開 `?t=` 端點（客戶會議也會記到內部判斷、競品、
  報價底線）。訪客連分頁鈕都不建，比照企劃書/報價單。e2e 有一項專門驗
  公開端點不含會議內容。
- PATCH 只認白名單且**沒送的欄位不動** —— 整包寫回會把沒送的欄位洗成預設值
  （repo 既有地雷，v2.0.5/2.0.6）。日期沿用 `api_proposals._parse_date/_fmt_date`
  的台灣午夜下錨，不自己再寫一套（v2.4.33 修過差一天）。
- 表已進 `db.models.PROPOSAL_CHILD_TABLES`（刪提案連帶清）。

### §13.1 錄音 → 逐字稿 → AI 整理（2026-08-12，owner「請規劃」後拍板執行）

AI 在這裡是**輸入輔助**不是產出物：每筆會議記錄可傳一個錄音檔
（POST `/proposals/{pid}/meetings/{mid}/audio`，multipart）→
`services/meeting_transcriber.py` 背景跑 ffmpeg 抽 16k wav → faster-whisper
（`turbo`，與語音辨識 Tab 同款、同 `models/` 快取）逐字稿 → claude 整理
（Markdown：摘要／討論重點／決議／待辦，鐵則同報價分析：「（資料中未見）」
不准編）。

- 🔴 **`content` 是人的正本**：AI 整理寫進 `ai_summary` 欄；只在 content
  **當下全空**時代填一次（`_fill_empty_content` 讀活列不讀快照 —— 處理期間
  有人動手寫就一個字都不碰）。前端代填時把 `_asSaved` 基準一起對齊，
  免得 autosave 把 AI 填的那份又送回去。
- 落點：專案資產夾/{提案的家}/**會議記錄**/（`land_in_home`，系統管的子夾
  → 換檔/刪列敢 best-effort 清磁碟檔；逐字稿另落一份 `.逐字稿.txt` 在錄音旁，
  同事直接從 NAS 拿）。比照報價單：**沒有 /uploads 退路**、下載走帶權限端點。
- 狀態生命週期同企劃書（pending/ok/failed + `core.bg_status.settle` 讀取端
  自癒）；🔴 **whisper 一小時錄音實測要 10–25 分鐘（CPU）**，遠超
  STALE_AFTER 240s —— 整段包 `core.bg_status.keepalive`，心跳每半個
  STALE_AFTER（目前 120s）蓋 `updated_at` + 呼叫端給的 `phase` 進度字
  （「辨識中 37%」），排隊等 `_LOCK`（一次一件）期間也在跳。心跳寫失敗
  只跳過那一拍，不讓長工被誤判。
- `POST /meetings/{mid}/summarize`：只重跑 claude 那段（用既有逐字稿）——
  AI 掛了不必重付一小時的 whisper；還在 pending → 409。
- 前端輪詢打**單筆** GET `/meetings/{mid}`（別為一個 status 掃整份清單）；
  上限由呼叫端一次聲明（`maxTicks: 240` × `tickMs: 15000` ≈ 一小時），
  🔴 **不准在 `onSettled` 裡重掛** —— `_poll` 只從「剛變成 pending」的三個
  地方呼叫（上傳成功、重跑整理、初次載入時已在跑的）。**只換那張卡的錄音區**
  （`_refreshCard`）—— 整清單重畫會把別張卡打字中的欄位掀掉。
- 逐字稿/AI 整理**不隨清單載**：清單只回 `has_transcript`/`has_summary`，
  使用者展開摺疊區時才打單筆 GET 拿全文（一小時錄音的逐字稿幾十 KB，
  每次新增/刪除都整份重載是白付）。
- 新欄位（`audio_rel/transcript/ai_summary/status/error/phase`）已進 main.py
  的 `_crm_cols` migration（dev 既有表要 ALTER；新環境 create_all 直接有）。

### §13.2 現場錄音（MediaRecorder，開會當下直接錄）

會議記錄那排多一顆「現場錄音」：按下去錄，停止後自動當成錄音檔上傳，接上
§13.1 同一條管線（不另開路徑）。錄出來的是 `audio/webm;codecs=opus`
（Safari 是 `audio/mp4`），兩種副檔名後端白名單本來就收；實測 webm/opus
走完整條 ffmpeg → whisper → claude 沒問題。

- 🔴 **按鈕是「有能力才長出來」**（`_CAN_REC`：`isSecureContext` +
  `mediaDevices.getUserMedia` + `MediaRecorder`）。`getUserMedia` **只在
  HTTPS 或 localhost 存在** —— 同事用 `http://192.168.1.107:8000` 內網 IP
  進來時瀏覽器根本不給麥克風，那條路照舊用「上傳會議錄音」，不留一顆按了
  會壞的鈕。要現場錄音就走 `foundry.originsun-studio.com`（HTTPS）。
- 拿不到麥克風（使用者按拒絕、沒有裝置）→ **照實說**，不假裝在錄。
- `rec.start(5000)` 每 5 秒切一塊；停止後要等 `'stop'` 事件才算收齊，
  **麥克風軌道也在那時候才收**（提早關軌可能吃掉最後一塊）。
- 🔴 **錄音狀態（`host.__mv.rec`）是渲染的輸入**，不是就地寫進 DOM 的東西：
  `_audioHtml(m, rec)` 看到它就畫錄音列。所以「錄音中按新增/刪除」造成的
  整份重畫會把錄音列**原樣畫回來**。這條不成立的話，使用者一按新增，錄音
  就變成看不見也停不掉的孤兒（只能重整整頁）——
  `tests/e2e/test_meeting_recorder.py::test_recording_survives_list_rerender`
  就是釘這個。
- 收尾**只有一個出口** `_finish`，而且**只跑一次**（`r.done`）：`stop()` 是
  同步把 state 變 inactive、事件另外排隊，所以連按兩下「停止並上傳」時第二下
  會看到 inactive 直接收尾、接著排隊的 `'stop'` 再送一次 —— 沒那道閘就會
  上傳兩份、跑兩次 whisper。三條路（使用者停止/取消、瀏覽器自己停掉、畫面
  被拆掉）都收斂到它，統一收麥克風、拆 `beforeunload`、清 `s.rec`。
  `'stop'` 監聽在**建立時**就掛（掛在停止時等於瀏覽器自停那條沒人接）；
  已 `inactive` 再呼叫 `stop()` 會 throw，所以先看 `state`。**不掛 `error`**
  —— 規格保證 error 後面跟一個 stop，掛了反而會把 intent 設掉，害「錄音被
  中斷了」那句永遠不出現。
- 開始錄音的位子在 **await 之前**就佔（`s.starting`）：`getUserMedia` 會讓出
  一個 task，連按兩下的第二下會通過檢查、開出第二個 recorder，而第一個從此
  沒人收得掉（麥克風、計時器、chunks、`beforeunload` 全留著）。
- **畫面被拆掉時會自動存起來**：`_tick` 每秒檢查 `host.isConnected`，詳情
  視窗被關掉（X／Esc／點背景，`proposals.js` 的 `_closeOverlay` 直接
  `ov.remove()`，沒有任何 teardown hook）→ 當成「停止並上傳」。關視窗弄丟
  一整場會議的錄音比多存一個檔糟糕得多。
- 🔴 「那一筆還在不在」問 **`s.notes`（資料）不問畫面**：用卡片存不存在判斷
  的話，一次 `_load` 失敗（隧道抖一下，錯誤訊息取代整個 host）就會被當成
  「被刪掉了」，把一整場會議的錄音丟掉、還配一句錯的說明。

## §14 提案頁＝工作流中樞（2026-08-13「請規劃」，⏳ 待 owner 拍板後動工）

owner 需求原文（兩則合讀）：「將現在的提案頁面裡的專案，依據專案的進度逐步地
完成到結案，然後這些功能都跟 CRM 系統同步。每個人的權限由 CRM 系統控制」＋
「我想把提案頁面擴充成我的工作流」。

一句話：**提案頁從「企劃工作區」升級成「專案從提案走到結案的駕駛艙」**——
在提案詳情（SPA overlay 與 `/proposal-plan.html` 兩介面）看得到專案現在走到
哪個階段、這個階段還缺什麼、下一步按哪裡；推進動作直接從這裡發。

### §14.0 四個核心設計判斷（先讀這裡再動工）

1. **同步靠合體，不靠複製**。§「提案=專案合體」之後，提案頁裡的專案「就是」
   `crm_projects` 那一列（project_id 1:1 衛星）。工作流視圖是**同一筆資料的另
   一個投影**——不新增第二份狀態、不雙寫。提案頁發生的每個寫入都打**既有**
   CRM 端點；「跟 CRM 同步」因此是結構保證，不是同步程式。
2. **進度＝訊號＋勾選，read-time 推導**（預支款結算的既有模式：前端不做任何
   狀態寫入，後端查詢時即時算）。自動訊號從既有資料推（報價單、人員配置、
   影像紀錄、審批、`work_completeness` 四項、發票請款）；只有無法從資料推的
   步驟（開拍、剪輯完成…）落新表當手動勾選項。
3. **階段與進度解耦**（2026-08-13 owner「有時候進度是多方前進」後修正）。
   **階段**沿用八狀態（投標/開發/洽詢/提案/製作/結案/歸檔/未成案）維持
   **單線**——它是商務主軸，CRM 的客戶分級、錢流口徑、結案看板全掛在上面，
   一個專案同時只能在一個狀態。**進度**則是**多軌並行**的訊號矩陣：訊號
   從資料推導、哪時發生哪時亮，**與當前階段無關**——還在「提案」階段時
   人員已配置、素材已入庫，照亮不誤。推進階段只是把商務主軸的標籤往前撥。
   不做第二套狀態機——兩套狀態機總有一天互相漂移。
4. **推進走既有閘門**。「推進到下一階段」＝打 `routers/crm/projects.py` 既有
   的專案狀態端點（狀態白名單 + `_sync_linked_proposals` 衛星 win/loss +
   客戶分級重算的完整副作用鏈）。🔴 合體鐵則沿用：**不能只 setattr、不能繞過
   副作用鏈另開寫入路**。
5. **各自工作、資料放正確的位置**（owner 2026-08-14 定調：「各自工作，但
   專案一起完成，資料都放在正確的位置」）。每種資料有它**唯一正確的家**：
   報價住報價管理、發票請款住帳務、素材住影像紀錄/素材庫、企劃住提案衛星列
   ——進度頁是**鏡頭不是容器**，查詢時從各正本聚合成四軌燈號，不搬不抄。
   財務在財務管理開發票、剪輯師在後期製作轉檔、企劃在企劃頁寫發想，
   沒有人需要把資料帶進提案頁。整個功能**唯一**的新儲存＝手動勾選表
   （`crm_project_flow_checks`——勾選這個「事實」沒有別的家，所以住這裡）。
   任何「把別處資料抄一份進來」的實作提案都違反本條。

### §14.1 資料層

新表 `crm_project_flow_checks`（只存**手動**項；自動訊號永不落庫）：

    id PK / project_id FK / item_key / checked bool
    / checked_by / checked_at / note TEXT
    UNIQUE(project_id, item_key)

（item_key 全域唯一，**不帶 stage 欄**——項目屬於軌道不屬於階段，
這正是「多方前進」的資料層體現。）

步驟範本＝純資料常數 `core/project_flow.py`（比照 plan-templates 慣例）：
每軌 `items[{key, label, kind: auto|manual, hint, link}]`，auto 項各配一個
推導函式。v1 一套通用範本，不做 per-project_type 客製（決策點 1）。

### §14.2 四條軌道草案（v1 內建，文字任 owner 改）

進度按**軌道**分組、各軌獨立前進，不按階段分組：

- **商務軌**：客戶已建檔(auto: client_id) → 報價單已備(auto:
  preprod_quote_files 或 crm_quotations) → 報價成交(auto: quotation 狀態)
  → 已請款/開發票(auto: crm_invoices 有列) → 款項結清(auto: 應收餘額 0)
- **企劃軌**：提案已建立(auto: 衛星列存在) → 創意發想已開始(auto:
  `_plan_started`) → 企劃書至少一版(auto: preprod_brief) → 已向客戶提案
  (auto: 衛星 status ∈ 已提案/入圍) → 成案(auto: 衛星 status)
- **製作軌**：人員已配置(auto: crm_project_staff ≥1) → 開拍(manual) →
  素材已進影像紀錄(auto: media_log) → 工時登記中(auto: timesheets 該專案
  有列；hover 顯示消耗 vs 時數池 burn%——api_timesheets 的 burn 檢核現成。
  ⚠ 動工時先確認 timesheets 列 ↔ crm_projects 的關聯鍵) → 剪輯完成(manual)
  → 客戶審批通過(auto: portal 最新一輪通過；沒開審批單則 manual)
- **交付軌**：完稿已交付(manual) → 上架素材完成度(auto:
  `work_completeness` 四項逐項亮燈；1:N 多作品專案**每支作品一列**) →
  官網上線(auto: showcase published；「不上官網」則此項顯示略過)

軌內箭頭是**建議順序**不是鎖——亮燈只看資料，跳著亮完全合法
（先收到訂金再補企劃書，商務軌就是比企劃軌快）。

**多提案並行**（v2.4.57 一專案 1:N 提案）：企劃軌訊號**聚合**所有衛星提案
（任一筆有企劃書＝亮；「成案」看成案那筆）。

**階段 gate**（推進時的建議檢查）引用**跨軌**的特定項，不是「某階段的清單」：

| 推進 | 建議完成（軟擋列缺項） | 硬守衛（既有，不動） |
|---|---|---|
| 前期→製作 | 企劃軌「成案」、商務軌「報價成交」 | 成案 outcome_reason 必填 |
| 製作→結案 | 製作軌「審批通過」 | — |
| 結案→歸檔 | 交付軌全亮、商務軌「已請款」、「專案復盤完成」(manual v1；見 §14.8) | — |
| 任一→未成案 | — | outcome_reason 必填 |

款項結清**永不阻擋**（黃燈；決策點 5）。推進遇缺項＝**軟擋**：確認框列出
缺什麼，確認後仍可推（決策點 2）。硬擋只保留既有守衛——不在 UI 層新蓋假守門。

### §14.3 API

**⚠️ 以下是實作後校正過的實際形狀**（規劃時寫的 URL 與欄位都飄了；
端點住 `routers/crm/flow.py`，所以在 CRM 前綴下）：

- `GET  /api/v1/crm/projects/{id}/flow` →
  ```
  { project_id, project_name,
    stage: { status, pipeline[], index, next, is_lost, is_terminal },
    tracks: [{ key, label, done, total, items: [{ key,label,kind,state,hint,
                                                  detail?,by?,at?,note? }] }],
    missing: { blocking: [{key,label,track}], advisory: [...] },
    can_check, can_advance, collects_outcome_reason }
  ```
  守衛 `proposal_auth`（能看提案的都能看）。自動訊號一趟聚合算完
  （17 個 scalar subquery + 1 趟 showcase，禁 N+1）。
- `POST /api/v1/crm/projects/{id}/flow/check` `{item_key, checked, note}`
  → 守衛 `core.project_flow.CHECK_MODULES`（＝`crm_projects` 模組即可）。
  回同一份 payload（勾完不必再打一次 GET）。
- **推進不新增端點**——前端打既有的 `PATCH /api/v1/crm/projects/{id}/status`，
  守衛 `core.project_flow.ADVANCE_MODULES`（空 tuple ＝ 管理員限定）。
  兩個政策常數各自被端點守衛與前端旗標共用，不靠「剛好都是 admin」對齊。
- `collects_outcome_reason` 由 `core.project_flow.wins_proposal()` 算，
  **與真正去標成案的 `_sync_linked_proposals` 共用同一份判定**——
  分兩處寫的話，對話框問了原因、後端不採用，使用者打的字會靜默消失。
- 🔴 公開 `?t=` 端點**完全不出 flow**（內部進度、金額訊號、人名都不出；
  防線在後端白名單，不在前端）。

### §14.4 前端（UI 規格，2026-08-14 詳化）

- 兩介面**結構共用**（v2.4.54 的教訓：逐項移植只會再分岔）：提案詳情 overlay
  與 `/proposal-plan.html` 各加「進度」分頁，跑同一份 `tabs/proposals/flow-*.js`
  （`flow-view.js` 純渲染 + `flow-store.js` 狀態與端點，比照 pins 拆法）。
  換膚比照 plan-matrix：`--flow-*` CSS 變數深色預設、`flow-theme-light` 白底。
  🔴 import closure 鐵則沿用：只准 `tabs/proposals/` 與 `js/shared/`。
  🔴 UI 無 emoji 鐵則（2026-07-17）：全部純文字。

**版面（桌機）**——三層，由上而下：

    ┌ 階段（商務主軸，單線）──────────────────────────────┐
    │  洽詢 ── 提案 ──【製作】── 結案 ── 歸檔    [推進到結案] │
    ├ 四軌（多方前進，各自亮）────────────────────────────┤
    │ 商務  ●建檔 ●報價已備 ●成交 ○請款 ○結清        3/5   │
    │ 企劃  ●提案 ●發想 ●企劃書 ●已提案 ●成案        5/5   │
    │ 製作  ●配置 □開拍 ●素材入庫 □剪輯完成 ○審批    2/5   │
    │ 交付  □完稿交付 ○上架完成度 ─官網上線(略過)    0/2   │
    └──────────────────────────────────────────────────────┘

- **階段列**：七狀態線性膠囊（投標/開發/洽詢 都是可能的起點，畫當下實際
  路徑；未成案＝紅色終態橫幅取代整列，附 outcome_reason）。目前階段實心、
  已過填色、未來空心。右端「推進到＜下一階段＞」主按鈕——只在有
  `crm_projects` 權限時可按，無權限顯示禁用態＋「需要專案管理權限」。
- **軌道列**：每軌一橫排，軌名＋依建議順序的節點＋右端 x/y 完成數。
  **軌色沿用既有四段進度條的色彙**（備份藍 #1f538d／轉檔橘 #d48a04／
  串接綠 #228b22／報表紫 #7c3aed）：企劃藍、製作橘、商務綠（錢）、交付紫
  ——同一套顏色語言，使用者已經認得。
- **節點四態**：
  - `●` 亮（auto）：實心，hover/tap 顯示**訊號來源**（「報價單 2026-031
    狀態＝成交」）——燈要能自己解釋為什麼亮；
  - `□` 手動項：有權限＝checkbox（勾選即 POST，樂觀更新，失敗 crmToast
    還原），無權限＝空心點＋署名 tooltip（誰勾的/何時）；
  - `○` 未亮（auto）：空心＋「去完成」deep-link；
  - `─` 略過：虛線灰（如「不上官網」的官網上線項）。
- **deep-link 門在目的地**：SPA 有權限者跳 CRM 對應分頁；`/proposal-plan.html`
  開對應獨立頁新分頁；無權限者連結禁用＋「需要＿＿權限」——不藏功能。
- **軟擋確認框**：推進時缺 gate 項 → 列缺項清單（各附 deep-link），
  「仍要推進」／「先去完成」兩鈕。
- **手機（≤720px，沿用 v2.4.55 斷點）**：階段列收成「目前：製作 → 下一步：
  結案」單膠囊；四軌轉直式清單（軌名為分組標題，節點一列一項）；觸控目標
  44px、字 16px；不准橫向捲動（回歸測試比照 test_proposal_plan_mobile）。
- **邊界態**：未成案＝紅橫幅＋四軌凍結唯讀；歸檔＝全灰唯讀；提案尚無殼專案
  （無 client_id legacy）＝「此提案尚未關聯專案」＋補客戶 CTA（PUT 自癒路）。
- **清單視圖**：提案清單每列補階段 chip ＋ **四格微型完成條**（每格一軌、
  按完成比例填色，寬約 40px）——列表掃一眼就看得到「哪個案子哪一軌卡住」。
- **重抓時機**：開分頁抓一次＋切回分頁重抓（比照清單慣例）；v1 不做輪詢。

### §14.5 員工權限（全由 CRM 現制控制，不新增權限 key、不動使用者管理 UI）

RBAC v2 原則沿用：權限＝帳號的 `modules[]`＋管理員開關，admin 在既有
「使用者管理」逐帳號勾。這功能**零新權限 key** → 免三處同步、免 UI 改動。

**動作 × 守衛**：

| 動作 | 守衛 | 說明 |
|---|---|---|
| 看進度分頁（四軌全貌） | `proposal_auth`（admin / preprod_proposals / preprod_plan / crm_projects） | 公司內部進度透明；訊號燈**不帶金額**（「報價成交」「款項結清」只是 bool 燈） |
| 勾手動項（開拍/剪輯完成/完稿交付…） | `check_admin_or_module('crm_projects')`；v2 選項見下 | 勾選落 audit（checked_by/checked_at） |
| 推進階段 | `check_admin_or_module('crm_projects')` | 動商務主軸＝動錢流口徑，與 CRM 專案編輯同門 |
| deep-link「去完成」目的地 | 各目的地自己的既有守衛 | 燈大家看得到；點進報價管理要 `crm_quotes`、帳目要 `crm_invoices`——不另設門，門在目的地 |
| 公開 `?t=` 客戶頁 | 一律不出 flow | 內部進度、人名、金額訊號都不出；防線在後端白名單 |

**員工分群建議**（照你們現有團隊形態，admin 在使用者管理直接勾）：

| 角色 | modules 組合 | 在工作流頁能做什麼 |
|---|---|---|
| 提案企劃人員 | `preprod_proposals` + `preprod_plan`（+`references`） | 看全部進度；在企劃軌工作（發想/企劃書/會議記錄）；勾不了、推不了 |
| 專案負責人／製片 | 上面 + `crm_projects`（+`crm_quotes`） | 全部：勾手動項、推進階段、跳報價 |
| 製作／後期人員 | 後期製作群模組（不給 CRM 也行） | 沒給 preprod/crm 模組＝看不到提案庫 tab；要讓他們看進度就加 `preprod_proposals`（純看） |
| 財務 | `crm_invoices` | 商務軌的請款/結清訊號**由他們的日常作業自動亮**——不用進提案頁做任何事 |
| 客戶（外部） | 無帳號，走 `?t=` | 永遠看不到工作流 |

**v2 選項（決策點 6）——專案成員層級的勾選權**：用 N0 的 `User.staff_id` ×
`crm_project_staff` 派工橋接：「被派到**這個專案**的人」可勾**該專案**的
製作軌手動項（開拍/剪輯完成），不需全域 `crm_projects`。這是全系統第一個
**列級**權限（現制全是模組級）——建議 v1 先不做，模組級跑一陣子，真有
「剪輯師想自己勾剪輯完成」的需求再上；資料橋都是現成的，隨時可加。

### §14.6 分階段交付（每階段 /simplify + 各自 commit）

1. `core/project_flow.py` 範本 + `GET /flow`（純推導，零新表）+ 詳情「進度」
   分頁唯讀 stepper。
2. `crm_project_flow_checks` 表 + check API + 勾選 UI（audit：checked_by）。
3. 推進按鈕（走既有狀態端點）+ 缺項軟擋確認框。
4. `/proposal-plan.html` 同步 + deep-links + 清單階段 chip。
   - 4A ✅ deep-link「去完成」（2026-08-14）。實作與 §14.4 草案的四處差異：
     1. **目的地不另立一組 key**：目的地就是一個 tab，tab 的鍵就是模組鍵，
        所以 `core.project_flow.ITEM_DEST` 只放模組鍵，前端用既有的
        `TAB_MAP` 換 section、`tabLabel()` 換中文名 —— 不再多一張
        「目的地 → 網址／名稱」的表（原本會是第三份 tab 中文名，而既有的
        兩份 `TAB_GROUPS`／`MODULE_LABELS` 已經在漂）。
     2. **不攔 click**：連結就是一條 `<a href="/#tab_x">`。SPA 自己的
        hashchange 路由本來就吃這個形式，所以原地換 tab 是免費的；
        `/proposal-plan.html` 因為 pathname 不同會是跨頁導覽 → `target=_blank`。
        元件因此不碰 `window.switchTab`（那是它不准 import 的東西）。
     3. **overlay 收在 `tab-changed`**：不是給 flow-view 一個 onNavigate 回呼。
        原本切走 tab 時 overlay 會留在畫面上蓋著別的分頁（側欄點按／上一頁／
        貼網址三條路本來就有這個 bug），收在既有的 tab-changed 監聽一處全包。
     4. **指回自己的燈不畫連結**：兩個掛載點都在提案工作區裡，所以由呼叫端傳
        `here='preprod_proposals'`。企劃軌那 6 盞燈該做的事就在手邊，把人送去
        提案庫清單反而是推離現場。由呼叫端說而不是讀 `location.hash` ——
        兩個掛載點的答案一樣，讀網址卻只在其中一個會對。
   - 4B ✅ 清單階段 chip + 微型完成條（2026-08-14）。與 §14.4 草案的差異：
     **五格不是四格**（收割軌是後來加的），而且**不是四格條而是五個量杯格**
     —— 一軌一格、由下往上填，空的那格看得出來。
     資料走新的 `GET /crm/projects/flow/summary?ids=`：`_gather_facts` 改成
     批次驅動（從 `crm_projects` 驅動 + 相關純量子查詢），**一趟查完且與專案
     數無關**，而且與詳情面板**同一支** —— 清單顯示 3/5、點進去 2/5 是這個
     功能最快失去信任的方式，e2e 直接拿兩支端點的數字對撞。
     摘要不回燈號明細（整包 tracks 是 4KB/專案，清單用不到其中 88%）。
     排序：點「進度」欄＝按整體完成比例排，也就是「哪個案子最卡」。
     沒有殼專案／摘要還沒到 → 畫「—」，不畫五個空格子（空格子跟「五軌全零」
     長得一樣，那是謊報）。
     畫面那半（flow-badge.js）是**靜態** import：它只依賴新抽出的
     `js/shared/dom.js`（零依賴的 `esc` + `ensureStyle`）。原本為了避開
     utils.js（57KB，還靜態拉 clip_utils 8KB，而前端一律 no-store）改成動態
     載入 —— 實測那只是把 68KB 延後、沒有省掉，還多三個序列 RTT。
     權限仍在後端算成 `links[key]`（同 `can_check` / `can_advance` 的慣例），
     但用 **`core.auth.tab_modules`**：提案庫／片庫本來就收不只一個模組，只認
     同名的話拍攝企劃的人會在自己**進得去**的 tab 上看到「你沒有權限」。
     那份名單順勢從兩個 router 收進 `core.auth.TAB_ACCESS`（它本來就是那些
     閘門的參數），前端 `TAB_EXTRA_ACCESS` 降為跨語言鏡射，由
     `test_rbac_module_sync` 比對 —— 它立刻抓到提案庫漏了 `preprod_plan`
     （那些人打 API 進得去、側欄卻沒有那個 tab），一併補上。

   - 4C ✅ 邊界態 + 重抓時機（2026-08-14）。§14.4 剩下的三條，共同點是
     **它們都只在畫面上存在** —— 後端照樣收得下勾選、照樣回得出缺項，所以
     沒有任何後端測試會發現它們壞掉（`tests/e2e/test_flow_edge_states.py`）。
     1. **終態凍結唯讀**（未成案／歸檔）：吃後端已經在回的 `stage.is_terminal`
        （哪些算終態是 `core.project_flow.TERMINAL` 的政策，前端不比對字面值），
        勾選項不再可點、「去完成」整個不畫、軌色轉灰。已亮的燈維持原樣 ——
        對一個結束的案子，那是歷史紀錄不是待辦。
        🔴 **凍結是畫面的事，不是守衛**：端點仍然收得下（歸檔後才發現里程碑
        記錯，admin 要補得回來），與這功能其他地方的軟性口徑一致。
     2. **沒有殼專案 → 一顆真按鈕**：`POST /proposals/{id}/project/shell`
        （冪等）。草案寫的是「補客戶 CTA」，但那是 `crm_projects.client_id`
        放寬前的前提 —— 現在殼專案不等客戶，所以按鈕直接做真正要做的事。
        `/proposal-plan.html` 因此**改成一律掛**進度分頁：那頁正是會發現
        「這案子沒進管線」的地方。
        ⚠️ 第一版是借 `PUT /proposals/{id}` 尾端的自癒副作用（空 body），
        /simplify 第二輪換掉：那條的契約是「只動 payload 有帶的欄位」，空
        body 在那份契約下**是 no-op**，能成立純粹因為它尾端剛好沒有
        early-return；而且借來的東西自帶包袱 —— 空 PUT 照樣蓋 `updated_at`，
        清單是 `updated_at.desc()` 排的，所以對一筆「擱置」提案按下按鈕，
        唯一的效果是把它浮到最上面。同一個理由早就寫在隔壁
        `PATCH /{pid}/project` 的開頭（帶副作用的動作要有自己的名字）。
        自動補件的政策正本＝`api_proposals.needs_shell_backfill`（第三輪從
        `enters_pipeline` 改名：它回 False 的正是**已經在**管線裡的提案，
        名字是反的）。它只服務兩個**自動**消費者（PUT 尾巴、startup 遷移）；
        這支端點**不問它** —— 豁免的用意是「別讓批次補件把停擺的案子洗進
        管線」，而使用者按一次按鈕不是批次補件。第二輪那個 409 分支因此拿掉：
        它的文案還說了假話（建立提案本來就無條件建殼，所以「這個狀態不會進
        管線」不是通則），而且教使用者去走另一支端點的隱性側門。
        遷移的 SQL 用 `coalesce(status,'') != '擱置'` 與那支**逐字對應**：
        裸的 `status != '擱置'` 對 NULL 是 false（狀態未設的提案永遠遷不到），
        而只篩 `project_id` 會讓每台機隊開機都把停擺提案逐列上鎖、把整列
        （plan JSONB 上限 200KB）拉過網路只為了讀兩個純量欄位。
        端點本身走 `for_update` —— 兩個分頁各按一次會建出兩個殼，其中一個
        變孤兒。
     3. **切回分頁重抓**（v1 仍不做輪詢）：掛在元件自己身上，兩個掛載點各自
        的分頁快取（SPA 的 `_mounted` Set／企劃頁的 `_pending` 槽）都不用改。
        🔴 用 **ResizeObserver 不是 IntersectionObserver**：IO 講的是「有沒有
        進到視窗裡」，捲到畫面外的 host 藏起來再顯示時兩次都是 not-intersecting
        ——它根本不會叫（實測症狀＝單跑過、跟別支一起跑就紅）。
        只認「看不見→看得見」那一次轉換（視窗縮放不算），並有 15s 新鮮度門檻。
        ⚠️ RO 的回報是**合併**的：同一 frame 內藏了又顯示只會收到一次「現在
        看得見」→ 不算轉換、不重抓（對使用者是對的）。轉換狀態因此記在
        `host.__flow.visible` 而不是閉包變數 —— 看得到才等得了、才驗得了。
        收拾走**一個** RO（收得下多個 target）+ 一個 document 監聽 + 一份名冊：
        關鍵是 `observe()` 當下就會叫一次，所以「開下一個提案的進度分頁」本身
        就是清掉上一個死掉的那個的時機 —— 而那正是「已經 display:none 才被
        移除」這個最常見情境下唯一會來的訊號。
     4. **一個 `onChanged()` 回呼**（原本是 onAdvanced / onLinked 兩個）。
        兩個都沒有呼叫端用得到參數，分開的唯一效果就是可以漏接一半 —— 而
        `/proposal-plan.html` 就漏了 onAdvanced：從那頁推進階段會連動衛星提案
        的狀態，標頭的狀態下拉卻一直顯示舊值。
        🔴 它在元件重畫自己**之前**被呼叫，而且**會被 await**：呼叫端有權把
        host 拆掉（SPA 就是整個重開詳情），拆掉之後就不必再抓那份會被丟掉的
        聚合查詢。不 await 的話這個守衛是**假的** —— SPA 的回呼同步返回、在
        它自己的 await 之後才換掉 overlay，所以檢查那一刻 host 永遠還在
        （/simplify 第二輪抓到；回歸測試
        `test_no_refetch_when_the_caller_tears_the_host_down`）。

測試：unit＝每個 auto 訊號正反面 + RBAC 反面（提案庫-only 勾選要 403）；
e2e＝勾選、推進、未成案原因守衛、公開頁不出 flow、終態凍結、自癒建殼、
切回重抓（含「才剛抓過就別再抓」的反面）。

### §14.7 決策點

**已由 owner 2026-08-14「各自工作、專案一起完成、資料放正確的位置」定調解決**：

3. ~~提案庫-only 的人可否勾手動項~~ → **不可**（各自工作＝勾選是專案負責人
   的事，勾選權跟著 `crm_projects`）
6. ~~專案成員列級勾選權 v1 要不要做~~ → **v1 不做**。且 v2 的正確形狀因此
   明朗：不是讓剪輯師來提案頁勾，而是在**他自己的工作面**放「回報完成」
   （各自工作原則的延伸），資料橋（staff_id × 派工）現成，需求出現再做
7. ~~製作／後期人員要不要看工作流頁~~ → **不需要**（他們的工作面＝後期
   製作群 tab，做的事透過資料自動亮燈；個別人想看再給 `preprod_proposals`）

**仍待拍板（都有建議值，說「照建議」即動工）**：

1. v1 步驟草案（§14.2 四軌）可否？要不要依專案類型（紀實/廣告/MV…）給
   不同 checklist？（建議 v1 一套通用，用了再分化）
2. 推進遇缺項：軟擋（確認框）還是硬擋？（建議軟擋）
4. 歸檔要不要自動（結案 N 天後）？（建議 v1 手動）
5. 「款項結清」要不要擋結案？（建議不擋，黃燈提示）
8. 復盤 v1 範圍：只放歸檔 gate 的手動勾選項，還是連 §14.8 的數據對照頁
   一起做？（建議 v1 只放勾選項＋復盤會議走既有會議記錄；數據頁＝下一段）

### §14.8 工時與專案復盤（owner 2026-08-14：「最終員工會登記製作工時、
會有專案復盤」——方向確認，v1 只接訊號，完整復盤另段）

兩件事的**正確位置**都已存在，照「鏡頭不是容器」原則接入：

**工時**：正本＝`timesheets`（N2 階段0 已上線：Google Sheet 同步 + burn
檢核；團隊既有習慣＝Sheet 填小時）。工作流只做一件事：製作軌加「工時登記中」
auto 訊號，hover 帶消耗 vs 時數池。員工在哪登記維持不變（Sheet／之後的
專案工時 tab）——**各自工作，燈自己亮**。

**專案復盤**＝結案後的學習迴圈，兩個面、各有各的家：

- **數據面**（＝一直鎖著等 N2 的 B2 估價復盤，這裡是它的落點）：
  read-time 對照表——報價的預估工時/金額（crm_quotations）vs 實際工時
  （timesheets × staff_rate_history 當期費率＝實際人力成本）vs 實際支出
  （專案帳目）vs 實收（發票/請款）。全部從正本聚合，**零新資料**；
  呈現為進度分頁在「結案/歸檔」階段長出的「復盤」區塊或獨立分頁。
  ⚠ 前置依賴：staff_rate_history 已落地（H1 ✓）、timesheets↔專案關聯鍵
  （動工時確認）。
- **敘事面**：復盤會議＝就是一場會議——**直接複用 §13 會議記錄管線**
  （現場錄音/上傳 → 逐字稿 → AI 整理），開在該提案的會議記錄分頁，
  標題慣例「專案復盤」。不另造復盤表。win/loss 的組織學習欄
  （outcome_reason）既有，不動。
- **工作流掛鉤**：歸檔 gate 加「專案復盤完成」手動勾選項（v1）；
  v2 可改 auto＝偵測到復盤會議記錄存在。

推進次序建議：工時訊號（便宜，資料現成）進 v1 階段一；復盤勾選項進 v1
階段三；復盤數據頁＝§14 完成後的下一段（B2 正式動工），不擠進 v1。

### §14.9 PARA 對映（2026-08-14 owner 問「用 PARA 的觀點怎麼規劃」）

系統現況與 PARA 的對映——**大結構已經是 PARA**，不需搬遷：

| PARA | 判準 | 本系統的對應（既有） |
|---|---|---|
| **P**rojects | 有目標有終點的努力 | `crm_projects` 活躍階段（投標→結案）；§14 工作流頁＝P 層的駕駛艙 |
| **A**reas | 無終點的持續責任（維持水準） | 客戶管理（分級）、財務管理（月結/現金流）、人事（費率/請補修）、器材庫（折舊/稼動）、官網經營、社群 |
| **R**esources | 跨案可重用的參考 | **片庫**（跨專案引用）、場景庫、企劃**範本庫**、素材庫（逐字稿檢索）、產業情報、報價範本 |
| **A**rchives | 不再動、可搜尋、不佔視野 | 「歸檔」階段、未成案（帶 outcome_reason）、LEGACY 表 |

PARA 給「資料放正確的位置」一個**可判定的規則**（actionability）：

> 只對這個案子有用 → 住專案（衛星列/專案帳目）；跨案子可重用 → 升級進庫
> （Resources）；持續責任的口徑 → 住 Area tab；不再動 → 歸檔（可搜尋、
> 移出視野）。

PARA 也反向印證兩個既有設計：工作流頁只顯示 actionable-now（P 視圖不混
Area 指標——現金流、稼動率留在各自 Area tab，別塞進專案進度頁）；
未成案不是垃圾是檔案庫（可復活：改狀態/換綁/情報轉提案都是既有路）。

**PARA 挑出的唯一缺口＝「結案收割」（harvest）**：Forte 的結案儀式裡有一步
「離開前把可重用的東西撈出來升級到 Resources」——系統的升級動作**全部現成**
（企劃「設為範本」、參考影片入片庫跨案引用、場景入場景庫、素材掃描入索引、
學習寫 outcome_reason/復盤會議），缺的只是**在對的時刻把它們排在眼前**。
落點：復盤區塊（§14.8）加一組「收割清單」手動勾選項：

    範本收割：這案的企劃書值得設為範本嗎？    [去設為範本]
    片庫收割：研究用的參考片都入庫了嗎？      [去片庫]
    場景收割：新場景要建檔嗎？                [去場景庫]
    素材收割：素材庫索引掃過了嗎？            [去素材庫]

四項全是**提醒＋deep-link**，零新儲存（勾選記錄住既有 flow_checks 表）——
又一個鏡頭，不是容器。進 v1 階段三（與復盤勾選項同批，便宜）。

### §14.10 動工前的現況校正（2026-08-14 讀碼後，**推翻上面四處假設**）

「照建議執行」後實際讀 schema 與既有實作，發現規劃時的四個假設有誤。
以本節為準（上面各節保留原文供追溯決策脈絡）：

**校正 1：復盤與歸檔清單「已經做好了」**——`crm_projects.archive_checklist`
（`core/project_archive.py` 5 列範本＋自訂列＋待收/已收/不適用＋資料夾掃描
自動勾）與 `crm_projects.review_kpta`（Keep/Problem/Try/Action 四欄）已上線，
掛在專案詳情「完稿結案」分頁最上方，端點在 `routers/crm/archive.py`。
→ §14.8 的「復盤完成」**不做手動勾選項**，改成**讀 KPTA 的 auto 訊號**；
歸檔清單同理。§14.9 的收割清單也不另造 —— 見校正 3。

**校正 2：儲存改 JSONB 欄，不新增表**。既有兩個先例（`proposal_survey` /
`project_archive`）都是**「欄目在程式碼、值在 DB JSONB」**＋共用
`core/row_table.py`。手動勾選項改存 `crm_projects.flow_checks` JSONB
（startup `ALTER TABLE ADD COLUMN IF NOT EXISTS`，房規），
不建 `crm_project_flow_checks` 表、不寫 migration 檔。

**校正 3：PARA 收割絕大部分可以 auto**（比規劃時樂觀）——反查鍵都現成：

| 收割項 | 訊號來源（已存在） |
|---|---|
| 企劃範本 | `preprod_brief_templates.source_project_id` |
| 片庫 | `preprod_reference_links(target_type='crm_project', target_id=)` |
| 場景庫 | `preprod_location_usages.project_id` |
| 素材庫索引 | `footage_index.project_id` |

→ 收割獨立成**第五軌**（原規劃塞進復盤區塊），四項全 auto、零手動。

**校正 4：🔴 RBAC 現況與 §14.5 假設不符**（2026-08-14 實測 dev+生產，
**下面這段是量出來的，不是讀碼推論的** —— 第一版讀碼推論寫成「整個 CRM
後端都是管理員限定」，實測發現只對了一半）：

| CRM 端點 | 實際守衛 | 實測 |
|---|---|---|
| `GET /crm/projects`（清單） | **無** | 匿名 200，生產讀到 236 筆 |
| `GET /crm/projects/{id}`（詳情） | **無** | 匿名 200 |
| `POST/PUT/PATCH/DELETE /crm/projects…` | `_check_auth`＝`check_admin`（Lv3） | lv1+crm_projects → 403 |
| `/crm/projects/{id}/archive`（歸檔/回顧） | 同上（Lv3） | lv1 → 403 |
| `/crm/projects/{id}/website-stage` | `_check_website_auth`（website_admin） | — |

→ 讀取端**完全沒有守衛**（見 §14.12 安全發現），寫入端才是 Lv3。
`crm_projects` 等模組 key 目前**只控前端 tab 顯示**，後端不吃。
所以 §14.5 那張表寫的「勾手動項＝`check_admin_or_module('crm_projects')`」
與現況不一致：真給了模組級寫入權，會出現「能勾進度、卻不能改專案狀態」的
怪組合（生產有 3 個 lv1 帳號被授予 `crm_projects`，他們今天點寫入就是 403）。

**✅ owner 2026-08-14 拍板 (b) 模組級**（已實作）：勾手動里程碑＝
`check_admin_or_module('crm_projects')`。這是 CRM 寫入面第一道模組級鬆綁
—— 生產 3 個 lv1 帳號（nashtsai / Ryansnap / soca）被授予 `crm_projects`
卻打不了任何 CRM 寫入端點，權限是空頭支票，這裡先兌現。
**階段推進仍走既有端點的既有守衛（Lv3），不在這裡放寬**（動商務主軸＝
動客戶分級與錢流口徑，量級不同）。
`GET /flow` 回傳 `can_check`，前端據此決定畫 checkbox 還是唯讀圓點 ——
提案庫-only 的人看得到全部進度、動不了（決策點 3）。

### §14.12 🔴 安全發現：CRM 專案讀取端點匿名可讀（2026-08-14，查權限時發現）

`routers/crm/projects.py` 的 `list_projects`（L60）與 `get_project`（L472）
**連 `request` 參數都沒有** → 沒有任何守衛。實測（唯讀）：

- dev 8001 匿名 `GET /api/v1/crm/projects` → 200，30 筆
- **生產 8000 匿名 → 200，236 筆**

回傳欄位含 `contract_amount`（合約金額）、`amount_receivable` /
`amount_received`（應收/已收）、`profit_target_pct`（利潤目標）、
`client_short_name`（客戶）、`am_username` / `pm_usernames`（負責人）、
`notes`、`folder_path`。⚠️ 8000 經 cloudflared 對外（foundry），
若 tunnel 前沒有 CF Access，等同對網際網路公開整個案件清單與金額。

**✅ 已修（2026-08-14，owner 同意）**。實測發現範圍比兩支大得多：匿名打得到
的是**整個 CRM 讀取面** —— projects / clients / staff / quotations / invoices /
payments / cash-entries / receivables-summary / payables-summary…

修法＝在 `_shared.router` 掛 router 層級的 `_crm_read_guard`（`check_logged_in`）：

- **為什麼掛 router 不逐支加**：漏一支等於沒修（當初就是這樣漏的），
  而 CRM 有 140+ 端點還在長。掛這裡，新端點預設就是安全的。
- **為什麼是 `check_logged_in` 不是 `check_admin`**：器材庫/場景庫/看片門戶/
  素材庫/現金流/提案庫六個分頁都在讀 CRM 清單，那些使用者是模組級授權、
  不是管理員 —— 用 admin 會把他們的畫面全打壞。
- **為什麼不是 `check_lan_or_logged_in`**：那條是**後期製作工具**的產品前提
  （owner 2026-08-12 拍板，剪輯師本機不登入直接用）。owner 2026-08-14 再次
  確認「後期製作 tab 是工具，開放使用」—— 本次修正**完全不碰**那批端點。
- `/crm/public/site/{works,team}` 放行（對外官網的 Astro build 在讀）。

實測三類（dev 8001）：商務資料匿名 401 / 模組使用者 200 / admin 200；
官網兩支維持匿名 200；後期製作十支（pick_folder、browse_dir、status、queue、
job_history、bookmarks、agents、tts/voices、drive_map、reports/history）
全部維持匿名 200 未受影響。
回歸測試 `tests/unit/test_crm_read_guard.py`：**列舉** app 上所有 CRM GET
路由逐一驗證匿名 401（不是挑幾支測）—— 將來有人在別的 router 上加
`/api/v1/crm/…` 也會被抓到。拿掉守衛後 8 條紅（已反向驗證）。

### §14.11 與 CRM 專案管理的分界線（owner 2026-08-14 問「最重要的功能差別是什麼」）

**一句話**：CRM 專案管理是**資料的家**（每個領域各自編輯），工作流頁是
**狀態的鏡頭**（跨領域只讀完成度）。兩者切的是同一批資料的**不同軸**——
CRM 按「領域」直切（客戶/報價/人力/帳務/結案各一個分頁），工作流橫切
（一個案子此刻在所有領域的完成度）。

三個實質差異：

| | CRM 專案管理 | 工作流頁 |
|---|---|---|
| 資料所有權 | **擁有**（CRUD 各領域正本） | **零擁有**（只有 2 個手動里程碑） |
| 切法 | 領域（縱） | 完成度（橫） |
| 可見範圍 | `check_admin`（Lv3）—— 企劃人員**完全看不到** | `proposal_auth` —— 企劃/提案人員看得到 |

🔴 **最大的差別其實是第三列**：CRM 後端整片是 Lv3 管理員限定，所以今天
提案企劃人員**看不到專案的任何狀態**。工作流頁是他們第一次能看到「我的案子
走到哪」。這不是重複，這就是這個功能存在的理由。反過來說——**如果團隊每個
人都是管理員，這個功能的價值就只剩「省去在七個分頁之間翻」**（仍然有用，
但等級差很多）。要衡量值不值得做，先看非管理員帳號有幾個。

**可判定的分界規則**（新東西要不要進工作流頁，用這條裁）：

> 工作流頁只回答「**還缺什麼**」，CRM 回答「**內容是什麼**」。

- ✅ 可以出現：有/沒有、幾分之幾、誰在什麼時候標的、去哪裡完成的連結
- ❌ 不可以出現：金額數字、內容全文、可編輯的欄位、任何領域的 CRUD
- ✅ 唯一的寫入：兩個手動里程碑（開拍/剪輯完成）＋ 階段推進，
  而階段推進**打的是既有端點**——是同一台電視的第二個遙控器，
  不是第二個真相來源

**已知的重疊風險**（會讓這頁長成 CRM #2 的三條路，動工時擋住）：
① 「去完成」連結愈加愈多 → 變成導覽列；② 有人要求「順手在這裡改一下」→
開始長編輯欄位；③ 為了好看把金額/內容搬進來 → 變成儀表板。
三條的共同解法就是上面那條規則。

**校正 5：訊號欄位對照**（動工時的實際名稱，⚠ 規劃時未定的都在這）：

| 訊號 | 來源 |
|---|---|
| 報價成交 | `crm_quotations.status == '已簽核'` |
| 已請款 | `crm_invoices(project_id, payment_type='收款')` 有列 |
| 款項結清 | 同上且無 `payment_status == '未收款'` |
| 工時 | `timesheets.project_id`（⚠ Sheet 列可能為 NULL，退而用 project_name 對映） |
| 素材入庫 | `project_media_files.project_id` |
| 客戶審批 | `portal_review_links.status == '已核准'` |
| 上架完成度 | `crm_project_showcase` × `core.crm_logic.work_completeness` |
| 官網上線 | `crm_project_showcase.published`；`prod_stage == '不上官網'` → 略過 |
- `renderMeetings` **刻意不收**上一輪的錄音：兩個呼叫端都不會對同一個 host
  重掛，而真重掛時 `_stopRecording` 只是「開始停」，下一行 `__mv` 就被換掉
  —— 收尾會拿到新的 `proposalId`，把舊提案的錄音 POST 到新提案去。
- 錄音只在記憶體裡：錄製中掛 `beforeunload` 攔關分頁/重整。一次只錄一筆
  （`host.__mv.rec`）；取消要 confirm，且不上傳。
- 回歸測試 `tests/e2e/test_meeting_recorder.py`（3 條）：**按鈕是條件長出來的**
  （拿掉 `MediaRecorder` 後按鈕要消失 —— 少了這半，一顆寫死的按鈕也會全過）、
  開始/取消的狀態轉換＋麥克風軌道真的停掉、錄音列撐得過整份重畫。
  🔴 Windows 的 headless Chromium **一個音訊輸入裝置都沒有**
  （`--use-fake-device-for-media-capture` 也救不了，實測回 `NotFoundError`），
  所以測試把 `getUserMedia` 換成 AudioContext 產生的真 MediaStream ——
  驗的是我們的狀態機，不是瀏覽器的裝置層。
