# health 報告 2026-09-13（第二次）

> 同日第二次：上午那份（`docs/health/2026-09-13.md`）之後又進了 5 個功能 commit（`16d8cc3e`…`dad7fbca`：現金匯款推送、
> 消耗率／預算一顆、要補填提醒條、案源四選項）＋ 一輪 /polish（`ed63c477`…`af1504af`）。這份只比「跟上午差在哪」，
> 依賴／衛生兩段今天沒人動，數字沿用上午的快取（不重跑）。

```
基準測試：通過（3953 支，42 秒）                                   上次：3783 / 34（上午那份之後 +170 支）
階段一 靜態閘門：ruff 0 ／ eslint 0 ／ pyright 5752 errors（0 reportUndefinedVariable；reportCallIssue 1687 全是 SQLAlchemy
                model 建構子的型別噪音）／ 前端漏 import 0 ／ preflight OK ／ 寫卡 0 張
階段二 依賴：沿用上午（釘版落後 2 ／ CVE 16 套 120 條，機隊中 pillow ／ requirements 漂移 25）—— 今天沒人動 requirements
階段三 衛生：沿用上午（候選刪除 7 ／ 候選 gitignore 1 ／ 大檔 0 ／ 一次性腳本 35）；git 追蹤中無 ignore 命中、無 pyc
階段四 測試：最慢 8.4 秒（test_every_frontend_module_parses）／ skipped 0 ／ xpass 0 ／ 覆蓋率 35.8%（core 77.3、routers 22.7、
            services 21.6）／ 補了 1 個檔 3 支（今天動過的純函式）
階段五 結構債：>2000 行 1（core_engine.py 2350）／ 1500–2000 行 py 5 + js 6（crm-invoices.js 1521 新跨線）／ 其餘同上午
階段六 文件：模組職責表這輪 /polish 已補 2 列 ／ CHANGELOG 仍落後（1.10.0 vs 2.5.12）／ ROADMAP／PLAN 未重查（同日）
commits：2e2ae85b（tests）、本檔
停止原因：正常結束（主流程 commit 2／8、卡片 0／10）
```

## 階段一明細

- `ruff check .`：All checks passed。`npm run lint`：0。`preflight.py`：OK（19 imports verified、21 非 agent 依賴跳過）。
- pyright 5752 errors／699 檔（上午 4237：上午那次沒把 `tests/`、`scripts/` 算進來，不可直接比）。前六名規則：
  reportPossiblyUnboundVariable 1803、reportCallIssue 1687、reportArgumentType 1113、reportAttributeAccessIssue 573、
  reportGeneralTypeIssues 242、reportOptionalSubscript 108。`reportUndefinedVariable` 0。reportCallIssue 抽樣全是
  `db/repos/*`、`db/seed_*` 的 model 建構子 `No parameter named "id"`（SQLAlchemy declarative 沒有型別 stub）—— 噪音。
- 前端 no-undef（臨時 config，同上午的 `eslint.noundef.mjs`）：613 筆、ES module 內 112 筆，全部是 `window.X =` 慣例
  （`appendLog`／`getComputeBaseUrl`／`fetchModelStatus`／`_doImportFromQuote`／`loadDictionary`）或外部函式庫全域
  （`io`／`google`）。**真的 0 筆**（上午那筆 remote-dispatch 的 BUG-4 已修）。今天新增的 `ts-zone/remind.js` 乾淨。

## 階段四明細

- 最慢 8 支：8.35s test_every_frontend_module_parses、5.61s test_the_scan_actually_finds_lazy_imports、4.57s
  test_no_undefined_names_in_es_modules、3.50s ×2 test_cash_entry_writers、2.74s test_dep_scan、2.41s test_office_surface、
  2.04s test_frontend_request_budget。都是掃整個 repo 的守衛，> 5 秒的兩支上午已列。
- 候選（30 天改 ≥3 次且覆蓋 <40%）48 檔，前五：routers/crm/finance.py（77 次／19.6%）、routers/crm/projects.py（49／18.6）、
  routers/api_finance_projects.py（41／25.0）、routers/api_finance_stmt.py（31／12.8）、routers/api_me.py（37／28.2）。
  上午已判定：這幾支 router 的純函式都釘住了，剩下的全吃 DB，mock 鏡像測試沒價值。這次改補**今天動過的純函式**：
  `2e2ae85b` tests/unit/test_ledger_project_health.py（link_note 自接＝不抽代辦費／混合、burn_rate 邊界、
  missing_log_days 對舊 Sheet 列沒狀態的處理）。api_me 的 `/me/reminders` 在 /polish 安全網已有整條走一遍的假 session 測試。

## 這輪 /polish（16d8cc3e^..HEAD）

- 安全網 `ed63c477`：/me/reminders 端點特徵測試。
- review 1 輪 → 寫卡 1 張、修 1 張（`fcb1cf6d` BUG-1：週記「去寫」連點兩個不同週、iframe 還在載時 load listener 閉包送第一個週）；
  第 2 輪無新發現 → 收斂。
- simplify 1 輪 `fa437e13`（拿掉前端沒用的 `pending_status` 鍵，< 20 行 → 收斂）。
- maintainability `af1504af`：CLAUDE.md 模組職責 +2 列（要補填提醒、預算／消耗率）、不要動的地方 +1（自接又可選）。

## 沒動的東西（依影響排序）

1. `link_note` 的「案源 代開發票」字面 vs 下拉的「自接（代開發票）」—— 母帳「後期連結」那一行仍寫原值；owner 看得懂原值，先不改。
2. ts-projects.js `projectFileHtml` 傳給 `budgetChip` 的 editable 表達式跟函式內的 `d.mapped` 重複判一次 —— 測試釘住那段字串
   （`test_batch2_hr_guards`），simplify 不動測試斷言，留著。
3. 依賴／衛生／ROADMAP／PLAN：同日不重查，見上午那份。
4. CHANGELOG 落後（1.10.0 vs version.json 2.5.12）—— 發版流程的事，不在 health 權限內。

## 未執行

- `pip list --outdated`／`pip-audit`／`npm audit`／衛生盤點：同日第二次，沿用上午快取。
- `/polish --debt` 全量：只重算了「超長檔案」那一項（唯一可能因今天 +468 行變動的）；超長函式／巢狀／循環 import 同上午。
