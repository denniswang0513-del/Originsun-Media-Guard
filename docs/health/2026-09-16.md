# health 報告 2026-09-16

> 上次：2026-09-15。中間進了 09-15 那份的五項處理（`a215d377`：刪 5 個根目錄孤兒檔＋lock 檔、Pillow 12.3.0、astro 7.3.2、
> STALE_PATHS 補兩條、ROADMAP 勾 3 項）、請假申請單三步（`services/leave_application.py`）、彈性外出、休假標示、
> 行事曆獨立頁、工作台切版修正，發到 v2.5.35。這份是完整跑（六個階段都重算）。

```
health 報告 2026-09-16                                              上次：2026-09-15

基準測試：通過（4068 支，42.7 秒*）                                  上次：4010 / 42.6
階段一 靜態閘門：ruff 0 ／ eslint 0 ／ pyright 5946 errors（0 reportUndefinedVariable）／ 前端漏 import 0 ／ preflight OK ／ 寫卡 0 張
階段二 依賴：釘版落後主版本 3 條（2 套，同上次）／ CVE 15 套 95 條（機隊中：0）／ npm high+ 0 ／ requirements 漂移 1 項
階段三 衛生：候選刪除 1（＋本機殘留 5，不在 git）／ 候選 gitignore 0 ／ 大檔 0（最大 3.3MB）／ 一次性腳本 23（>60 天 1）
階段四 測試：最慢 8.3 秒（test_every_frontend_module_parses）／ skipped 0 ／ xpass 0 ／ 覆蓋率 35.9%（core 77.9、routers 22.5、services 21.8）／ 補了 3 個模組 14 支測試
階段五 結構債：>2000 行 1（core_engine.py 2350）／ 1500–2000 行 py 5 + js 5 ／ >200 行函式 11 ／ 巢狀 ≥5 層 95 ／ 循環 import 0
階段六 文件：模組表缺 5（同上次那五個）／ ROADMAP 可勾 3 ／ CHANGELOG 同步 ／ 過期 PLAN 0 ／ STALE_PATHS 齊 ／ CLAUDE.md 死連結 1
commits：03d34a97（leave_application tests）、2e9df920（timesheet_self tests）、14d07487（timesheet_ingest tests）、本檔
停止原因：正常結束（主流程 commit 4／8，卡片 0／10）
```

\* 第一次基準跟 eslint／ruff 同時跑量到 71.5 秒（跟上次同一種干擾）；乾淨全套 42.7 秒（含新測試 4082 支）、最慢一支單獨量 8.27 秒。

## 上次報告的十條，這次的下落

| # | 09-15 的項目 | 這次 |
|---|---|---|
| 1 | STALE_PATHS 漏 `db/models.py`／`core/finance_logic.py` | ✅ `a215d377` 補齊，三條都在 |
| 2 | npm critical astro ≤7.2.7＋high 9 | ✅ astro 7.3.2／vite 8.3.0／sharp 0.35.4，`npm audit --omit=dev` **0** |
| 3 | pillow 12.2.0 25 條 CVE、在機隊 | ✅ `49a0b719` 釘 12.3.0（server／agent 都釘），pip-audit 已無 pillow；**機隊中的 CVE 歸零** |
| 4 | lock 檔可刪、xxhash／aiofiles 落後主版 | lock 檔已刪（漂移 12 → 1）；xxhash 3.6.0→4.0.1、aiofiles 24→25 仍在（走 `requirements_agent.txt`，不在 health 權限） |
| 5 | 根目錄 5 個零引用檔 | ✅ 全刪 |
| 6 | 本機殘留 4 個 | 還在（不在 git，可直接刪；這次多一個 `dev_boot.log`） |
| 7 | 模組職責表缺 5 | 還是那五個（finance_statements／api_finance_stmt／api_crm_mobile／timesheet_manual／timesheet_lookup） |
| 8 | ROADMAP 可勾 2 | ✅ owner 勾了 3 項（多勾了 J-4 那條） |
| 9 | core_engine.py 2350 行 | 未動 |
| 10 | 最慢測試 8.7 秒 | 8.27 秒，刻意全掃，留著 |

## 沒動的東西（依影響排序，一行一條；想追再看附錄）

1. **`requirements_agent.txt` 的 `xxhash==3.6.0`（最新 4.0.1）與 `aiofiles==24.1.0`（25.1.0）落後主版本** —— 跟上次一樣，
   升了就是全機隊；xxhash 是備份 hash 的核心，主版本跳號要先確認 digest 相容（不然「比對驗證」會把舊報表全判成不符）。owner 拍板。
2. **pip-audit 15 套 95 條，全部不在 `requirements_agent.txt`**：gitpython 26／pip 12／starlette 10／setuptools 8／pypdf 8…（附錄 B）。
   starlette 0.52.1 是 FastAPI 的傳遞依賴、跑在 master 與 NAS 容器；pypdf 在 `requirements_server.txt`。升級不在 health 權限內。
3. **`requirements_server.txt` 與 `requirements_agent.txt` 的 `python-multipart` 版本不同**（0.0.32 vs 0.0.22）—— 唯一剩下的漂移；
   上傳（發票影像／收據／存摺影本）兩邊行為可能有細微差（0.0.22 → 0.0.32 之間有 CVE 修補），機隊那份比較舊。
4. **`title_name.txt`**（根目錄、追蹤中、兩行測試字串、初始 commit 進來、零引用）—— 候選刪除（09-13 就列過，09-15 那批漏了它）。
5. **CLAUDE.md:1104 指的 `tests/unit/test_finance_files_stay_readable.py` 不存在**（現在叫 `test_files_stay_readable.py`）—— 死連結，
   下次 /polish 的 maintainability 階段順手改。
6. **ROADMAP 對得上、可勾的 3 項**（不勾，owner 決定）：L696「User 加 `staff_id` 關聯人力庫」← v2.2.0 N0（`db/models/_system.py:154`）；
   L463「選擇專案後自動帶入 project_name／三根」← 09-10 備份頁綁專案（`frontend/tabs/backup` 打 `/api/v1/projects/backup-roots`）；
   L799「H2 請假假勤：極簡申請+核可+額度」← `8572a72e` 假勤一期 ＋ 09-15 申請單（`HrLeaveApplication`）。
7. **模組職責表沒登記、30 天內動 ≥10 次的模組，跟上次同五個**：`services/finance_statements.py`（32）、`routers/api_finance_stmt.py`（31）、
   `routers/api_crm_mobile.py`（22）、`services/timesheet_manual.py`（20）、`services/timesheet_lookup.py`（14）。連兩次上榜，下次 /polish 補。
8. **`core_engine.py` 2350 行仍是唯一紅檔**；`core/worker.py::_execute_job` 巢狀 12 層；11 支 >200 行函式一支都沒少（附錄 E）。
9. 本機殘留（已 ignore，不在 git）：`settings.json.bak_20260808_214848`、`uvicorn_err.log.bak`、`uvicorn_out.log.bak`、`dev8001_boot.log`、
   `dev_boot.log`——可直接刪。
10. `test_every_frontend_module_parses` 8.27 秒（> 5 秒紅）——刻意全掃，留著（同前兩次）。

## 未執行

- 無。六個階段全部跑完；工具都在 `.venv`（ruff／pyright 1.1.414／pytest-cov／pip-audit），沒裝新東西。
- 階段一沒寫卡：四個閘門都是零（ruff／eslint／前端漏 import／preflight），pyright 0 筆 `reportUndefinedVariable`。

---

## 附錄 A：階段一明細

- `ruff check .`：All checks passed。`npm run lint`：0。`preflight.py`：OK（19 imports verified、21 非 agent 依賴跳過）。
- pyright 5946 errors／292 檔（上次 5853／289；+93 大致＝新進的 leave_application／api_calendar／彈性外出那批）。
  前六名：reportPossiblyUnboundVariable 1885、reportCallIssue 1741、reportArgumentType 1139、reportAttributeAccessIssue 587、
  reportGeneralTypeIssues 254、reportOptionalMemberAccess 108。`reportUndefinedVariable` **0**。
  reportCallIssue 前 20 筆全是 SQLAlchemy model 建構子 `No parameter named "id"`（bookmarks_repo／seed_cash_taxonomy／seed_finance）；
  reportAttributeAccessIssue 前 20 筆是 `Result.rowcount`、`Cannot assign to attribute "updated_at" for class "CrmProject"`、
  pptx `BaseShape.text_frame`、`PIL.Image.LANCZOS`——同上次，全是 stub 缺口，不是 bug。
- 前端 no-undef（臨時 config 同 09-15 附錄 G，放 `.health_tmp/` 跑完刪）：686 筆／18 檔（上次 611／18；多的 75 筆全在
  `js/my/cards-hr.js` 164 筆——假勤申請單／彈性外出那批用的 `esc`／`$`／`mfetch` 都是同頁 shell.js 的頂層宣告）。
  有 import/export 的 ES module 仍只有 5 檔命中：`app.js`（appendLog／getComputeBaseUrl／fetchModelStatus）、`js/app/remote-dispatch.js`
  （appendLog／getComputeBaseUrl）、`tabs/projects/agent-update.js`（appendLog）、`tabs/crm/crm-projects-cost.js:1016`（_doImportFromQuote）、
  `tabs/tts/tts.js:51`（loadDictionary）——五個名字的 `window.X =` 這次重新 grep 過都在（utils.js:1132／1134、transcribe.js:299、
  crm-projects-cost.js:1051、tts.js:702）。判定同前兩次：慣例，不是漏 import。其餘 13 檔是傳統 script（`test_no_undefined_names_in_classic_page_scripts` 守著）。

## 附錄 B：階段二明細

釘版且落後主版本（跟上次完全相同）：
- `requirements_server.txt`：aiofiles==24.1.0 → 25.1.0
- `requirements_agent.txt`：aiofiles==24.1.0 → 25.1.0；xxhash==3.6.0 → 4.0.1

pip-audit（15 套 95 條；AGENT＝在 requirements_agent.txt —— **這次一個都沒有**，上次是 pillow）：

| 套件 | 現版 | 條數 | fix | 機隊 |
|---|---|---|---|---|
| gitpython | 3.1.50 | 26 | 3.1.59 | |
| pip | 24.0 | 12 | 26.2.0 | |
| starlette | 0.52.1 | 10 | 1.3.1 | |
| setuptools | 65.5.0 | 8 | 83.0.0 | |
| pypdf | 6.14.2 | 8 | 6.16.1 | |
| pyasn1 | 0.6.3 | 6 | 0.6.4 | |
| cryptography | 48.0.1 | 6 | 50.0.0 | |
| aiohttp | 3.14.1 | 6 | 3.14.3 | |
| gradio | 6.10.0 | 5 | 6.16.0 | |
| msgpack | 1.2.0 | 2 | 1.2.1 | |
| httplib2 | 0.31.2 | 2 | 0.32.0 | |
| torch／hydra-core／datasets／accelerate | — | 1 各 | — | |

npm audit（website/，`--omit=dev`）：**0**（info／low／moderate／high／critical 全 0）。astro 7.3.2、vite 8.3.0、sharp 0.35.4（`a215d377`）。

requirements 漂移：同套件不同版本 **1**（python-multipart server 0.0.32／agent 0.0.22；上次 12，lock 檔刪掉後其餘全消）。
只出現在一份：server 11、agent 12（兩份本來就是不同機器的依賴面，不算漂移）。`0225_requirements.txt` refs=9（Install bat／ota_manifest／
publish_update 都吃），不是死檔。

## 附錄 C：階段三明細

根目錄非程式檔（T＝追蹤中；refs＝其他 py/md/bat/ps1/vbs/js/html 提到檔名的檔數）：

| 檔 | 追蹤 | 最後改 | refs | 判定 |
|---|---|---|---|---|
| title_name.txt | T | 06-13（初始 commit） | 0（只有 09-13 的 health 報告提到） | **候選刪除** |
| pyrightconfig.json | T | 06-13 | 1 | 保留（pyright 自己讀） |
| Create_Shortcut.vbs／Fix_Task.bat／Install_*.bat／update_agent.bat／dev_start.ps1／start_hidden.vbs | T | 06 | 3–19 | 保留 |
| FINISHING.md／PUBLISHING.md／TEST_INSTRUCTIONS.md／TEST_REPORT.md／Maintainer_Guide.md | T | 06–07 | 2–3 | 保留 |
| settings.json.bak_20260808_214848、uvicorn_*.log.bak、dev8001_boot.log、dev_boot.log、server_test_log.txt | – | 06–09 | 0–1 | 本機殘留，可刪 |
| Originsun_Agent.zip／*.json runtime／uvicorn_*.log／檔案處理流程.txt | – | 近期 | 有 | 已 ignore，正常 |

- `git ls-files -i -c --exclude-standard`：空。`.pyc`／`__pycache__`：0。
- 追蹤中最大 blob：exiftool_files/perl532.dll 3.3MB、Geolocation.dat 3.1MB、TagNames.pod 2.0MB、libstdc++-6.dll 1.4MB——全 < 5MB。
- scripts/ 一次性腳本 23 支（backfill_／import_／seed_／_tmp_；另 4 個 `_placeholder_backup_*.json` 已 ignore）：只有
  `seed_dev_website_data.py`（06-19）超過 60 天；零引用 16 支同上次（backfill_petty_*×2、backfill_cash_*×2、backfill_me_petty、
  import_cashbook_coop、import_payment_requests、import_welfare、import_my_equipment、import_my_snapshots、seed_my_accounts_assets、
  seed_financial_asset_account、_tmp_deploy193、seed_dev_website_data）——候選歸檔到 `scripts/archive/`（目前沒有這個目錄）。

## 附錄 D：階段四明細

- 最慢（乾淨單跑）：8.27s test_every_frontend_module_parses、4.54s test_no_undefined_names_in_es_modules、2.45s
  test_office_module_graph_has_no_schedulers_or_runners、2.23s test_no_undefined_names_in_classic_page_scripts、2.22s
  test_the_scan_actually_finds_lazy_imports、2.02s test_run_stream_kills_the_child_on_timeout。只有第一支 > 5 秒。
- skipped 0／xfail 0／xpass 0。
- 覆蓋率 35.9%（core 77.9%／83 檔、routers 22.5%／107 檔、services 21.8%／56 檔）——跟上次持平（35.9）。
- 候選（30 天改 ≥3 次且覆蓋 <40%）51 檔（同上次），前十：routers/crm/finance.py（75 次／19.6%）、routers/crm/projects.py（48／18.6）、
  routers/api_me.py（49／22.3）、routers/api_finance_projects.py（42／25.0）、routers/api_finance_stmt.py（31／12.8）、routers/crm/costs.py
  （28／17.2）、services/finance_statements.py（32／28.8）、routers/crm/quotes.py（28／18.7）、routers/crm/project_links.py（35／38.7）、
  routers/api_finance.py（25／15.2）。這批 09-13 已判定：公開純函式都釘住了、剩下全吃 DB（這次逐檔重掃 `api_me`／`projects`／`costs`／
  `quotes`／`petty`／`cash`／`finance`／`api_finance_projects` 的公開函式，未覆蓋且不碰 session 的只剩 `leave_proof_dir`／
  `get_receipts_root`／`get_quotations_root` 三支設定讀取，不值得釘）。
- 純規則層的新進模組覆蓋都很高（`core/reminder_logic` 100、`core/schedule_logic` 96、`core/leave_logic` 94.8、`core/milestone_logic` 95.1、
  `core/ledger_project` 98.4、`core/invoice_share` 96.2、`core/payout_share` 100）；沒人釘的是 service 層裡的純函式——
  - `03d34a97` tests/unit/test_leave_application_health.py（6 支，模組 11.7%、09-15 新進）：application_dict（dates／items 從 JSON 字串或
    list 讀、壞 JSON 退預設；start/end 由 dates 推；days＝hours/8；kinds 去重排序；proof_required 看 RECORD_META；cancel_mode 只在已核准
    且有日期；children=None 不帶鍵、[] 帶空清單）、day_slots（同一組時段套每一天；時段只在 range 留；bad_range 逐日進 errors；
    週末 am／all 回 0 小時不報錯）。
  - `2e9df920` tests/unit/test_timesheet_self_health.py（5 支，22.1%、30 天改 24 次）：ts_dict（UTC 16:00＝台北隔天；hours 兩位；
    planned_hours None 保留；舊列缺 start_time／stage_id 等六欄退空字串；note 只在 with_note；editable 只在給 staff_id——本人＋手填＋
    草稿 True、別人 False、手填已鎖 False、Sheet 列本人 True）、month_or_422（跨年、空＝本月、格式錯 422）、metrics_input／
    rows_by_month（台北分月、0 小時與沒日期不進）、set_stage（兩個鏡射欄一起寫、None 清空）。
  - `14d07487` tests/unit/test_timesheet_ingest_health.py（3 支，17.9%）：row_hash 逐字釘 digest（`550140…918e`；strip；None＝空字串；
    **hours 走 f-string 所以 `8` 與 `8.0` 不同**——ingest 走 `TimesheetRow.hours: Optional[float]` 所以現況永遠 float，釘住是為了防有人
    改成 int／字串後整本 Sheet 被當新列）、parse_date 三種分隔符與失敗回 None。
  三檔都以 `_health.py` 結尾，整檔可刪。全套 4068 → 4082。
- 沒挑的：`services/milestone_service.py`（14.2%）、`routers/api_calendar.py`（19.6%）、`routers/crm/project_map.py`（15.7%）的純函式全是
  `_` 開頭（`_week_of`／`_shadowed`／`_parse_day`／`_apply`／`_norm_name`），照規則不釘私有函式。`services/timesheet_manual.py` 41.1%
  差 1.1 個百分點沒進候選，但 `normalize_row`（20 行未覆蓋、CLAUDE.md 點名的「空白列 422」規則）下次值得看。

## 附錄 E：結構債（/polish --debt 規則；掃描腳本在 `.health_tmp/debt.py`，跑完刪）

### py: >2000 🔴 1 ／ 1500–2000 🟠 5 ／ 800–1500 🟡 26
- 🔴 core_engine.py 2350
- 🟠 routers/api_finance.py 1946、routers/api_proposals.py 1823、core/worker.py 1635、routers/crm/media_log.py 1610、routers/crm/finance.py 1507
- 🟡 前十：core/finance_logic/_core.py 1493、routers/crm/showcase.py 1461、routers/api_references.py 1432、routers/api_finance_stmt.py 1366、
  routers/crm/petty.py 1329、routers/crm/proposal_assets.py 1308、routers/api_ota.py 1303、routers/crm/quotes.py 1283、routers/crm/cash.py 1219、
  services/finance_statements.py 1136（上次寫 31 是含 tests/；這次排除 tests/ 是 26，同口徑沒變）

### js: >2000 🔴 0 ／ 1500–2000 🟠 5 ／ 800–1500 🟡 15
- 🟠 crm-cashbook.js 1894、finance/subviews/recon.js 1854、projects/projects.js 1698、crm-projects-cost.js 1538、crm-invoices.js 1521
  （上次 6：website/subviews/blog/editor.js 1602 這次掃描排除了 website/ 目錄名——它在 `frontend/tabs/website/`，是掃描口徑差，檔案沒變）

### css: 🟡 3（crm.css 1372、projects.css 1365、crm-project-views.css 978）

### 超長函式（py > 200 行）11 支（一支都沒變）
556 core_engine.run_concat_job、440 core_engine.run_backup_job、437 core/worker._drone_meta_sync、314 publish_update.main、
291 core_engine._run_concat_advanced、290 api_finance_stmt.apply_bank_statement、266 core/report_job._run_report_job、
263 transcriber.run_transcribe_job、233 scripts/reconcile_ledger_sheet.run、221 bootstrap.run_update、216 api_finance_stmt._build_statement_preview

### 巢狀 ≥5 層 95 支（上次 106，口徑：這次不含 tests/ 與 website/），最深：core/worker._execute_job 12 層；7 層的 12 支：
transcriber.run_transcribe_job、finance_statements.drilldown、project_links.mirror_project_to_mine、api_schedules.update_schedule、
api_proxy.verify_standalone_proxies、api_agents.list_agents、core_engine.run_transcode_job／run_concat_job、worker._run_backup、
report_job._run_report_job、core/project_folders._iter_entries、finance_logic/_statements.equity_transfer_position。

### 無測試模組 58（同口徑、同上次），最大：core/schemas_website.py 1082、services/website/rebuild_service.py 698、
translation_service.py 642、post_service.py 635、watchdog_runner.py 586、routers/api_drone_meta.py 547、notion_url_service.py 542、
post_seo_runner.py 485、backup_service.py 472、routers/website/admin_seo.py 406、routers/api_tts.py 390。多數是 website/ 的 runner 與
admin router（跑在 NAS 容器、吃網路）。

### 循環 import：0（core/services/routers/db 頂層 import 圖，長度 ≤4）。

## 附錄 F：階段六明細

- **CLAUDE.md 引用但不存在的路徑 5**：`tests/unit/test_finance_files_stay_readable.py`（**真死連結**，1104 行；現名 `test_files_stay_readable.py`）、
  `core/schemas.py`／`db/models.py`（講歷史，STALE_PATHS 有登）、其餘 2 個是刻意的縮寫（`core/bank_codes.resolve`、
  `routers/services/core/db/main_website.py/config.py`）。
- **曾是單檔、現為套件 → STALE_PATHS**：`core/finance_logic.py` ✅、`core/schemas.py` ✅、`db/models.py` ✅（三個都登了）。
- 30 天內動過、CLAUDE.md 完全沒提到的模組 107 個（含只提功能名不提檔名的）；動 ≥10 次的 10 個：上面那五個＋
  `routers/api_finance_card.py`（15）、`routers/crm/cash_splits.py`（12）、`routers/api_journal.py`（11）、`routers/crm/benefits.py`（10）、
  `core/bank_statement.py`（10）——後五個在「模組職責」以功能名登記過（信用卡對帳／收支拆項／週記／福委會／對帳單），不算漏。
- ROADMAP 未勾 134 項（上次 137，owner 勾了 3），關鍵字對得上 13，人工看過真的做完的 3 項（「沒動的東西」第 6 條）；
  L765「user 加 workspace 欄」、L768「現場人員手機工作台」、L744「通告單」只是相關、沒做完。
- CHANGELOG：「自動生成：v1.10.1 → v2.5.35」跟 version.json 2.5.35 **同步**。
- docs/*_PLAN.md 超過 90 天沒動：0。
- CLAUDE.md「不要動的地方」：這次沒有新地雷（row_hash 的型別敏感已用測試釘住、且被 Pydantic 擋著，不夠格進去），未動。

---

## 追記 2026-09-16（owner「都處理」後的下落）

| # | 報告裡的項目 | 這次的處置 | commit |
|---|---|---|---|
| 1 | xxhash 3.6.0 落後主版本（怕 digest 不相容） | **升 4.0.1**。先在隔離 venv 對 6 組資料（空／1 byte／1MB+ 分塊／3MB 亂數）跑同一套分塊 xxh64，digest 與舊版逐字相同 → 備份的比對驗證不受影響 | `b381dd02` |
| 1 | aiofiles 24.1.0 落後 | **升 25.1.0**（server＋agent）。我們的程式碼一行都沒 import 它，NAS 兩個容器早就跑 25.1.0 | `b381dd02` |
| 2 | pip-audit 15 套 95 條 | **審計方法先修掉**：原本掃整個 `.venv`（含 gradio／torch 這種只在本機的東西），改成只掃真的出貨的兩份清單。真正屬於我們的是 4 條：pypdf 6.14.2→**6.16.1**（8 條 CVE）、python-socketio 5.16.1→**5.16.2**、requests 2.32.5→**2.33.0**（後兩者在機隊，所以原報告「機隊中 CVE：0」不準）；torch 2.12.0 留著（見下） | `b381dd02` |
| 3 | python-multipart 兩份清單版本不同 | **對齊 0.0.32**；dev 8001 真打一次 multipart 上傳（假勤證明）回 200 | `b381dd02` |
| 4 | `title_name.txt` 候選刪除 ＋ 5 個本機殘留 | **刪掉**。settings 那份備份比對過：沒有獨有的鍵、只是舊副本，而且裡面那把 jwt_secret 已輪替 | `c173c0d7` |
| 5 | CLAUDE.md 死連結 | **修好**（`test_finance_files_stay_readable.py` → `test_files_stay_readable.py`） | `c173c0d7` |
| 6 | ROADMAP 可勾 3 項 | **勾了**，各附落地處 | `c173c0d7` |
| 7 | 模組職責表缺 5 個（連兩次上榜） | **補了 4 列**（對帳單那組兩支併一列）：finance_statements＋api_finance_stmt、api_crm_mobile、timesheet_manual、timesheet_lookup | `c173c0d7` |
| 8 | `core_engine.py` 2350 行、27% 覆蓋 | **沒動程式碼，先補網**：16 支特徵測試（半成品約定、AtomicOutput 的四種路徑、分塊 xxh64）。2350 行的拆檔要自己一個分支、一次一塊 —— 它跑在十台機器的備份與轉檔上，現在的覆蓋率接不住一次大改 | `ecc7c92f` |
| 9 | 本機殘留 5 個 | 同 #4，刪了 | `c173c0d7` |
| 10 | `test_every_frontend_module_parses` 8.27 秒 | 留著（刻意全掃） | — |

**還沒動的，只剩一件**：`torch 2.12.0`（PYSEC-2025-194，修版 2.13.0）。torch 是 f5-tts 帶進來的，換版要連模型權重與 wheel（CPU／CUDA）一起驗，
而且只跑在主控，不在機隊 —— 自成一件事，等你要動語音那塊時一起做。

驗證：4098 全套綠、ruff／eslint 零、preflight OK；dev 8001 上 multipart 上傳、socket.io handshake、pypdf 抽字都實打過。
機隊那份清單這次有動（xxhash／socketio／requests／multipart／aiofiles），但十台目前都還停在 2.5.30，
要先跑過 `O:\修復Agent更新_請每台雙擊一次.bat` 才會拿到。
