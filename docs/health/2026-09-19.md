# health 報告 2026-09-19

> 上次：2026-09-18（v2.5.48 基準）。中間進了 v2.5.49～2.5.57（知識庫第二批：研究助理／週報／公開分享／PDF／圖表抽取＋兩輪 /polish）。
> 六個階段都重跑；數字跟 09-18 那份比。工具：ruff／pyright／pytest-cov／pip-audit 四個 venv 裡都有，本次沒裝東西。
> 基準那一輪是跟 ruff／eslint／preflight 同時跑的（CPU 搶），所以最慢測試另外單獨量了一次。

```
health 報告 2026-09-19                                              上次：2026-09-18

基準測試：通過（4642 支，77.5 秒，同時在跑 lint）                   上次：4331 / 50.4
階段一 靜態閘門：ruff 0 ／ eslint 0 ／ pyright 4862 errors（0 reportUndefinedVariable；上次 4857）／ 前端漏 import 0（no-undef 730 筆／19 檔，跟上次一模一樣）／ preflight OK ／ 寫卡 0 張：修 0、回報 0、需人工 0
階段二 依賴：釘版落後主版本 0 ／ CVE 16 套 94 條（直接釘在機隊的 0；**經 fastapi／google-auth 傳遞到機隊的 4 套**，上次沒算這層）／ npm high+ 0 ／ requirements 漂移 0（只出現在一份裡 7）
階段三 衛生：候選刪除 0 ／ 候選 gitignore 0 ／ 大檔 0（最大 3.3MB exiftool）／ 一次性腳本 23＋4 個未追蹤 placeholder json（1 支超過 60 天）／ pyc 進 git 0
階段四 測試：最慢 9.0 秒單獨量（test_every_frontend_module_parses；上次 12.9 搶 CPU）／ skipped 0 ／ xpass 0 ／ 覆蓋率 39.4%（上次 38.2；core 80.6／routers 23.3／services 29.0）／ 補了 3 個模組 15 支測試
階段五 結構債：>2000 行 1（core_engine.py 2350）／ 800–2000 行 65（上次 64）／ >200 行函式 11（py；上次 10）＋ 17（js 啟發式；上次 14）／ 巢狀 ≥5 層 108（上次 100）／ 循環 import 0
階段六 文件：模組表缺 0（知識庫四支已登記；website 子套件 39 支從沒逐支登記，屬第 0 節／規則 G）／ ROADMAP 可勾 0（61 個關鍵字命中全是泛詞）／ CHANGELOG 落後：是（最新 2.5.44，version 2.5.57，落後 13 版）／ 過期 PLAN 0
commits：7fc94a3f health: baseline、e6885d59 health: tests for routers/api_references、c83c2dd9 health: tests for services/website/project_service、93ef5526 health: tests for core/drone_watcher.check_and_fire、health: report（本檔）
停止原因：正常結束（主流程 commit 4／8，卡片 0／10）
```

## 上次的「沒動的東西」，這次的下落

| # | 上次的項目 | 這次 |
|---|---|---|
| 1 | `routers/api_finance.py` 1956 行 | **1956**，沒動 |
| 2 | core_engine.py 2350、`run_concat_job` 556 | 未動 |
| 3 | `core/worker.py::_drone_meta_sync` 437 | 未動 |
| 4 | 知識庫四支模組未登記模組表 | **已登記**（09-18／09-19 兩行「知識庫」「知識庫第二批」） |
| 5 | pip-audit 14 套 90 條、機隊 0 | 16 套 **94** 條（多 pyasn1、soupsieve）；直接釘的仍 0，但見下面「新看到的 1」 |
| 6 | `scripts/seed_dev_website_data.py` 06-19 | 同；仍是唯一過 60 天線的 |
| 7 | CHANGELOG 2.5.44 vs 2.5.48 | 仍 2.5.44，version 已 **2.5.57**（落後從 4 版變 13 版） |
| 8 | `showcase-edit/events.js::_bindEvents` 746、`reference-page.js::renderReference` 589 | `_bindEvents` 仍 746；`renderReference` 這次的啟發式沒抓到（應是函式收尾的縮排變了），未動 |

## 這次新看到的

1. **pip-audit 的「機隊 0」只看直接釘版，漏掉傳遞相依**。`requirements_agent.txt` 釘了 `fastapi==0.134.0`／`google-auth==2.48.0`／`google-api-python-client==2.190.0`，它們拉進來的 `starlette`（10 條、dev venv 0.52.1、修在 ≥1.0.1）、`cryptography`（6 條、48.0.1）、`pyasn1`（6 條）、`httplib2`（2 條）在每一台機器上都會裝——**版本由各機器 pip 解析當時決定，不等於 dev venv 的版本**。09-17／09-18 兩份報告的「機隊中 0」是這個量法的假陰性。要不要處理是升級 fastapi 主版本的事（starlette 1.x 要 fastapi ≥0.135），不在 health 權限內。
2. **前端 no-undef 完全沒變**（730 筆／19 檔，跟 09-18 逐筆一致）：知識庫第二批新增的 `frontend/js/knowledge/{extend,report,info,gallery}.js` 都是 ES module、import 齊全。19 檔仍全是 `js/my/`、`js/showcase-edit/` 那些傳統 script ＋ ES module 裡裸用 `window.X` 慣例名（`appendLog`／`getComputeBaseUrl`／`_doImportFromQuote`／`loadDictionary`），每一個都在同檔或 utils.js 有 `window.X =`。
3. **pyright +5**（4862）：`core/knowledge_logic.py`／`services/knowledge_*.py` 這批幾乎沒貢獻；主要仍是 SQLAlchemy `select()` 與 cryptography 聯集型別的 stub 噪音（`core/ota_sign.py` 一行 `sign()` 對五種金鑰型別各報一次）。`reportUndefinedVariable` 0。
4. **覆蓋率 +1.2**（39.4%）：純函式那層（core 80.6%）已經很高，routers 23.3／services 29.0 拉不動是因為低的那些是端點本體（要 DB）。這次候選前 14 名（`routers/crm/finance.py` 19.6%、`routers/crm/projects.py` 18.6%、`routers/api_me.py` 22.7%…）裡**每一支公開純函式都已經有測試**（前幾次 health 挑過），所以往下掃到第 15 名以後才找到三個沒測的：`routers/api_references.py`（`ref_dict`／`apply_ref_patch`／`shot_dict`）、`services/website/project_service.py`（`append_old_slug_if_changed*`／`mirror_main_work_to_project`／`work_completeness_dict`）、`core/drone_watcher.py::check_and_fire`。下次 health 若再想拉覆蓋率，剩下的只有端點本體，得用 mock session，價值低——建議停在這裡。
5. **結構債微升**：800–2000 行 +1（`core/knowledge_logic.py` 1320、`services/knowledge_service.py` 1046 進榜；`tests/unit/test_knowledge_base.py` 1019）；>200 行 py 函式 +1（`routers/api_finance_stmt.py::_build_statement_preview` 216）；巢狀 ≥5 層 +8。都是知識庫與對帳單這兩條線長出來的，沒有一支破 2000。
6. **CHANGELOG 落後 13 版**：09-13 那次 /health 從 notes 生成到 2.5.44 之後就沒再接，publish 流程本身不寫它。要嘛在 publish_update.py 加一行 append、要嘛下次 /health 再補生成一段——這次沒動（不在白名單）。
7. **模組職責表的「缺」這次算法改了**：09-18 是「表裡沒這支」，本次改成「整份 CLAUDE.md 哪裡都沒提」——結果 39 支全是 `routers/website/admin_*.py`（15）與 `services/website/*.py`（24），2026-05～07 官網 Phase M 的東西，第 0 節與規則 G 用目錄名整批指到它們，表本身的定義是「/polish 逐次累積」不是全目錄。**不當作缺**。30 天內動過的只有 `post_seo_runner`／`project_service`／`translation_service` 三支（都是舊模組的小改）。

## 沒動的東西（依影響排序，一行一條）

1. `routers/api_finance.py` 1956 行 — 離 `test_files_stay_readable` 上限 44 行；已明訂新端點另開檔
2. 機隊經 fastapi 帶進 starlette 0.52.x（10 條 CVE，修在 1.x）— 升 fastapi 主版本才解得掉；走釘版＋兩跳金絲雀，不是 health 的事
3. `core_engine.py` 2350 行、`run_concat_job` 556／`run_backup_job` 440；`core/worker.py::_execute_job` 巢狀 12 層 — 老債
4. CHANGELOG 停在 2.5.44（version 2.5.57）— publish 流程不寫它；需要決定「publish 順手寫」還是「/health 定期補」
5. pip-audit 16 套 94 條 — master 端重型套件（gitpython 29、pip 12、setuptools 8、gradio 5、torch 1…）；`pip 24.0`／`setuptools 65.5.0` 是 venv 自帶的舊版，`python -m pip install -U pip setuptools` 就解 20 條但那是 venv 不是 repo
6. `core/worker.py::_drone_meta_sync` 437 行 — 空拍同步一支函式
7. `frontend/js/showcase-edit/events.js::_bindEvents` 746 行、`render.js::_render` 412、`js/shared/cash-split-editor.js::openCashSplitEditor` 406 — JS 超長函式前三
8. `scripts/seed_dev_website_data.py` 06-19 — 92 天沒動、沒人引用；候選歸檔 scripts/archive/
9. 四個未追蹤的 `scripts/_placeholder_backup_*.json`（08-18）— 本機殘留、git 沒追蹤；候選手動刪
10. 根目錄 `uvicorn_out.log`／`uvicorn_err.log`／`update_status.json`／`watcher_history.json`（08-12～08-28）— 都被 ignore、都是 runtime 產物；不影響 repo，只是本機占位

未執行：
- 無（六個階段全跑；沒裝新工具）

---

## 附錄 A：階段一明細

- ruff：`ruff check .` → All checks passed。
- eslint：`npm run lint` → 0。
- preflight：OK（20 imports verified，21 個 master 專用套件跳過；`fitz` 印了一行 pymupdf 的 deprecation warning，不是錯）。
- pyright（範圍 core／routers／services／db／main.py／main_office.py，309 檔，35.5 秒）：**4862** errors（上次 4857）。
  全 repo 含 tests 是 6379（769 檔）——不同範圍，不拿來比。
  規則分布：reportPossiblyUnboundVariable 1938、reportCallIssue 1420、reportArgumentType 609、reportAttributeAccessIssue 516、reportGeneralTypeIssues 232、reportOptionalMemberAccess 82、其餘 65。
  檔案前十：routers/crm/finance.py 236、routers/crm/projects.py 215、routers/crm/quotes.py 210、routers/crm/staff.py 176、routers/api_finance.py 175、routers/crm/cash.py 175、routers/crm/costs.py 165、routers/crm/benefits.py 156、routers/crm/showcase.py 153、routers/api_shoots.py 151。
  reportUndefinedVariable：**0**。
  reportAttributeAccessIssue 前 20（全是 stub）：`core/doc_text.py:59-62`（python-pptx `BaseShape.text_frame`）、`core/image_utils.py:104,135`（`PIL.Image.LANCZOS`）、`core/ota_sign.py:42,55`（cryptography 私鑰／公鑰聯集型別對 `sign`／`verify` 各報 5 次）、`db/repos/*_repo.py`（`Result.rowcount`）。
  reportCallIssue 前 20：同上 `ota_sign.py:42,55`（padding／algorithm 參數）、`core/payroll_logic.py:146`（dict `__setitem__` overload）、`db/repos/bookmarks_repo.py:22`（ORM kwargs）。
- 前端 no-undef（臨時 flat config，`frontend/**/*.js`、browser globals、`no-undef: error`）：730 筆／89 個名字／19 檔。
  分類：傳統 script 頁面級全域（`js/my/` 七支＋`js/showcase-edit/` 六支；`$`、`esc`、`mfetch`、`mjson`、`DATA`、`EDITABLE`、`_esc`、`_toast`…）＝ 616 筆；ES module 裡裸用 `window.X` 慣例名 114 筆（`appendLog` 97、`getComputeBaseUrl` 10、`money` 9、`fetchModelStatus`、`_doImportFromQuote`、`loadDictionary`…），每一個都在同檔或 `js/shared/utils.js` 有 `window.X =` 賦值。
  **可自動補 import 的（唯一來源且沒有 window 賦值）：0。**

## 附錄 B：階段二明細

- `pip list --outdated`：venv 裡 104 套有新版；對照兩份 requirements「有釘且落後主版本」：**0**。
- pip-audit（venv 全量）：16 套 94 條。
  | 套件 | dev venv 版 | 條數 | 修在 | 機隊 |
  |---|---|---|---|---|
  | gitpython | 3.1.50 | 29 | 3.1.51+ | 否 |
  | pip | 24.0 | 12 | 25.3+ | 否（venv 自帶） |
  | starlette | 0.52.1 | 10 | 1.0.1+ | **經 fastapi 傳遞** |
  | setuptools | 65.5.0 | 8 | 65.5.1+ | 否（venv 自帶） |
  | aiohttp | 3.14.1 | 6 | 3.14.2+ | 經 edge-tts／discord.py（master 專用） |
  | cryptography | 48.0.1 | 6 | 49.0.0+ | **經 google-auth 傳遞** |
  | pyasn1 | 0.6.3 | 6 | 0.6.4 | **經 google-auth 傳遞** |
  | gradio | 6.10.0 | 5 | 6.15.0+ | 否 |
  | anyio | 4.13.0 | 2 | 4.14.2 | 經 fastapi／httpx 傳遞 |
  | httplib2 | 0.31.2 | 2 | 0.32.0 | **經 google-api-python-client 傳遞** |
  | msgpack | 1.2.0 | 2 | 1.2.1 | 否 |
  | soupsieve | 2.8.4 | 2 | 2.9.0 | 經 beautifulsoup4 |
  | accelerate／datasets／hydra-core／torch | — | 各 1 | — | 否 |
- website `npm audit --omit=dev`：info 0／low 0／moderate 0／high 0／critical 0。
- requirements 漂移（四份）：同一套件不同版本 **0**；只出現在一份裡 7：`requirements_server.txt` 獨有 asyncpg／openpyxl／pypdf／python-docx／python-pptx／SQLAlchemy（都是 master 專用、合理）；`0225_requirements.txt` 獨有 `torchaudio`（無釘版）。09-18 那份算出 23 是把 `requirements.txt` 裡的也算進去、比對方式不同；本次以「套件名只在一份出現」計。

## 附錄 C：階段三明細

根目錄非程式檔（T=追蹤 U=未追蹤；refs=其他 .py/.md/.bat/.ps1/.vbs 提到檔名的次數）：

| 狀態 | 最後修改 | refs | 檔案 | 判定 |
|---|---|---|---|---|
| T | 06-13 | 7 | Create_Shortcut.vbs | 保留（安裝流程） |
| T | 06-13 | 8 | Fix_Task.bat | 保留（OTA 排程修復） |
| T | 06-14 | 7 | Install_Originsun_Agent.bat | 保留 |
| T | 06-14 | 5 | Install_or_Update.bat | 保留 |
| U | 09-19 | 15 | Originsun_Agent.zip | 已 ignore（發版產物） |
| U | 09-09 | 1 | api_keys.json | 已 ignore（runtime） |
| U | 06-13 | 2 | bookmarks.json | 已 ignore（runtime） |
| T | 06-14 | 8 | dev_start.ps1 | 保留 |
| U | 08-25 | 4 | job_history.json | 已 ignore（runtime） |
| T | 07-06 | 2／4 | package-lock.json／package.json | 保留（eslint） |
| U | 09-19 | 2 | publish_history.json | 已 ignore |
| T | 06-13 | 4 | pyrightconfig.json | 保留 |
| U | 09-18 | 88 | settings.json | 已 ignore |
| T | 06-13 | 23 | start_hidden.vbs | 保留 |
| T | 06-13 | 11 | taiwan_dict.json | 保留（熱更新字典） |
| T | 06-14 | 14 | update_agent.bat | 保留 |
| T | 09-19 | 9 | update_manifest.json | 保留（發版維護） |
| U | 08-28 | 7 | update_status.json | 已 ignore（runtime） |
| U | 09-18 | 11 | users.json | 已 ignore（DB 鏡射） |
| U | 08-28 | 8 | uvicorn_out.log／uvicorn_err.log | 已 ignore（runtime） |
| T | 09-19 | 30 | version.json | 保留 |
| U | 08-12 | 1 | watcher_history.json | 已 ignore（runtime） |

- `git ls-files -i -c --exclude-standard`：空。
- `__pycache__`／`.pyc` 進 git：0。
- 追蹤中最大 blob：exiftool_files/perl532.dll 3.3MB、Geolocation.dat 3.1MB、TagNames.pod 2.0MB、libstdc++-6.dll 1.4MB…；`frontend/div_report.html` 0.6MB。無 >5MB。
- `scripts/` 一次性腳本 23 支追蹤（06-19 seed_dev_website_data.py；08-17～09-03 其餘 22 支）＋4 個未追蹤 `_placeholder_backup_*.json`（08-18）。超過 60 天：1（seed_dev_website_data.py，92 天）。

## 附錄 D：階段四明細

- 基準（同時在跑 lint）：4642 passed，77.5 秒；補完測試後：**4657 passed，50.1 秒**。
- 最慢 15（基準那輪，搶 CPU）：test_every_frontend_module_parses 27.8、test_no_undefined_names_in_es_modules 4.4、test_the_scan_actually_finds_lazy_imports 2.6、test_office_module_graph_has_no_schedulers_or_runners 2.5、test_every_writer_routes_classification_through_one_rule 2.5、test_no_new_file_crosses_the_read_limit 2.4、test_no_undefined_names_in_classic_page_scripts 2.3、test_run_stream_kills_the_child_on_timeout 2.0、test_the_exemption_list_has_no_dead_names 1.9、test_the_repo_scan_yields_no_non_ascii_package_names 1.6、其餘 <1.1。
  單獨重量 `test_js_parses.py`：8.97／4.44／2.28 秒——**沒有 >5 秒的紅字以外的新問題**；`test_every_frontend_module_parses` 一直是 9 秒級（node 逐檔 parse），上次 12.9 是搶 CPU 量到的。
- skipped 0／xfail 0／xpass 0。
- 覆蓋率（`--cov=core --cov=routers --cov=services`）：整體 **39.4%**（19,476／49,491 行）；core 80.6%（90 檔）、routers 23.3%（114 檔）、services 29.0%（62 檔）。
- 候選（30 天改 ≥3 次 × 覆蓋率 <40% × ≥30 句）：52 支。前 10（score＝改動次數×(1−覆蓋率)）：
  routers/crm/finance.py 49.9（62 次／19.6%）、routers/crm/projects.py 38.3（47／18.6%）、routers/api_me.py 37.1（48／22.7%）、routers/api_finance_projects.py 31.5（42／25.0%）、routers/api_finance_stmt.py 28.7（33／12.9%）、services/finance_statements.py 22.8（32／28.8%）、routers/crm/quotes.py 22.8（28／18.7%）、routers/crm/project_links.py 21.4（35／38.7%）、routers/api_finance.py 21.2（25／15.0%）、routers/crm/cash.py 20.2（25／19.4%）。
  這十支的公開純函式（`collection_fields`、`is_mirrored`、`normalize_backup_roots`、`resync_receivable`、`explode_cash_splits`、`ts_dict`／`month_or_422`／`metrics_input`／`rows_by_month`／`set_stage`、`quotation_sent_transition`、`validate_root_dir`）**全部已有直接測試**；沒測的都是端點本體。
- 實際補的 3 個模組（各 5 支，全部一次通過、一模組一 commit）：
  - `tests/unit/test_api_references_health.py`：`ref_dict` 從 URL 推導 provider／video_id／thumb 且存了就不推導、`research=False` 省掉整份 JSONB；`apply_ref_patch` 公開連結只准 title／note／description／research（facet、url 一律 403）、單選族收字串／多選族拒非 list、url 非 http 422 並同步 video meta、研究格 404／409 樂觀鎖／寫入帶 user；`shot_dict` 空值預設。
  - `tests/unit/test_project_service_slug_mirror_health.py`：`append_old_slug_if_changed` 只在真變時追加一次、空／純數字 slug 不加、改回舊 slug 時把它從舊清單移除（防 redirect 自循環）；`mirror_main_work_to_project` 全量鏡射 `WORK_WIRE_FIELD_MAP` 且 `public_number` 只在空時補；`work_completeness_dict` 讀 showcase 形狀。
  - `tests/unit/test_drone_watcher_fire_health.py`：`check_and_fire` 關閉／不是那一分鐘不跑、同一天只跑一次、重啟後靠 history 前 5 筆補防（第 6 筆看不到）、掃描失敗寫 history 且**不鎖今天**（下一分鐘重試）。

## 附錄 E：階段五明細（/polish --debt 等價掃描）

- ≥2000 行（紅）：`core_engine.py` 2350。
- 800–1999 行 65 支，前 20：routers/api_finance.py 1956、frontend/project.html 1956、frontend/tabs/crm/crm-cashbook.js 1894、frontend/tabs/finance/subviews/recon.js 1854、routers/api_proposals.py 1823、frontend/tabs/projects/projects.js 1698、core/worker.py 1635、routers/crm/media_log.py 1610、frontend/tabs/crm/crm-projects-cost.js 1538、core/finance_logic/_core.py 1535、frontend/tabs/crm/crm-invoices.js 1521、routers/crm/finance.py 1507、routers/crm/showcase.py 1461、frontend/tabs/crm/crm-quotes.js 1458、routers/api_references.py 1432、frontend/tabs/crm/crm.css 1372、frontend/tabs/projects/projects.css 1365、routers/api_finance_stmt.py 1353、routers/crm/petty.py 1329、routers/api_ota.py 1325。
- >200 行 Python 函式 11：core_engine.py::run_concat_job 556、::run_backup_job 440、core/worker.py::_drone_meta_sync 437、publish_update.py::main 314、core_engine.py::_run_concat_advanced 291、routers/api_finance_stmt.py::apply_bank_statement 290、core/report_job.py::_run_report_job 266、transcriber.py::run_transcribe_job 263、scripts/reconcile_ledger_sheet.py::run 233、bootstrap.py::run_update 221、routers/api_finance_stmt.py::_build_statement_preview 216。
- >200 行 JS 函式（啟發式）17，前 8：showcase-edit/events.js::_bindEvents 746、showcase-edit/render.js::_render 412、js/shared/cash-split-editor.js::openCashSplitEditor 406、app.js::setupSocket 374、crm-projects-cost.js::_renderCostLines 362、js/app/remote-dispatch.js::mergeHostOutputs 344、petty/petty-view.js::renderOverview 336、crm-projects.js::initCrmProjectsTab 322。
- 巢狀 ≥5 層 108 支；最深：core/worker.py::_execute_job 12、transcriber.py::run_transcribe_job 7、services/finance_statements.py::drilldown 7、routers/crm/project_links.py::mirror_project_to_mine 7、routers/api_schedules.py::update_schedule 7。
- 無任何測試檔提到的模組（core／routers／services，啟發式）66 支，前 10：core/schemas_website.py 1082、routers/api_hr.py 764、core/finance_logic/_statements.py 715、routers/api_drone_meta.py 547、routers/api_agents.py 537、core/finance_logic/_flows.py 534、core/schemas/_workos.py 491、routers/crm/cost_lines.py 485、routers/api_cashflow.py 424、routers/api_finance_card.py 410。
- 模組層循環 import（core／routers／services／db）：0。

## 附錄 F：階段六明細

- 模組職責表 vs 程式碼：表裡有、檔案沒了 **0**（brace 縮寫 `crm-projects-{detail,core}.js` 已展開比對）。整份 CLAUDE.md 完全沒提到的 39 支＝`routers/website/admin_{backup,categories,credits,initiatives,inquiries,posts,rebuild,redirects,services,settings,social,team,translation,watchdog,works}.py`（15）＋`services/website/{_crud_base,_runner_util,backup_service,category_service,credit_service,initiative_service,inquiry_service,nav_service,notify_service,notion_service,notion_url_service,post_seo_runner,post_service,project_service,rebuild_service,redirect_service,seo_runner,series_service,service_service,settings_service,social_runner,translation_service,video_utils,watchdog_runner}.py`（24）。第 0 節與規則 G 以目錄整批指到；不算缺。
- `ota_manifest.STALE_PATHS` 3 條，本機都不存在（拆檔清乾淨了）。
- ROADMAP 未勾 131 項；30 天 commit 關鍵字命中 61，抽查全是泛詞（「Notion」對到假勤規章那筆、「Agent」對到 deploy 那筆、「儀表板」對到 polish 那筆）；**可勾 0**。
- CHANGELOG：自動生成段到 **2.5.44**（2026-09-17），`version.json` **2.5.57**。
- `docs/*_PLAN.md` 超過 90 天沒動：0。
- CLAUDE.md「不要動的地方」：這次沒有新地雷（漏 import 0、xpass 0），**沒動**。
