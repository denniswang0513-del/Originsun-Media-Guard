# health 報告 2026-09-15

> 上次：2026-09-13（第二次）。中間進了行事曆一期＋二期（`fd3cf0df`…`b1ca156f`）、假勤寬頁／大家的休假／病假證明
> （`6417d75d`…`217db030`）、drive_map 的 O: 與 map_drives.bat、專案清單「帳本」欄，發到 v2.5.23。
> 這份是完整跑（六個階段都重算，不沿用快取）。

```
health 報告 2026-09-15                                              上次：2026-09-13-2

基準測試：通過（4010 支，70 秒*）                                    上次：3953 / 42
階段一 靜態閘門：ruff 0 ／ eslint 0 ／ pyright 5853 errors（0 reportUndefinedVariable）／ 前端漏 import 0 ／ preflight OK ／ 寫卡 0 張
階段二 依賴：釘版落後主版本 3 條（2 套）／ CVE 16 套 120 條（機隊中：pillow）／ npm high+ 10（critical 1：astro）／ requirements 漂移 12 項
階段三 衛生：候選刪除 5（＋本機殘留 4，不在 git）／ 候選 gitignore 0 ／ 大檔 0（最大 3.3MB）／ 一次性腳本 23（>60 天 1）
階段四 測試：最慢 8.7 秒（test_every_frontend_module_parses）／ skipped 0 ／ xpass 0 ／ 覆蓋率 35.9%（core 77.6、routers 22.6、services 21.6）／ 補了 3 個模組 15 支測試
階段五 結構債：>2000 行 1（core_engine.py 2350）／ 1500–2000 行 py 5 + js 6 ／ >200 行函式 11 ／ 巢狀 ≥5 層 106 ／ 循環 import 0
階段六 文件：模組表缺 5（30 天內動過且 CLAUDE.md 完全沒提）／ ROADMAP 可勾 2 ／ CHANGELOG 同步（前兩份的「落後」是 regex 假陽性）／ 過期 PLAN 0 ／ STALE_PATHS 漏登 2
commits：741c1742（calendar_sync tests）、1c18a3ee（google_calendar tests）、e0f534d3（leave_service tests）、本檔
停止原因：正常結束（主流程 commit 4／8，卡片 0／10）
```

\* 基準那次跟 eslint／ruff 同時在跑，被拖慢；之後乾淨的全套是 42.6 秒（4025 支）、最慢一支單獨量 8.66 秒——跟上次一致。

## 沒動的東西（依影響排序，一行一條；想追再看附錄）

1. **`ota_manifest.STALE_PATHS` 漏了兩個早於那條規則的拆檔**：`db/models.py`（`6c568eb7` 拆成 `db/models/`）、`core/finance_logic.py`
   （`38044921` 拆成 `core/finance_logic/`）。master 與 `C:\OriginsunAgent` 上都已不存在，但機隊 OTA 是「覆蓋、不刪」——2.4.2xx 之前就裝的
   機器上兩份並存（Python 先找到套件，功能不壞，下一個去那台看碼的人會改到死檔）。加進清單＝下一次 OTA 會刪機隊上的檔，不在 health 權限內，
   owner 拍板後在發版前一輪加。
2. **npm audit critical：`astro <=7.2.7`**（website/ 建置鏈，`fixAvailable`）＋ high 9（vite／postcss／sharp／nanoid／js-yaml／svgo／
   devalue／fast-uri／smol-toml）。只影響 NAS 上 build 那一步，不是對外執行環境；升 astro 主版要重跑 seo-audit 與整站 build，另開分支做。
3. **pillow 12.2.0 有 25 條 CVE（fix 12.3.0），在 `requirements_agent.txt`＝機隊也中**（跟上次相同）。patch 版升級、但走
   `requirements_agent.txt` 就是全機隊，照 CLAUDE.md 規則不在 health 權限內。
4. **`requirements_agent.txt` 有 10 套比 `requirements_lock_20260614.txt` 舊**（fastapi／pydantic／uvicorn／requests／google-*…）＋
   `xxhash==3.6.0` 落後主版本（4.0.1）——lock 檔已 3 個月沒人引用，判斷：**可刪**（`0225_requirements.txt` 同）。
5. 根目錄 5 個追蹤中、零引用、2026-06 以來沒動的檔：`Create_Agent_Shortcut.vbs`／`Install_or_Update_DEBUG.bat`／
   `create_install_shortcut.ps1`／`setup_env.bat`／`start_server.vbs`——候選刪除（跟 09-13 那 7 個重疊）。
6. 本機殘留（已 ignore，不在 git）：`settings.json.bak_20260808_214848`、`uvicorn_err.log.bak`、`uvicorn_out.log.bak`、`dev8001_boot.log`——可直接刪。
7. CLAUDE.md 模組職責表沒登記、但 30 天內動過 ≥10 次的模組：`services/finance_statements.py`（32）、`routers/api_finance_stmt.py`（31）、
   `routers/api_crm_mobile.py`（22）、`services/timesheet_manual.py`（20）、`services/timesheet_lookup.py`（14）。下次 /polish 的
   maintainability 階段順手補。
8. ROADMAP 對得上、可勾的 2 項：「排班日曆：專案 × 日期 × 人員 × 職務（J-4）」← `fd3cf0df` 行事曆工作登記；
   「Google Sheet 歷史匯入：幾千列 → timesheets」← 09-03 `import_timesheets.py`（生產 9,8xx 列已進）。不勾、owner 決定。
9. `core_engine.py` 2350 行仍是唯一紅檔（`run_concat_job` 556、`run_backup_job` 440、`_run_concat_advanced` 291）；
   `core/worker.py::_execute_job` 巢狀 12 層。拆法同 09-13 那份的建議，未動。
10. `test_every_frontend_module_parses` 8.7 秒（> 5 秒紅）——整個 frontend 逐檔丟 acorn，跟上次一樣；要快只能改成只掃 git diff 的檔，
    但它是「所有 js 都 parse 得過」的守衛，故意全掃，留著。

## 未執行

- 無。六個階段全部跑完；工具都在 `.venv`（ruff 0.15.20／pyright 1.1.414／pytest-cov 7.1.0／pip-audit 2.10.1），沒裝新東西。

---

## 附錄 A：階段一明細

- `ruff check .`：All checks passed。`npm run lint`：0。`preflight.py`：OK（19 imports verified、21 非 agent 依賴跳過）。
- pyright 5853 errors／289 檔（上次 5752）。前六名：reportPossiblyUnboundVariable 1879、reportCallIssue 1706、reportArgumentType 1122、
  reportAttributeAccessIssue 569、reportGeneralTypeIssues 241、reportOptionalSubscript 108。`reportUndefinedVariable` **0**。
  reportCallIssue 抽樣（api_api_keys／api_bulletin）全是 SQLAlchemy model 建構子 `No parameter named "id"`；reportAttributeAccessIssue
  抽樣是 `Cannot assign to attribute "x" for class "Model"`（同一來源，declarative 沒有型別 stub）＋ pptx `BaseShape.text_frame`、
  `PIL.Image.LANCZOS`、`Result.rowcount`——全部是 stub 缺口，不是 bug。
- 前端 no-undef（臨時 config 見附錄 G，放 repo 根目錄下任一子資料夾才解析得到 node_modules；跑完刪掉，全 frontend 當 module 掃）：611 筆（上次 613）。有 import/export 的
  ES module 只有 5 檔命中：`app.js`（appendLog／getComputeBaseUrl／fetchModelStatus）、`js/app/remote-dispatch.js`（appendLog／
  getComputeBaseUrl）、`tabs/projects/agent-update.js`（appendLog）、`tabs/crm/crm-projects-cost.js:1016`（_doImportFromQuote）、
  `tabs/tts/tts.js:51`（loadDictionary）——五個名字都有 `window.X =`（utils.js:1132／1134、transcribe.js:299、
  crm-projects-cost.js:1051、tts.js:702），而且呼叫都在函式內、utils.js 在 app.js 第 22 行就 import 了。跟 09-13 判定相同：慣例，不是漏 import。
  其餘 13 檔是 `js/my/`、`js/showcase-edit/`、`js/update/` 的傳統 script（共用全域，`test_no_undefined_names_in_classic_page_scripts` 守著）。

## 附錄 B：階段二明細

釘版且落後主版本（server／agent）：
- `requirements_server.txt`：aiofiles==24.1.0 → 25.1.0
- `requirements_agent.txt`：aiofiles==24.1.0 → 25.1.0；xxhash==3.6.0 → 4.0.1

pip-audit（16 套 120 條；AGENT＝在 requirements_agent.txt）：

| 套件 | 現版 | 條數 | fix | 機隊 |
|---|---|---|---|---|
| pillow | 12.2.0 | 25 | 12.3.0 | **AGENT** |
| gitpython | 3.1.50 | 26 | 3.1.59 | |
| pip | 24.0 | 12 | 26.2.0 | |
| starlette | 0.52.1 | 10 | 1.3.1 | |
| pypdf | 6.14.2 | 8 | 6.16.1 | |
| setuptools | 65.5.0 | 8 | 83.0.0 | |
| aiohttp | 3.14.1 | 6 | 3.14.3 | |
| cryptography | 48.0.1 | 6 | 50.0.0 | |
| pyasn1 | 0.6.3 | 6 | 0.6.4 | |
| gradio | 6.10.0 | 5 | 6.16.0 | |
| httplib2 | 0.31.2 | 2 | 0.32.0 | |
| msgpack | 1.2.0 | 2 | 1.2.1 | |
| accelerate／datasets／hydra-core／torch | — | 1 各 | — | |

npm audit（website/，`--omit=dev`）：critical 1 astro ≤7.2.7；high 9：devalue 5.6.3–5.8.0、fast-uri 3.0.0–3.1.5、js-yaml 4.0.0–4.3.1、
nanoid ≤3.3.17、postcss ≤8.5.22、sharp ≤0.35.4-rc.0、smol-toml ≤1.7.0、svgo 4.0.0–4.0.2、vite 7.0.0–7.3.3；moderate 4、low 1。全部 fixAvailable。

requirements 漂移 12（同套件不同版本）：fastapi 0.134.0／0.136.3、google-api-python-client 2.190.0／2.197.0、google-auth 2.48.0／2.54.0、
google-auth-httplib2 0.3.0／0.4.0、google-auth-oauthlib 1.3.0／1.4.0、pillow 12.2.0（server）／12.1.1（agent）／12.2.0（lock）、
pydantic 2.10.6／2.13.4、python-multipart 0.0.32（server）／0.0.22（agent）、python-socketio 5.16.1／5.16.2、requests 2.32.5／2.34.2、
uvicorn 0.41.0／0.49.0、xxhash 3.6.0／3.7.0——除 pillow／python-multipart 外全是 agent vs lock。只出現在一份：lock 149 套（全是 F5-TTS／
gradio／torch 那串傳遞依賴）、server 4 套（openpyxl／pypdf／python-docx／python-pptx）。
（上次算 25 是把「只在一份」也算進去；這次分開列。）

## 附錄 C：階段三明細

根目錄非程式檔（T＝追蹤中；refs＝其他 py/md/bat/ps1/vbs/js 有幾個提到檔名）：

| 檔 | 追蹤 | 最後改 | refs | 判定 |
|---|---|---|---|---|
| Create_Agent_Shortcut.vbs | T | 06-13 | 0 | 候選刪除 |
| Install_or_Update_DEBUG.bat | T | 06-13 | 0 | 候選刪除 |
| create_install_shortcut.ps1 | T | 06-13 | 0 | 候選刪除 |
| setup_env.bat | T | 06-13 | 0 | 候選刪除 |
| start_server.vbs | T | 06-13 | 0 | 候選刪除（start_hidden.vbs refs=19 才是活的） |
| pyrightconfig.json | T | 06-13 | 0 | 保留（pyright 自己讀） |
| Create_Shortcut.vbs／Fix_Task.bat／Install_*.bat／update_agent.bat／dev_start.ps1 | T | 06 | 2–9 | 保留 |
| settings.json.bak_20260808_214848、uvicorn_*.log.bak、dev8001_boot.log | – | 06–08 | 0 | 本機殘留，可刪 |
| Originsun_Agent.zip／*.json runtime／uvicorn_*.log | – | 近期 | 有 | 已 ignore，正常 |

- `git ls-files -i -c --exclude-standard`：空。`.pyc`／`__pycache__`：0。`.coverage`／`htmlcov` 已在 .gitignore（`de640dc4`）。
- 追蹤中最大 blob：exiftool_files/perl532.dll 3.3MB、Geolocation.dat 3.1MB、TagNames.pod 2.0MB——全 < 5MB。
- scripts/ 一次性腳本 23 支（backfill_／import_／seed_／_tmp_）：只有 `seed_dev_website_data.py`（06-19）超過 60 天，其餘 08-17～09-03；
  零引用的 16 支（backfill_petty_*×2、backfill_cash_*×2、backfill_me_petty、import_cashbook×2、import_payment_requests、import_welfare、
  import_my_snapshots、seed_household_account、seed_my_accounts_assets、seed_financial_asset_account、_tmp_deploy193、seed_dev_website_data）
  ——都是已跑過的資料遷移，候選歸檔到 `scripts/archive/`。

## 附錄 D：階段四明細

- 最慢（乾淨單跑）：8.66s test_every_frontend_module_parses、4.75s test_no_undefined_names_in_es_modules、2.43s
  test_no_undefined_names_in_classic_page_scripts、2.37s test_office_module_graph_has_no_schedulers_or_runners、2.26s
  test_the_scan_actually_finds_lazy_imports、2.02s test_run_stream_kills_the_child_on_timeout。只有第一支 > 5 秒。
- skipped 0／xfail 0／xpass 0。
- 覆蓋率 35.9%（core 77.6%／83 檔、routers 22.6%／107 檔、services 21.6%／55 檔）。
- 候選（30 天改 ≥3 次且覆蓋 <40%）51 檔，前十：routers/crm/finance.py（76 次／19.6%）、routers/crm/projects.py（49／18.6）、
  routers/api_finance_projects.py（42／25.0）、routers/api_me.py（41／26.8）、routers/api_finance_stmt.py（31／12.8）、
  routers/crm/costs.py（30／17.2）、routers/crm/quotes.py（29／18.7）、services/finance_statements.py（32／28.8）、
  routers/api_finance.py（26／15.2）、routers/crm/project_links.py（35／38.7）。09-13 已判定：這批 router 的純函式都釘住了、剩下的
  全吃 DB。**這次改看 09-13 之後新進的模組**：純規則 `core/schedule_logic.py` 96.0%、`core/leave_logic.py` 95.6%、`core/drive_map.py`
  97.0% 都有人釘；沒人釘的是三支 service 裡的純函式——
  - `741c1742` tests/unit/test_calendar_sync_health.py（5 支）：attendees_of 的 JSON／list／壞字串三形狀與去重、timesheet_ids_of、
    schedule_dict 的全欄預設與 ISO 時間、colors_from_settings 合併預設。覆蓋 22% → 純函式段全綠。
  - `1c18a3ee` tests/unit/test_google_calendar_health.py（5 支）：calendars 只留四種且有值、calendar_for 專屬 > 共用 > 設定、
    calendar_id 去空白、load_sa_from_settings 空白回 None、CALENDAR_KINDS。
  - `e0f534d3` tests/unit/test_leave_service_health.py（5 支）：hours_from_body 三種來源優先序＋四種拒絕、request_dict 的
    cancel_mode（free／apply／locked／未核准 None）、credit_dict 餘額、build_request 只在 range 留時段。
  三檔都以 `_health.py` 結尾，整檔可刪。

## 附錄 E：結構債（/polish --debt 規則；掃描腳本沒留在 repo，規則同 09-13 那份）

### py: >2000 🔴 1 ／ 1500–2000 🟠 5 ／ 800–1500 🟡 31
- 🔴 2350 core_engine.py
- 🟠 1946 routers/api_finance.py、1823 routers/api_proposals.py、1635 core/worker.py、1610 routers/crm/media_log.py、1507 routers/crm/finance.py
- 🟡 前十：core/finance_logic/_core.py 1493、routers/crm/showcase.py 1461、routers/api_references.py 1432、routers/api_finance_stmt.py 1366、
  routers/crm/petty.py 1329、routers/crm/proposal_assets.py 1308、routers/api_ota.py 1288、routers/crm/quotes.py 1283、routers/crm/cash.py 1219、
  services/finance_statements.py 1136（上次 26 → 31：quotes／cash／costs／ledger_project／project_links 過線）

### js: >2000 🔴 0 ／ 1500–2000 🟠 6 ／ 800–1500 🟡 16
- 🟠 crm-cashbook.js 1894、finance/subviews/recon.js 1854、projects/projects.js 1698、website/subviews/blog/editor.js 1602、
  crm-projects-cost.js 1538、crm-invoices.js 1521

### css: 🟡 3（crm.css 1372、projects.css 1365、crm-project-views.css 978）

### 超長函式（py > 200 行）11 支
556 core_engine.run_concat_job、440 core_engine.run_backup_job、437 core/worker._drone_meta_sync、314 publish_update.main、
291 core_engine._run_concat_advanced、290 api_finance_stmt.apply_bank_statement、266 core/report_job._run_report_job、
263 transcriber.run_transcribe_job、233 scripts/reconcile_ledger_sheet.run、221 bootstrap.run_update、216 api_finance_stmt._build_statement_preview

### 巢狀 ≥5 層 106 支，最深：core/worker._execute_job 12 層；7 層的有 transcriber.run_transcribe_job、三個 website `_scheduler_loop`、
finance_statements.drilldown、project_links.mirror_project_to_mine、api_schedules.update_schedule、api_agents.list_agents、
core_engine.run_transcode_job／run_concat_job、worker._run_backup、report_job._run_report_job。

### 無測試模組 58（tests/ 完全沒提到檔名），最大：core/schemas_website.py 1082、services/website/rebuild_service.py 698、
translation_service.py 642、post_service.py 635、watchdog_runner.py 586、routers/api_drone_meta.py 547、notion_url_service.py 542、
post_seo_runner.py 485、backup_service.py 472、routers/website/admin_seo.py 406、routers/api_tts.py 390。多數是 website/ 的 runner
與 admin router（跑在 NAS 容器、吃網路），跟上次一樣。

### 循環 import：0（core/services/routers/db 頂層 import 圖，長度 ≤4）。

## 附錄 F：階段六明細

- **CLAUDE.md 引用但不存在的路徑 7**：`core/schemas.py`（已拆套件，STALE_PATHS 有登）、`db/models.py`（第 3 節目錄樹還寫著單檔）、
  其餘 5 個是刻意的 glob／樣板（`routers/crm/*.py`、`<領域>.py`、`{detail,core}.js`、`<日期>.md`、`routers/services/core/db/…` 那串）。
- **曾是單檔、現為套件、STALE_PATHS 沒登**：`core/finance_logic.py`、`db/models.py`（見「沒動的東西」第 1 條）。
- 30 天內動過、CLAUDE.md 完全沒提到的模組 43 個；動 ≥10 次的 5 個列在「沒動的東西」第 7 條。其餘（api_journal／journal_logic／
  api_milestones／api_calendar 等）多半在「模組職責」以功能名而非檔名登記，不算漏。
- ROADMAP 未勾 137 項，關鍵字對得上 15，人工看過只有 2 項真的做完（「沒動的東西」第 8 條）；其餘是「Astro」「build」這種泛詞誤配。
- CHANGELOG：第 7 行「自動生成：v1.10.1 → v2.5.23」段落跟 version.json 2.5.23 **同步**。09-13 兩份寫「落後（1.10.0 vs …）」
  是 regex 只認 `## [x.y.z]` 標題的假陽性——下次 health 的檢查要連「自動生成」那段一起看。
- docs/*_PLAN.md 超過 90 天沒動：0（最舊的也在 6 月底之後）。
- CLAUDE.md「不要動的地方」這次沒有新地雷可加（STALE_PATHS 那條規則已存在，只是兩個舊拆檔沒登），未動。

## 附錄 G：前端 no-undef 臨時 config（每次重寫，不進 git）

```js
import globals from "globals";
export default [{
  files: ["frontend/**/*.js"],
  ignores: ["frontend/vendor/**", "frontend/**/*.min.js", "frontend/dist/**"],
  languageOptions: { ecmaVersion: 2024, sourceType: "module",
    globals: { ...globals.browser, window: "readonly", io: "readonly", Chart: "readonly", google: "readonly" } },
  rules: { "no-undef": "error" },
}];
```
`npx eslint -c <path> --no-config-lookup frontend -f json`（`unix`／`compact` formatter 已不在 eslint 9 核心，只有 json 能用）。
