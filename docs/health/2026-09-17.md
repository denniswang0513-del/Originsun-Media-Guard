# health 報告 2026-09-17

> 上次：2026-09-16。中間進了 v2.5.36–2.5.46（OTA updater-first＋簽章、私帳堡壘三批、財富自由、登記餘額、私帳月報，
> 各自帶 /polish）、09-16 報告的十條 owner 處理了六條（見下表）。這份是完整跑（六個階段都重算）。

```
health 報告 2026-09-17                                              上次：2026-09-16

基準測試：通過（4215 支，43.8 秒*）                                  上次：4068 / 42.7
階段一 靜態閘門：ruff 0 ／ eslint 0 ／ pyright 6113 errors（0 reportUndefinedVariable）／ 前端漏 import 0 ／ preflight OK ／ 寫卡 0 張
階段二 依賴：釘版落後主版本 0（上次 3）／ CVE 14 套 87 條（機隊中：0）／ npm high+ 0 ／ requirements 漂移 0（上次 1）
階段三 衛生：候選刪除 0（候選搬 docs/ 1）／ 候選 gitignore 0 ／ 大檔 0（最大 3.3MB）／ 一次性腳本 23（>60 天 1、零引用 14）
階段四 測試：最慢 8.5 秒（test_every_frontend_module_parses）／ skipped 0 ／ xpass 0 ／ 覆蓋率 36.9%（core 79.1、routers 22.6、services 22.2）／ 補了 3 個模組 13 支測試
階段五 結構債：>2000 行 1（core_engine.py 2350）／ 1500–2000 行 py 6（+1）+ js 5 ／ >200 行函式 11 ／ 巢狀 ≥5 層 102 ／ 循環 import 0
階段六 文件：模組表缺 0（上次那五個都補了）／ ROADMAP 可勾 57（Phase M 執行階段整塊）／ CHANGELOG 落後 2 版 ／ 過期 PLAN 0 ／ STALE_PATHS 齊 ／ CLAUDE.md 死連結 0
commits：881a830b（timesheet_manual tests）、b21dcdc3（api_equipment tests）、051888c4（cash_tree tests）、本檔
停止原因：正常結束（主流程 commit 4／8，卡片 0／10）
```

\* 第一次基準跟 ruff 同時跑量到 81.7 秒（`test_every_frontend_module_parses` 被撐到 46 秒，跟前三次同一種干擾）；
乾淨全套 43.8 秒（含新測試 4228 支）、最慢一支單獨量 8.50 秒。

## 上次報告的十條，這次的下落

| # | 09-16 的項目 | 這次 |
|---|---|---|
| 1 | `xxhash==3.6.0`／`aiofiles==24.1.0` 落後主版本 | ✅ `9faedb6c` 機隊釘版放回：xxhash 4.0.1、aiofiles 25.1.0（釘版落後主版本歸零） |
| 2 | pip-audit 15 套 95 條，全部不在機隊 | 14 套 87 條，仍全部不在機隊；少的是 pypdf（6.14.2 → 6.16.1，8 條清掉） |
| 3 | `python-multipart` server／agent 版本不同 | ✅ 兩份都 0.0.32（漂移歸零） |
| 4 | `title_name.txt` 零引用 | ✅ `c173c0d7` 刪了 |
| 5 | CLAUDE.md:1104 死連結 `test_finance_files_stay_readable.py` | ✅ 改成 `test_files_stay_readable.py` |
| 6 | ROADMAP 可勾 3 項 | ✅ owner 三項都勾了（L463／L696／L799） |
| 7 | 模組職責表缺五個模組 | ✅ finance_statements／api_finance_stmt／api_crm_mobile／timesheet_manual／timesheet_lookup 都進表了 |
| 8 | core_engine.py 2350 行、11 支長函式 | 未動（一支都沒少） |
| 9 | 本機殘留 5 個 .bak／.log | ✅ 清了；這次新長出 `dev_boot.err`（已 ignore） |
| 10 | 最慢測試 8.27 秒 | 8.50 秒，刻意全掃，留著 |

## 沒動的東西（依影響排序，一行一條；想追再看附錄）

1. **`routers/api_finance.py` 1956 行，離 `test_files_stay_readable` 的 2000 上限 44 行**（上次 1946）—— CLAUDE.md 已寫「別再往裡面加端點」
   （登記餘額因此獨立成 `api_balance_register.py`），但這週還是長了 10 行。下一個進去的功能就會撞紅；建議下次 /polish 先拆（候選：
   `_balances_by_account` 那組帳戶餘額算法搬 `services/`）。
2. **ROADMAP L536–624「Phase M 執行階段（3 大 Stage 共 8 個子階段）」57 項全部沒勾**，而 Phase M 2026-04-29 就上線了（同檔 L943 樹狀圖標 ✅）——
   整塊是上線前的計畫清單，跟現況對不上；候選：整塊勾掉或收成一行「✅ 見 docs/WEBSITE_ARCHITECTURE.md」。owner 決定。
3. **CHANGELOG 停在 v2.5.44，version.json 是 v2.5.46**（2.5.45 登記餘額、2.5.46 私帳月報沒進去）—— 發版 commit `c7120718`／`f83551df` 沒帶 CHANGELOG。
4. **pip-audit 14 套 87 條，全部不在 `requirements_agent.txt`**（附錄 B）：gitpython 26／pip 12／starlette 10／setuptools 8…同上次；
   starlette 是 FastAPI 傳遞依賴跑在 master 與 NAS 容器。升級不在 health 權限內。
5. **`檔案處理流程.txt`**（根目錄、**追蹤中**、42 行的備份衝突流程設計筆記、零引用）—— 09-16 報告把它寫成「已 ignore，正常」是錯的
   （中文檔名在 bash 的 `git ls-files` 比對上失手）。內容還有用，候選**搬 `docs/`** 不是刪。
6. **`normalize_row` 現況：只帶 `project_id`、沒帶案名也沒別的內容 → 422「空白列不存」**（id→名稱回填排在空白檢查之後）。兩個前端都一起送
   `project_name` 所以沒人踩到；已用 `test_timesheet_manual_health` 釘住，改順序的人會看見。不算 bug、不寫卡。
7. **`core/cash_tree.index_paths` 對自環節點會把名字印兩次**（walk 起手 `seen` 是空的）—— 有限、不遞迴，後台編輯做不出這種資料；釘住不修。
8. **`core/finance_logic/_core.py` 1535 行，這次跨進 1500–2000 那層**（上次 1493；登記餘額的 `derive_balance` 進來了）。
9. **一次性腳本零引用 14 支**（附錄 C；`backfill_cash_depth`／`backfill_my_ledger_detail` 這次有引用了，從 16 降到 14）；`scripts/archive/` 仍不存在。
10. **`ota_signing_key.pem` 在 dev checkout 根目錄**（09-16 10:37 建、已 ignore、ZIP 只帶 `ota_signing_pub.pem`、`.gitignore:46` 有）——
    跟 CLAUDE.md「私鑰在 `C:\OriginsunAgent\`」的描述多一份；deploy_to_prod 整棵複製所以它就是那份的來源。**正常**，只是記一筆讓下次看到不要驚。

## 未執行

- 無。六個階段全部跑完；工具都在 `.venv`（ruff／pyright 1.1.414／pytest-cov／pip-audit），沒裝新東西。
- 階段一沒寫卡：四個閘門都是零（ruff／eslint／前端漏 import／preflight），pyright 0 筆 `reportUndefinedVariable`。
- 階段六「唯一寫入」沒動：這次沒有「確定是地雷」的新發現（第 6、7 條是釘住的現況，不是咬過人的東西）。

---

## 附錄 A：階段一明細

- `ruff check .`：All checks passed。`npm run lint`：0。`preflight.py`：OK（19 imports verified、21 非 agent 依賴跳過）。
- pyright 6113 errors／302 檔（上次 5946／292；+167 ≈ 登記餘額／月報／`core/ota_sign.py` 那批）。
  前八名：reportPossiblyUnboundVariable 1947、reportCallIssue 1772、reportArgumentType 1172、reportAttributeAccessIssue 613、
  reportGeneralTypeIssues 255、reportOptionalMemberAccess 112、reportOptionalSubscript 108、reportOptionalCall 67。`reportUndefinedVariable` **0**。
  reportAttributeAccessIssue 前 20 筆：pptx `BaseShape.text_frame`、`PIL.Image.LANCZOS`、**新進** `core/ota_sign.py:42／55` 的
  cryptography key union（`DHPrivateKey`／`X25519PrivateKey`… 沒有 `.sign`，實際 load 出來是 Ed25519／RSA）、`Result.rowcount`——全是 stub 缺口。
  reportCallIssue 前 20 筆：同上 `ota_sign` 的 `sign()` 參數（RSA 簽章要 padding／algorithm，Ed25519 不要）＋ SQLAlchemy model 建構子
  `No parameter named "id"`（bookmarks_repo／seed_cash_taxonomy／seed_finance）。同上次，不是 bug。
- 前端 no-undef（臨時 config 同 09-15 附錄 G，放 `.health_tmp/` 跑完刪）：686 筆／18 檔（**跟上次一模一樣**）。有 import/export 的 ES module
  仍只有 5 檔命中：`app.js`（appendLog／getComputeBaseUrl／fetchModelStatus）、`js/app/remote-dispatch.js`（appendLog／getComputeBaseUrl）、
  `tabs/projects/agent-update.js`（appendLog）、`tabs/crm/crm-projects-cost.js:1016`（_doImportFromQuote）、`tabs/tts/tts.js:51`（loadDictionary）——
  五個名字的 `window.X =` 這次重新 grep 過都在（utils.js:1132／1134、transcribe.js:299、crm-projects-cost.js:1051、tts.js:702）。判定同前三次：慣例，不是漏 import。

## 附錄 B：階段二明細

釘版且落後主版本：**0**（`requirements_server.txt` 18 條、`requirements_agent.txt` 19 條全部對到 `pip list --outdated` 的最新主版）。

pip-audit（14 套 87 條；AGENT＝在 requirements_agent.txt —— 一個都沒有）：

| 套件 | 現版 | 條數 | fix | 機隊 |
|---|---|---|---|---|
| gitpython | 3.1.50 | 26 | 3.1.59 | |
| pip | 24.0 | 12 | 26.2.0 | |
| starlette | 0.52.1 | 10 | 1.3.1 | |
| setuptools | 65.5.0 | 8 | 83.0.0 | |
| pyasn1 | 0.6.3 | 6 | 0.6.4 | |
| cryptography | 48.0.1 | 6 | 50.0.0 | |
| aiohttp | 3.14.1 | 6 | 3.14.3 | |
| gradio | 6.10.0 | 5 | 6.16.0 | |
| msgpack | 1.2.0 | 2 | 1.2.1 | |
| httplib2 | 0.31.2 | 2 | 0.32.0 | |
| torch 2.12.0／hydra-core 1.3.3／datasets 5.0.0／accelerate 1.14.0 | — | 1 各 | — | |

npm audit（website/，`--omit=dev`）：**0**（info／low／moderate／high／critical 全 0）。

requirements 漂移：同套件不同版本 **0**（上次 1，python-multipart 已統一 0.0.32）。只出現在一份：server 11、agent 12（兩份本來就是不同機器的依賴面）。
`requirements.txt`（13 行，全是註解／-r）與 `0225_requirements.txt`（初裝完整版，Install bat／ota_manifest／publish_update 都吃）沒有 `==` 釘版，不進比對。

## 附錄 C：階段三明細

根目錄非程式檔（T＝追蹤中；refs＝其他 py/md/bat/ps1/vbs/js/html 提到檔名的檔數；health 報告不算）：

| 檔 | 追蹤 | 最後改 | refs | 判定 |
|---|---|---|---|---|
| 檔案處理流程.txt | **T** | 03-16（歷史改寫前） | 0 | **候選搬 docs/**（備份衝突流程的設計筆記，內容仍與 core_engine 的衝突對話框一致） |
| Create_Shortcut.vbs／Fix_Task.bat／Install_*.bat／update_agent.bat／dev_start.ps1／start_hidden.vbs | T | 06 | 2–19 | 保留 |
| FINISHING.md／PUBLISHING.md／TEST_INSTRUCTIONS.md／TEST_REPORT.md／Maintainer_Guide.md | T | 06–07 | 2–3 | 保留 |
| taiwan_dict.json／update_manifest.json／pyrightconfig.json | T | — | 1–9 | 保留 |
| server_test_log.txt（06-17）、dev_boot.err（新）、.coverage、docs/timesheet_import_report.md | – | — | 0 | 本機殘留，已 ignore，可刪 |
| ota_signing_key.pem | – | 09-16 | — | 已 ignore、ZIP 不帶；見「沒動的東西」第 10 條 |
| Originsun_Agent.zip／api_keys／bookmarks／job_history／publish_history／update_status／watcher_history .json／uvicorn_*.log | – | 近期 | 有 | 已 ignore，正常 |

- `git ls-files -i -c --exclude-standard`：空。`.pyc`／`__pycache__`：0。
- 追蹤中最大 blob：exiftool_files/perl532.dll 3.3MB、Geolocation.dat 3.1MB、TagNames.pod 2.0MB、libstdc++-6.dll 1.4MB——全 < 5MB。
- scripts/ 一次性腳本 23 支（另 4 個 `_placeholder_backup_*.json` 已 ignore）：只有 `seed_dev_website_data.py`（06-19）超過 60 天；
  零引用 14 支：`_tmp_deploy193`、`backfill_cash_fields`、`backfill_cash_invoice_links`、`backfill_me_petty`、`backfill_petty_dates`、`backfill_petty_owner`、
  `import_cashbook_coop`、`import_my_equipment`、`import_my_snapshots`、`import_payment_requests`、`import_welfare`、`seed_dev_website_data`、
  `seed_financial_asset_account`、`seed_my_accounts_assets`——候選歸檔到 `scripts/archive/`（目前沒有這個目錄）。

## 附錄 D：階段四明細

- 最慢（乾淨單跑）：8.50s test_every_frontend_module_parses、4.32s test_no_undefined_names_in_es_modules、2.46s
  test_office_module_graph_has_no_schedulers_or_runners、2.42s test_the_scan_actually_finds_lazy_imports、2.23s
  test_no_undefined_names_in_classic_page_scripts、2.01s test_run_stream_kills_the_child_on_timeout。只有第一支 > 5 秒。
- skipped 0／xfail 0／xpass 0。
- 覆蓋率 36.9%（core 79.1%／86 檔、routers 22.6%／110 檔、services 22.2%／56 檔）——上次 35.9（core 77.9／routers 22.5／services 21.8）。
- 候選（30 天改 ≥3 次且覆蓋 <40%）52 檔，前十跟前兩次同一批（routers/crm/finance.py 75 次／19.6%、projects.py 48／18.6、api_me.py 49／22.3、
  api_finance_projects.py 42／25.0、api_finance_stmt.py 33／12.9、services/finance_statements.py 33／28.8、api_finance.py 27／15.0、quotes.py 28／18.7、
  costs.py 27／17.2、project_links.py 35／38.7）——09-13／09-16 已判定全吃 DB。這次改用 AST 掃「公開、非 route、參數不含 session／request、
  函式體有 ≥4 行沒覆蓋」的函式，整個候選集只剩 33 支，其中純函式（不碰 I/O）的只有：`timesheet_manual.normalize_row`、
  `api_equipment.mark_checked_out／mark_returned`、`cash_tree.index_paths`、`api_references.ref_dict`、`website/project_service.append_old_slug_if_changed*`、
  `scheduler.save_schedules`；其餘是 async 的 I/O 包裝（`generate_quotation_snapshot`、`calendar_sync.*`、`send_digest`、`run_watcher_scan`…）。
  - `881a830b` tests/unit/test_timesheet_manual_health.py（5 支，模組 41%→；30 天改 20 次、09-16 報告點名）：起訖推時數（跨午夜 +24h、有時數不重算、
    起訖正規化 HH:MM）、空白列 422（plan 旗標繞不過；專案／做了什麼／備註／工作階段任一格放行；有時數不算空白）、status 三態
    （draft／plan／pending）與 Sheet 列 `manual=False` status None 且空白也收、分類白名單 422 原句與三種日期格式、
    project_id ＞ keep ＞ 名稱對映（對映表 map／內部桶 bucket／撞案 ambiguous／none）＋ strip 規則。**順手釘住**：只帶 project_id 沒別的內容仍 422。
  - `b21dcdc3` tests/unit/test_api_equipment_health.py（4 支，19.7%）：領用填 out_at＋器材「出勤」、person 只補空白且截 64；歸還填 returned_at＋
    「在庫」、備註 strip、空備註不覆蓋、截 255。
  - `051888c4` tests/unit/test_cash_tree_health.py（4 支，33%）：根到自己的路徑、`None` 與 `ROOT` 都當根、孤兒留自己那一節（跟 build_tree 丟孤兒不同）、
    二環有限、**自環現況印兩次**、列順序無關、空輸入空。
  三檔都以 `_health.py` 結尾，整檔可刪。全套 4215 → 4228。
- 沒挑的：`api_references.ref_dict`（36 行序列化、依 ORM 欄位太多，SimpleNamespace 要塞 30 個屬性，特徵測試會變成抄欄位表）、
  `website/project_service.append_old_slug_if_changed`（`063ff95d` 09-13 已有 old-slug 測試檔，只是覆蓋沒算到那兩支的分支）、`scheduler.save_schedules`（寫檔）。

## 附錄 E：結構債（/polish --debt 規則；掃描腳本在 `.health_tmp/debt.py`，跑完刪；口徑：不含 tests/、website/、exiftool_files/）

### py: >2000 🔴 1 ／ 1500–2000 🟠 6 ／ 800–1500 🟡 27
- 🔴 core_engine.py 2350
- 🟠 routers/api_finance.py **1956**（↑10）、routers/api_proposals.py 1823、core/worker.py 1635、routers/crm/media_log.py 1610、
  **core/finance_logic/_core.py 1535**（↑42，新進這層）、routers/crm/finance.py 1507
- 🟡 前十：routers/crm/showcase.py 1461、routers/api_references.py 1432、routers/api_finance_stmt.py 1353、routers/crm/petty.py 1329、
  routers/api_ota.py 1325、routers/crm/proposal_assets.py 1308、routers/crm/quotes.py 1283、routers/crm/cash.py 1219、
  services/finance_statements.py 1136、routers/api_me.py 1125

### js: >2000 🔴 0 ／ 1500–2000 🟠 5 ／ 800–1500 🟡 16
- 🟠 crm-cashbook.js 1894、finance/subviews/recon.js 1854、projects/projects.js 1698、crm-projects-cost.js 1538、crm-invoices.js 1521（跟上次相同）

### css: 🟡 3（crm.css 1372、projects.css 1365、crm-project-views.css 978）

### 超長函式（py > 200 行）11 支（一支都沒變）
556 core_engine.run_concat_job、440 core_engine.run_backup_job、437 core/worker._drone_meta_sync、314 publish_update.main、
291 core_engine._run_concat_advanced、290 api_finance_stmt.apply_bank_statement、266 core/report_job._run_report_job、
263 transcriber.run_transcribe_job、233 scripts/reconcile_ledger_sheet.run、221 bootstrap.run_update、216 api_finance_stmt._build_statement_preview

### 巢狀 ≥5 層 102 支（上次 95；+7 ＝ 登記餘額／月報那批），最深：core/worker._execute_job 12 層；7 層的 13 支：
transcriber.run_transcribe_job、finance_statements.drilldown、project_links.mirror_project_to_mine、api_utils.api_resolve_drop、api_schedules.update_schedule、
api_proxy.verify_standalone_proxies、api_agents.list_agents、core_engine.run_transcode_job／run_concat_job／run_backup_job、worker._run_backup、
scheduler._check_and_dispatch、report_job._run_report_job。

### 無測試模組 42（口徑：模組路徑、檔名或 stem 在 tests/unit 任一檔出現過就算有；比上次的 58 寬鬆，數字不能直接比），最大：
core/schemas_website.py 1082、db/migrations_website.py 897、routers/api_drone_meta.py 547、routers/api_tts.py 390、db/seed_website_testdata.py 350、
routers/api_intel.py 299、routers/crm/briefs.py 261、db/seed_website.py 260、routers/api_bulletin.py 258、routers/api_footage.py 240、
routers/api_bookmarks.py 229、services/ga_service.py 225。

### 循環 import：0（core/services/routers/db 頂層 import 圖，長度 ≤4）。

## 附錄 F：階段六明細

- **模組職責表**：09-16 點名的五個都補了。30 天內動過、CLAUDE.md 完全沒提到檔名或 stem 的模組 107 個；動 ≥5 次的 11 個：
  `routers/api_finance_card.py`（15）、`routers/crm/cash_splits.py`（12）、`routers/api_journal.py`（11）、`routers/crm/benefits.py`（10）、
  `core/bank_statement.py`（10）、`core/card_statement.py`（9）、`services/timesheet_digest.py`（8）、`services/timesheet_puller.py`（6）、
  `services/timesheet_ingest.py`（6）、`routers/api_milestones.py`（6）、`core/project_link.py`（6）。前兩個（信用卡摘要、收支拆項）
  下次 /polish 的 maintainability 階段值得各補一行。
- **CLAUDE.md 引用但不存在的路徑 3**：`core/schemas.py`／`db/models.py`（講歷史，STALE_PATHS 有登）、`routers/services/core/db/main_website.py`
  （刻意的縮寫）。`[text](path)` 形式的死連結 **0**（上次 1，已修）。
- **曾是單檔、現為套件 → STALE_PATHS**：`core/finance_logic.py`／`core/schemas.py`／`db/models.py` 三個都登了。
- **ROADMAP**：關鍵字對 30 天 commit 的自動比對只命中 5 條，全是噪音（L622「7/1」對到「會計年度 7/1」之類）。人工看：L536–624
  「Phase M 執行階段」57 項 `- [ ]` 全部沒勾、0 項有勾，而 L943 樹狀圖與 CLAUDE.md 檔頭都寫 Phase M ✅ 2026-04-29 上線——整塊是上線前的計畫，
  候選整塊勾掉或收成一行。上次那三條（L463／L696／L799）owner 已勾。
- **CHANGELOG**：最後一條 v2.5.44，version.json v2.5.46 → 落後 2 版（上次同步）。
- **docs/*_PLAN.md 超過 90 天沒動：0**（最舊五份都是 08-30）。
- CLAUDE.md「不要動的地方」這次沒有新地雷可加，未動。
