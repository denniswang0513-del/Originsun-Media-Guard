# health 報告 2026-09-17（第二次）

> 上次：2026-09-17（早上，v2.5.46 基準）。中間進了 /polish 那 17 個 commit 的收尾（收尾 review 三修）與 owner 點名的五項調整
> （作品清單、月報收支改吃三表分類、office-api 補欄位、共用 fmtWan、台積電穿透比例進設定）。六個階段都重跑；數字跟早上那份比。

```
health 報告 2026-09-17-2                                            上次：2026-09-17

基準測試：通過（4230 支，44.0 秒）                                   上次：4215 / 43.8
階段一 靜態閘門：ruff 0 ／ eslint 0 ／ pyright 4645 errors（0 reportUndefinedVariable；早上 6113，範圍不同見附錄 A）／ 前端漏 import 0（no-undef 686 筆／18 檔，跟早上一模一樣）／ preflight OK ／ 寫卡 0 張
階段二 依賴：釘版落後主版本 0 ／ CVE 14 套 87 條（機隊中：0；跟早上相同）／ npm high+ 未執行（website 沒 node_modules）／ requirements 漂移 0（只出現在一份裡 23，同早上）
階段三 衛生：候選刪除 0 ／ 候選 gitignore 0（`git ls-files -i` 空）／ 大檔 0（最大 3.3MB exiftool）／ 一次性腳本 23（同早上）／ pyc 進 git 0
階段四 測試：最慢 8.8 秒（test_every_frontend_module_parses）／ skipped 0 ／ xpass 0 ／ 覆蓋率 37.1%（早上 36.9）／ 補了 0 個模組（前 5 個候選都是 router 端點，見附錄 D）
階段五 結構債：>2000 行 1（core_engine.py 2350）／ 800–2000 行 54（py+js）／ >200 行函式 11（同早上）／ 無 test_<name> 檔的模組 155/244（含命名不對得上的，見附錄 E）
階段六 文件：模組表死路徑 1（`core/schemas.py` → 套件，已修）／ ROADMAP 關鍵字命中 10（全是「儀表板」「純函式」這種泛詞，判 0 可勾）／ CHANGELOG 落後（最新條目 2.5.44，version 2.5.46；發版自動生成）／ 過期 PLAN 0
commits：health: docs（CLAUDE.md 死路徑＋三條地雷）、health: report（本檔）
停止原因：正常結束（主流程 commit 2／8，卡片 0／10）
```

## 早上那份的「沒動的東西」，這次的下落

| # | 早上的項目 | 這次 |
|---|---|---|
| 1 | `routers/api_finance.py` 1956 行，離 2000 上限 44 行 | 1956 → **1956**（登記餘額的端點已在獨立檔；月報也是獨立檔）。仍是下一個要拆的 |
| 2 | core_engine.py 2350 行、11 支長函式 | 未動 |
| 3 | 覆蓋率 routers 22.6% | 37.1% 整體（+0.2）；routers 端點沒有純函式可釘，只能靠 e2e |

## 這次新看到的

1. **`routers/api_balance_register.py`／`api_monthly_report.py` 一個請求裡的重複查詢**已在 /polish simplify 收掉（帳戶列傳入、卡費傳入、快照只撈 25 筆、清單不拖 payload），這次沒有新的。
2. **五項調整的副作用檢查**：作品清單 dev 447 件、重複的只剩「同名的兩個私帳案」（媒體顧問 202606–202609、連通案拍攝，都不是連結對，是資料本身兩筆）——這是資料問題不是程式，列在附錄 F 給 owner 決定要不要合併。
3. **月報收支改吃 `cashflow_lines` 之後**，dev 的 9 月本月收入／支出仍是 0（9 月沒明細），跟早上一致；分類正本只剩一份。

## 沒動的東西（依影響排序，一行一條）

1. `routers/api_finance.py` 1956 行 — 離單次讀取上限 44 行；下一批端點一定要另開檔（CLAUDE.md 已寫）
2. `core_engine.py` 2350 行、`run_concat_job` 556 行 — 老債，超過 800 行的 54 個檔裡最大的；沒有這次的 diff 動到它
3. pip-audit 14 套 87 條 — 全部不在 `requirements_agent.txt`，都是 master 端的重型套件（torch／gradio／datasets…）；升級要走機隊釘版流程，不在 health 權限內
4. `website/` npm audit 未執行 — 本機沒裝 node_modules（跟早上一樣）
5. 私帳兩筆同名專案 ×5（附錄 F）— 資料，owner 決定
6. CHANGELOG 最新條目 2.5.44 — 發版時自動補，這裡只回報
7. `scripts/` 23 支一次性腳本 — 同早上，最老的 `_tmp_deploy193.py`（08-27）；候選歸檔 scripts/archive/，不刪

未執行：
- website npm audit（沒有 node_modules）
- 階段四補測試：候選 51 個模組的前五名（routers/crm/finance.py 20%、routers/crm/projects.py 19%、routers/api_me.py 22%、routers/api_finance_projects.py 25%、routers/api_finance_stmt.py 13%）公開函式全是 async 端點；`services/finance_statements.py`（29%）的純函式 `explode_cash_splits`／`category_map_scope` 已有 16 處測試，未覆蓋的是 `_load_inputs`／`compute_live` 這種要 session 的取數層

## 附錄 A：階段一明細

- ruff：`ruff check .` 0。
- eslint：`npm run lint` 0。
- pyright（範圍 core／routers／services／db／main.py／main_office.py；早上是整個 repo 所以 6113）：4645 errors。
  規則分布：reportPossiblyUnboundVariable 1938、reportCallIssue 1336、reportArgumentType 541、reportAttributeAccessIssue 486、
  reportGeneralTypeIssues 221、reportOptionalMemberAccess 62、reportOptionalOperand 20、reportReturnType 12。
  三條要看的規則前 20 筆全在 `core/doc_text.py`（python-pptx 的 BaseShape 型別）、`core/image_utils.py`（PIL.Image.LANCZOS）、
  `core/ota_sign.py`（cryptography 的 union 型別）—— 都是第三方 stub 的型別不完整，不是 bug；reportUndefinedVariable 0。
- 前端 no-undef（臨時 config 同 09-15 附錄 G，放 `.health_tmp/` 跑完刪）：686 筆／18 檔。ES module 命中 5 檔：
  `app.js`（appendLog 20／getComputeBaseUrl／fetchModelStatus）、`js/app/remote-dispatch.js`（appendLog 70／getComputeBaseUrl 9）、
  `tabs/crm/crm-projects-cost.js`（_doImportFromQuote）、`tabs/projects/agent-update.js`（appendLog 4）、`tabs/tts/tts.js`（loadDictionary）
  —— 跟早上、09-15、09-13 判定相同：都有 `window.X =`，慣例不是漏 import。其餘 13 檔是傳統 script。
- preflight：OK（19 imports verified，21 個 master 專用套件跳過）。

## 附錄 B：階段二明細

pip-audit（14 套 87 條；沒有一套在 requirements_agent.txt）：accelerate 1.14.0、aiohttp 3.14.1（fix 3.14.2）、cryptography 48.0.1（fix 49.0.0）、
datasets 5.0.0（fix 5.0.1）、gitpython 3.1.50（fix 3.1.51）、gradio 6.10.0、httplib2 0.31.2（fix 0.32.0）、hydra-core 1.3.3（fix 1.3.4）、
msgpack 1.2.0（fix 1.2.1）、pip 24.0（fix 25.3）、pyasn1 0.6.3（fix 0.6.4）、setuptools 65.5.0（fix 65.5.1）、starlette 0.52.1（fix 1.0.1）、torch 2.12.0（fix 2.13.0）。
釘版落後主版本 0；四份 requirements 同套件不同版本 0；只出現在一份裡 23（同早上）。

## 附錄 C：階段三明細

根目錄非程式檔（最後修改／被引用次數）：Create_Shortcut.vbs 03-16／5、Fix_Task.bat 05-02／6、Install_Originsun_Agent.bat 06-14／5、
Install_or_Update.bat 06-14／3、Originsun_Agent.zip（未追蹤）／13、dev_start.ps1 06-13／6、start_hidden.vbs 05-04／21、update_agent.bat 06-14／12、
uvicorn_err.log／uvicorn_out.log（未追蹤，runtime）。全部保留。`git ls-files -i -c --exclude-standard` 空；追蹤中最大物件 3.3MB（exiftool）。
一次性腳本 23（`_placeholder_backup_*.json` ×4、`_tmp_deploy193.py` 08-27、backfill_* ×7、import_* ×8…），最老 08-17，沒有超過 60 天的。

## 附錄 D：階段四明細

最慢 15：8.81s test_every_frontend_module_parses、6.04s test_the_scan_actually_finds_lazy_imports、4.68s test_no_undefined_names_in_es_modules、
3.75s／3.62s test_cash_entry_writers 兩支、2.70s test_dep_scan、2.57s／0.94s test_office_surface、2.44s test_no_undefined_names_in_classic_page_scripts、
2.16s test_private_files_never_land_in_disk_cache、2.01s test_run_stream_kills_the_child_on_timeout。>5 秒 2 支（都是刻意全掃）。skipped 0、xfail 0、xpass 0。
覆蓋率 37.1%（core／routers／services）。「30 天內改 ≥3 次且 <40%」候選 51，前十：routers/crm/finance.py（75 次、20%）、routers/crm/projects.py（48、19%）、
routers/api_me.py（49、22%）、routers/api_finance_projects.py（42、25%）、routers/api_finance_stmt.py（33、13%）、services/finance_statements.py（33、29%）、
routers/api_finance.py（27、15%）、routers/crm/quotes.py（28、19%）、routers/crm/costs.py（27、17%）、routers/crm/project_links.py（35、39%）。
最低：core/loopsetup.py 0%、core/report_job.py 8%、core/worker.py 9%、routers/api_schedules.py 9%。

## 附錄 E：階段五明細

>2000 行：core_engine.py 2350。1500–2000：routers/api_finance.py 1956、frontend/tabs/crm/crm-cashbook.js 1894、subviews/recon.js 1854、
routers/api_proposals.py 1823、tabs/projects/projects.js 1698、core/worker.py 1635、routers/crm/media_log.py 1610、crm-projects-cost.js 1538、
core/finance_logic/_core.py 1535、crm-invoices.js 1521、routers/crm/finance.py 1507。
>200 行函式 11：run_concat_job 556、run_backup_job 440、_drone_meta_sync 437、publish_update.main 314、_run_concat_advanced 291、
apply_bank_statement 290、_run_report_job 266、run_transcribe_job 263、reconcile_ledger_sheet.run 233、bootstrap.run_update 221、_build_statement_preview 216。
「沒有 test_<模組名>* 檔」155/244 是二元判定，含命名對不上的（例如 core/fortress_logic.py 的測試叫 test_fortress.py）；真正沒測試的看覆蓋率那欄。

## 附錄 F：私帳同名專案（資料，owner 決定）

dev 8001 的作品清單在調整後仍有 5 組兩筆同名的**私帳**案（都沒有連結、都是 entity=mine）：媒體顧問 202606／202607／202608／202609、連通案拍攝。
不是母帳／私帳的連結對，是私帳本身重複建了兩筆；要合併請在私帳的專案頁處理，程式這邊不猜。

## 附錄 G：階段六明細

模組職責表：`core/schemas.py` 三處死路徑 → 改成 `core/schemas/`（套件）。表裡以路徑列出的模組沒有其他消失的；「程式碼有、表裡沒有」用檔名比對是 182，
但表是按功能分行寫的（很多模組在同一列的說明裡），早上那份人工核過是 0，這次沒有新模組（月報與登記餘額早上已登記）。
ROADMAP 未勾 131，關鍵字命中 10 全是「儀表板」「純函式」「手機頁」這種泛詞，沒有一條真的做完了。
CHANGELOG 最新 2.5.44（version.json 2.5.46）—— 發版自動生成，這裡只回報。`docs/*_PLAN.md` 沒有超過 90 天沒動的。
