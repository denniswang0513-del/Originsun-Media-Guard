# 使用者管理重整規劃（RBAC v3 草案）

> 2026-09-08 盤點。起因：owner「權限管理很混亂，同事很常遇到授權不足」。
> 原則：**不把角色表做回來**（RBAC v2 決定：權限直接綁帳號），改的是「怎麼給、怎麼守、怎麼看」。

## 1. 現況（數字都是 2026-09-08 量的）

| 面向 | 現況 | 為什麼會變成 403 |
|------|------|------------------|
| 鑰匙數 | `ALL_MODULES` **47 把**，扁平、逐帳號勾選 | 每個新同事都是手挑；漏一把就 403。15 個帳號的鑰匙數從 2 到 47 都有，沒有兩個一樣 |
| 守衛 | routers 裡 **30 種**守衛函式（`_check_auth`／`check_admin_or_module`／`require_bound_staff`／`_guard`／`_stage_guard`…） | 同一個分頁裡「看」走模組、「存」走管理員（今天蘇家弘兩次：編輯專案、預算表）。CRM 還有 **51 支**端點是管理員限定，但分頁本身是模組級開的 |
| 隱藏相依 | 總開關＋子鑰匙（`me_today_zone` × 5）、`me_leave` vs `hr_leave`、`finance_mine` 指名才有、`money_view`／`finance_approve` | 權限畫面只對「今天與這週」畫了縮排，其餘相依全靠記憶 |
| 第二軸：人員綁定＋身份 | `User.staff_id` ＋ `crm_staff.status`（在職／合夥／兼職／專案／單位） | 沒綁定＝整區空白或 403，畫面不說為什麼；兼職排班還看對方身份 |
| token 快照 | modules 登入時簽進 JWT（7 天） | 改權限後舊 token 看不到（今天劉禮瑜）。**v2.4.396 已修**：/auth/me、/me/workspace 漂了就換新 token |
| 回饋 | 403 只有前端 alert，沒有任何記錄 | owner 只能靠同事來抱怨才知道 |

一句話：**「看得到」跟「做得到」是兩套規則、47 把鑰匙沒有範本、沒人知道誰被擋在哪。**

## 2. 目標

1. 新同事到職：套一個範本、綁人員檔，5 分鐘內能工作，之後只加不減。
2. 分頁看得到＝裡面的按鈕做得到；管理員限定的動作**明列**（刪、推進狀態、錢、設定），其餘不准偷用管理員守衛。
3. 被擋時畫面直接說「缺哪一把」，管理員在「最近授權不足」一鍵開通。

## 3. 分期

### 階段 0：先看得見（半天～1 天，先做）

✅ 已做（2026-09-08，第三批一起）：`check_admin_or_module` 的 403 detail 說缺哪把（`core.auth.MODULE_LABELS` 後端正本、`denied_detail`）；每個 403 記進記憶體環形緩衝（`record_denial`，重啟就清、不建表）；`GET /auth/denials`（管理員）；使用者管理「使用者」分頁上方「最近授權不足」＋一鍵「開通『X』」（勾到那列、管理員再按儲存）。稽核腳本＝`docs/RBAC_AUDIT.md`。

- **403 帶原因**：`check_admin_or_module` 等守衛回 403 時 `detail` 帶 `{"missing": [key...], "label": "專案管理"}`；共用前端 `authFetch` 的錯誤處理把它顯示成「缺少『專案管理』權限，請管理員開通」，不再是「權限不足」四個字。
- **403 記錄**：`main.py` 加一個 middleware，每個 403 記 `username／path／method／missing` 到 DB（`auth_denials`，留 30 天）。
- **使用者管理加一塊「最近授權不足」**：誰、什麼時候、缺哪把；旁邊就是「開通」按鈕（PUT modules 補那把）。
- **一次性稽核腳本**：每個帳號 → 看得到哪些分頁 → 這些分頁會打的端點裡哪些會 403。先把今天存在的「看得到做不到」全部列出來，直接修。

### 階段 1：四個身份＝四個範本（1 天）

owner 2026-09-08 拍板：權限只有四種——**管理員、合夥、在職、兼職**。管理員＝Lv3（王士源；不套範本、什麼都有），
其餘三個就是 `crm_staff.status`（人員身份）。**官網管理（`website_admin`）三個身份都有**，不是獨立身份。
所以**範本不是另一個下拉，是身份本身**：

- 帳號綁了人員檔 → 範本＝那個人的身份。沒綁人員檔的 Lv1 帳號（目前 `Web`、`staf`）是例外：要嘛綁人員、要嘛留著手勾。
- 使用者管理只剩「身份（自動帶出）＋這個帳號額外多給的幾把（例外）」。改身份（兼職轉在職）＝重套範本，例外保留。
- 帳號上仍存自己那份 `modules`（RBAC v2 不變、守衛不變、token 不變）；範本存 `settings.json rbac.templates`，套用＝寫回 modules。

照 2026-09-08 生產 15 個帳號聚出來的初稿（「全部都有」直接進範本；「有人有」列成待拍板）：

| 範本 | 進範本 | 待拍板（只有部分人有） |
|------|--------|------------------------|
| 合夥（黃聖鈞、蘇家弘） | **owner 2026-09-08 拍板：除了私帳（`finance_mine`）以外全部**——即 `ALL_MODULES − {finance_mine}`（含零用金、福委會、人事、工作追蹤、公布欄、ComfyUI）。Lv3 硬閘門（使用者管理／設定／發版）仍只有管理員 | — |
| 在職（劉禮瑜、蔡念栩、連婕妤、李宜庭） | 基本資料、週誌、零用金、今天與這週全套（含兼職排班）、**官網管理** | 前期 5 把、後期 9 把、CRM 專案、工作追蹤、假勤、影像紀錄、片庫、福委會、ComfyUI（四個人各不相同——這裡就是 403 的來源，要定「在職就有」的底線） |
| 兼職（郭昭君、吳宇晨、陳偉健） | 基本資料、今天與這週（總開關＋我的一週＋團隊的一週）、**官網管理** | 今天的專案紀錄、專案查詢（有人沒有——兼職要不要填工時？）；測試帳號多的後期 9 把應拿掉 |
| 管理員（王士源） | Lv3，全部；不套範本 | — |

建議底線（owner 改）：**在職＝合夥扣掉錢流三把與看片門戶**（前期、後期、CRM 專案、工作追蹤、假勤、影像紀錄、片庫都給），
**兼職＝基本資料＋今天與這週整套＋官網管理**（含今天的專案紀錄，兼職也要填工時才有團隊的一週）。

### 階段 1.5：「公開區」——對外免登入的功能集中一處、owner 自己開關（1 天）

✅ 已做（2026-09-08）：`core/public_access.py` 登記表＋`surface_gate`（掛 public_router／token_router／portal；/q/ 與 /register 手動）；`GET/PUT /auth/public-access`；使用者管理第四分頁「公開區」（10 面、有效連結數、關閉／連結／公開）。

owner 2026-09-08：「雜支與影像紀錄是公開給外部人員編輯的，列一個全公開的區塊讓我自己可以設定。」

**現況**：對外免登入的入口散在 11 個地方、各自一種憑證、沒有任何開關——`public_router`（token 逐字比對）、`token_router`（短碼／編輯 token）、
`api_portal` public、`/q/{code}`、`/register`。有些面「同一件事兩條路」：雜支 `expense.html?t=<token>` 免登入，`?project=<id>` 卻要登入
（8/14 加的 CRM 匿名防線），稽核裡「登入即可寫雜支」那三支就是這條舊路的殘影。

**設計：一份登記表＋一個開關＋一個守衛**

1. 正本 `core/public_access.py`：`PUBLIC_SURFACES` 登記表，每一面一筆——鍵、名稱、誰在用、怎麼進（頁面／網址樣式）、支援的模式、預設值。

| 鍵 | 面 | 誰在用 | 怎麼進 | 支援模式 | 預設 |
|---|---|---|---|---|---|
| `expense` | 雜支登記（expense.html／group-expense.html／advance-expense.html） | 外部製片、臨時人員 | `?t=<連結 token>`（每個專案／子表／預支款各自一條，可撤銷）；`?project=<id>` 是內部路，要登入＋crm_projects | 關閉／連結 | 連結 |
| `media_log` | 影像紀錄上傳牆（media-log.html） | 劇組成員手機 | 專案 QR／`?token=`（每個專案一條） | 關閉／連結 | 連結 |
| `portal` | 客戶看片審批（review.html） | 客戶 | 看片連結 | 關閉／連結 | 連結 |
| `quote` | 報價單線上檢視（/q/{code}） | 客戶 | 短碼連結 | 關閉／連結 | 連結 |
| `invoice_file` | 發票影像分享 | 客戶／會計 | 分享連結 | 關閉／連結 | 連結 |
| `proposal_share` | 提案企劃可寫分享（project.html?t=、meeting-note.html；片庫分享是同一把 plan token，併在這面） | 客戶、外部顧問 | 分享連結（可寫） | 關閉／連結 | 連結 |
| `showcase_edit` | 結案上架編輯（showcase-edit.html） | 外部剪接、企劃 | 編輯 token | 關閉／連結 | 連結 |
| `staff_edit` | 員工自助編輯履歷（staff-edit.html） | 員工本人 | 編輯 token | 關閉／連結 | 連結 |
| `resume` | 公開履歷（resume.html?staff=<id>） | 對外 | 員工檔勾 resume_visible 才看得到（沒有 token） | 關閉／公開 | 公開 |
| `register` | 員工自助註冊（my.html 註冊區） | 新同事 | 公開（兩題公司知識） | 關閉／公開 | 公開 |

（官網對外 API `routers/website/public.py` 是網站本體，不進這張表。）

2. 模式語意（一句話）：**關閉**＝該面所有 public／token 端點回 404「此功能未開放」，頁面顯示同一句；**連結**＝現況（token 逐字比對、可撤銷）——
   owner 2026-09-08：「正常來說都是連結公開的」，所以**預設全部＝連結**；**公開**（連 token 都不用）只有履歷與註冊兩面有，因為它們本來就沒有 token。
   設定存 `settings.json public_access = {鍵: 模式}`。

3. 守衛只加一處：`public_router`／`token_router`／portal public／`/q/` 掛同一個 `Depends(surface_gate)`，用路徑前綴對回登記表的鍵
   （`/public/expense/` → expense、`/public/media-log/` → media_log …），查模式：關閉 → 404；連結／公開 → 放行到各端點原本的 token 檢查。
   雜支那三支「登入即可」端點（`?project=` 舊路）改成內部路：登入＋`crm_projects`；外部一律走 `?t=` 連結。稽核第一批第 3 項因此不用另外收。

4. 畫面：使用者管理第四個分頁「**公開區**」——每一面一列：名稱、誰在用、怎麼進（可複製的網址樣式）、目前有效連結數（有 token 表的面才算）、
   模式（關閉／連結／公開三選一）、一行說明。改完按「儲存」，立即生效（守衛每次讀設定，不用重啟）。管理員限定 `GET/PUT /auth/public-access`。

5. 測試：登記表每個鍵都對得到至少一支路徑（源碼掃描）；三種模式各一支端點的行為；分頁存讀一致。

**與其他階段的關係**：第二批「雜支寫入歸誰」——owner 說雜支本來就是外部人員在登，所以內部寫入改成 `crm_projects`（跟同畫面 cost-lines 一致），
外部那條由公開區管；財務端只留刪除給管理員。

### 階段 2：一致性（2～3 天，最有感）

- 規則寫死成測試：**一個分頁的模組鑰匙守它會打的每一支端點（讀＋寫）**；管理員限定只准出現在一份明列清單 `ADMIN_ONLY_ACTIONS`（刪除、專案狀態推進、錢流三表、設定、發版、使用者管理）。
- 把 CRM 那 51 支 `_check_auth` 逐支歸類：屬於分頁的改模組守衛（像今天 `update_project`／預算表那樣），真的要管理員的進清單。
- 源碼掃描測試（`_srcscan`）：`routers/crm/*` 每支端點不是模組守衛就得在清單裡，白名單外直接紅——以後不會再長出「看得到做不到」。
- 人員綁定的區塊（今天與這週、假勤、零用金、福委）：沒綁定時畫面直接顯示「此帳號尚未綁定人員檔案」＋管理員入口，不是空白。

### 階段 3：權限畫面重整（1～2 天）

✅ 已做（2026-09-08）：每把鑰匙一行相依說明（`MODULE_HINTS`，title＋灰字）、`money_view`／`me_today_zone` 父子縮排、沒綁人員檔案的紅字、每個帳號一顆「以他的角度看」（照目前勾的、未儲存也算：頂層分頁走 `shouldShowTab` 同一個閘門、員工工作台區塊、財務分頁完整／缺金額檢視、手機版進不進得去、管理員限定清單）。工作面分群沿用 tab-config 的 PERMISSION_GROUPS（沒另開一份）。

- 47 把鑰匙改成**工作面分群＋父子縮排**（照 `PERM_PARENT` 那套推廣）：後期製作、前期製作、CRM、帳務與錢流、員工工作台（總開關＋子鑰匙）、人事、系統。
- 每把鑰匙旁一行說明＋相依（「需綁定人員檔案」「指名才有，管理員也不含」「兼職排班還看對方身份」）。
- 同一張卡放**人員綁定＋身份**（在職／合夥／兼職），沒綁的紅字。
- 「以他的角度看」：唯讀預覽這個帳號會看到哪些分頁、哪些按鈕灰掉。

### 階段 4：收鑰匙（可選，1 天＋一次回填）

資料支持合併：有後期任一把的帳號（Web／Assitance／liyuliu／nashtsai／chiehyu）都是整組 9 把一起有。
- 後期 9 把 → `postprod` 一把；前期 6 把 → `preprod` 一把（片庫另留）；`hr_leave`／`hr_benefits` 併成 `hr`。47 → 約 25。
- 做法跟今天一樣：開機一次性回填（settings 旗標）、舊鑰匙保留一版當別名（Cloudflare 對 `.js` 有 4 小時快取）。

## 4. 刻意不做

- 不做角色表／角色管理 UI（RBAC v2 的決定；範本只是「填勾選的捷徑」）。
- 不改 token 結構、不改守衛簽名（階段 2 是換守衛函式，不是換機制）。
- 不動 `finance_mine`「指名才有」與 Lv3 唯一硬閘門。

## 5. owner 已拍板（2026-09-08）

1. 合夥＝除了私帳以外全部；在職／兼職 owner 在「身份範本」分頁自己勾（已做）。
2. **推進專案階段（啟動專案、改階段）開給 `crm_projects`**（`core/project_flow.ADVANCE_MODULES=("crm_projects",)`）。
3. **假勤核准留管理員**：`hr_leave` 一把管看與登記，核准／退回／補休發放／特休額度仍 Lv3，前端依 Lv3 藏。
4. 雜支與影像紀錄對外部人員公開，走「公開區」（階段 1.5）；內部雜支寫入改 `crm_projects`。
5. `ADMIN_ONLY_ACTIONS`（就算有分頁鑰匙也要管理員）：刪除（客戶／員工／報價／雜支／作品集）、三支 CSV 匯入、全站根目錄與設定、歸檔 folders／scan、Sheet 拉取與代填工時正本、私帳對映與預算、假勤核准、intel 設定與 run、提案分享／刪除、公布欄 ask／delete／reorder、官網發佈（website_admin）、使用者管理／設定／發版。
6. 階段 4（收鑰匙）：未定。

## 6. 順序與工時

| 階段 | 工時 | 效果 |
|------|------|------|
| 0 看得見 | 0.5–1 天 | 403 有原因、有記錄、有一鍵開通；稽核一次修掉現存的「看得到做不到」 |
| 1 範本 | 1 天 | 新同事 5 分鐘上線 |
| 2 一致性 | 2–3 天 | 之後不會再長出這類 403（測試釘住） |
| 3 畫面 | 1–2 天 | 管理員不用記相依 |
| 4 收鑰匙 | 1 天 | 47 → 25 |

建議 0 → 1 → 2 先做（約一週），3、4 看 0 的記錄再決定。

## 附錄：47 把鑰匙逐把比較（2026-09-08；✓ 給、— 不給、？ 待 owner 拍板）

| 鑰匙 | 現在誰有（合夥 2 人／在職 4 人／兼職 3 人） | 管理員 | 合夥（拍板） | 在職（建議） | 兼職（建議） |
|---|---|:-:|:-:|:-:|:-:|
| **公布欄／總覽** | | | | | |
| 公布欄 `bulletin` | 合夥 0/2、在職 0/4、兼職 0/4 | ✓ | ✓ | ？ | — |
| 專案總覽 `projects` | 合夥 2/2、在職 0/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| **前期製作** | | | | | |
| 拍攝企劃 `preprod_plan` | 合夥 2/2、在職 1/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| 場景庫 `preprod_locations` | 合夥 2/2、在職 1/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| 提案庫 `preprod_proposals` | 合夥 2/2、在職 1/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| 產業情報 `intel` | 合夥 2/2、在職 1/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| 器材庫 `equipment` | 合夥 2/2、在職 1/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| 片庫 `references` | 合夥 2/2、在職 1/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| **後期製作** | | | | | |
| 備份 `backup` | 合夥 2/2、在職 3/4、兼職 1/4 | ✓ | ✓ | ✓ | — |
| 比對 `verify` | 合夥 2/2、在職 3/4、兼職 1/4 | ✓ | ✓ | ✓ | — |
| 轉檔 `transcode` | 合夥 2/2、在職 3/4、兼職 1/4 | ✓ | ✓ | ✓ | — |
| 串帶 `concat` | 合夥 2/2、在職 3/4、兼職 1/4 | ✓ | ✓ | ✓ | — |
| 空拍寫入 `drone_meta` | 合夥 2/2、在職 3/4、兼職 1/4 | ✓ | ✓ | ✓ | — |
| 報表 `report` | 合夥 2/2、在職 3/4、兼職 1/4 | ✓ | ✓ | ✓ | — |
| 逐字稿 `transcribe` | 合夥 2/2、在職 3/4、兼職 1/4 | ✓ | ✓ | ✓ | — |
| 語音 `tts` | 合夥 2/2、在職 3/4、兼職 1/4 | ✓ | ✓ | ✓ | — |
| 素材庫 `footage` | 合夥 2/2、在職 3/4、兼職 1/4 | ✓ | ✓ | ✓ | — |
| ComfyUI `comfyui` | 合夥 0/2、在職 2/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| **業務管理** | | | | | |
| 客戶 `crm_clients` | 合夥 2/2、在職 0/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| 專案管理 `crm_projects` | 合夥 2/2、在職 1/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| 報價 `crm_quotes` | 合夥 2/2、在職 0/4、兼職 0/4 | ✓ | ✓ | ？ | — |
| 審批門戶 `portal` | 合夥 2/2、在職 0/4、兼職 0/4 | ✓ | ✓ | — | — |
| 影像紀錄 `media_log` | 合夥 2/2、在職 1/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| 官網管理 `website_admin` | 合夥 2/2、在職 0/4、兼職 1/4 | ✓ | ✓ | ✓ | ✓ |
| **人事** | | | | | |
| 員工檔案 `crm_staff` | 合夥 0/2、在職 0/4、兼職 0/4 | ✓ | ✓ | — | — |
| 工作追蹤（全員工時） `timesheets` | 合夥 0/2、在職 1/4、兼職 0/4 | ✓ | ✓ | ？ | — |
| 請補修（全員） `hr_leave` | 合夥 0/2、在職 1/4、兼職 0/4 | ✓ | ✓ | — | — |
| 福委會（管理） `hr_benefits` | 合夥 0/2、在職 0/4、兼職 0/4 | ✓ | ✓ | — | — |
| 週誌 `journal` | 合夥 0/2、在職 4/4、兼職 0/4 | ✓ | ✓ | ✓ | ？ |
| **帳務與錢流** | | | | | |
| 帳務（發票／請款） `crm_invoices` | 合夥 2/2、在職 0/4、兼職 0/4 | ✓ | ✓ | — | — |
| 金額檢視 `money_view` | 合夥 2/2、在職 0/4、兼職 0/4 | ✓ | ✓ | — | — |
| 零用金審核 `finance_approve` | 合夥 2/2、在職 0/4、兼職 0/4 | ✓ | ✓ | — | — |
| 母公司報表 `finance_partner` | 合夥 2/2、在職 0/4、兼職 0/4 | ✓ | ✓ | — | — |
| 私帳（我的帳） `finance_mine` | 合夥 0/2、在職 0/4、兼職 0/4 | ✓ | — | — | — |
| **員工工作台** | | | | | |
| 基本資料 `me_profile` | 合夥 2/2、在職 4/4、兼職 4/4 | ✓ | ✓ | ✓ | ✓ |
| 今天與這週（總開關） `me_today_zone` | 合夥 2/2、在職 4/4、兼職 4/4 | ✓ | ✓ | ✓ | ✓ |
| 今天的專案紀錄 `me_worklog` | 合夥 2/2、在職 4/4、兼職 3/4 | ✓ | ✓ | ✓ | ✓ |
| 我的一週 `me_week_plan` | 合夥 2/2、在職 4/4、兼職 4/4 | ✓ | ✓ | ✓ | ✓ |
| 團隊的一週 `me_team_week` | 合夥 2/2、在職 4/4、兼職 4/4 | ✓ | ✓ | ✓ | ✓ |
| 專案查詢 `me_project_lookup` | 合夥 2/2、在職 4/4、兼職 1/4 | ✓ | ✓ | ✓ | ✓ |
| 兼職排班 `me_plan_parttime` | 合夥 0/2、在職 4/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| 我的請假 `me_leave` | 合夥 0/2、在職 0/4、兼職 0/4 | ✓ | ✓ | ✓ | ？ |
| 零用金 `me_petty` | 合夥 1/2、在職 4/4、兼職 0/4 | ✓ | ✓ | ✓ | ？ |
| 我的福委會 `me_benefits` | 合夥 1/2、在職 3/4、兼職 0/4 | ✓ | ✓ | ✓ | — |
| 我的專案 `me_projects` | 合夥 0/2、在職 1/4、兼職 1/4 | ✓ | ✓ | ✓ | ？ |
| 我的待辦 `me_todos` | 合夥 0/2、在職 0/4、兼職 0/4 | ✓ | （畫面未開） | （畫面未開） | （畫面未開） |
| 我的工時請款 `me_finance` | 合夥 0/2、在職 0/4、兼職 0/4 | ✓ | （畫面未開） | （畫面未開） | （畫面未開） |

說明：合夥＝owner 拍板「除了私帳以外全部」。在職＝合夥扣掉錢流、帳務、審批門戶、人事管理端（員工檔案／全員請補修／福委會管理）。兼職＝基本資料＋今天與這週整套＋官網。「？」是我拿不準的：在職要不要公布欄／報價／工作追蹤總表；兼職要不要週誌／請假／零用金／我的專案。
